# CLAUDE.md — AXIOM DataOps Agent

> **NEXT SESSION STARTS HERE (as of 2026-08-05).** Item 6 (long-running
> AXIOM tasks — see `docs/PRODUCT_AUDIT.md` section 1.8/1.9 for the full
> design) is now the active thread, superseding the banner below until
> it's done. Design fully locked in section 1.9 (5 core questions + 4
> amendments — failure semantics, structural verification, visibility,
> termination, mid-task approvals, plan provenance, role re-read at
> execution time, 3 narrow v1 task shapes) — read that section before
> touching any of this. **Stage 1 (schema + migration + the
> `tasks.manage_all` capability, no behavior) is done and live-verified**
> — see the Status Table row below. **Stage 2 (planning phase: goal+shape
> → a real persisted, human-reviewable plan) is next.** Everything below
> this paragraph is the pre-item-6 state of the project, kept for history
> — start with item 6 stage 2, not with anything in the old banner.
>
> **NEXT SESSION STARTS HERE (as of 2026-08-02, superseded above).** All 5 Phase 19 P0 items
> are done, and the **migration-drift session is now also done** (see
> below) — the **dark-theme screenshot verification pass** (token redesign
> already landed in `tokens.css`, the visual pass across every
> screen/both themes is the only thing left open on that thread — needs
> zero LLM calls, so quota isn't a blocker) is the only unstarted item
> left from the two this file's banner had been alternating between.
> Full breakdown of what's done/remaining/undecided/accepted-for-launch/
> domain-gated is in `FRONTEND_BUILD_PLAN.md`'s "Phase 19 partition
> status" section — read that section before picking up any Phase 19
> work.
>
> **Public-launch risk cluster (2026-08-02) — OPEN, approved order:
> (c) → (b) → provision infra → (e) → (a) → (d).** Proposed and approved
> in a docs-only planning pass (size/blast-radius/order for all 5 items),
> then execution started same session. **(c) and (b) are both done, fully
> live-verified — see below.** Next up: **provision `wunomo.in`/
> `api.wunomo.in` infra**, then (e) backups, then (a) flip to paid LLM
> keys (real per-turn token cost already measured — see below — plus a
> live pricing pull still owed before enabling billing), then (d) R2
> migration (confirmed **not** launch-blocking — target host is a plain
> VPS with a persistent Docker volume, so this genuinely follows rather
> than blocks). **Also outstanding from this cluster, not yet done**: pull
> live current Gemini/Groq paid-tier pricing and compute a real monthly
> cost at 20 users × 5 turns/day using the ~3,419 avg-tokens/turn figure
> below; and note in the eventual deployment checklist to set a hard
> spend cap in each provider's own console when paid keys go live — an
> independent, provider-level backstop alongside (b)'s app-layer fix, not
> a substitute for it.
>
> **(b) change-plan free-upgrade hole — RESOLVED (2026-08-02), Option 1:
> disabled self-serve entirely, not the payment_verified/usage-check
> architecture originally proposed as the alternative.** `POST
> /change-plan` now always `501`s for any caller who passes the existing
> role gate — matching the checkout/webhook honest-stub pattern exactly.
> `BillingService.change_plan()` itself is untouched and stays real,
> now documented as the sanctioned way to manually provision a real
> paying customer's plan before Stripe exists (direct service call or a
> raw `Tenant.plan` SQL write — either bypasses the disabled endpoint on
> purpose). Live-verified against the real running server: a real Owner's
> direct `curl` bypass attempt got a real `501` with the plan provably
> unchanged (checked via both the API and a direct DB query); a demoted
> Viewer still correctly `403`s (role gate fires first, unchanged from
> before); the administrative path was exercised for real (a direct
> `BillingService.change_plan()` call inside the container) and
> genuinely persisted. Frontend's plan-picker UI removed, not just
> disabled. Full backend suite: 255 passed. See the now-resolved
> Known-broken row and the new Status Table row for full detail.
>
> **(c) WebSocket chat auth — RESOLVED BY DELETION, not hardened
> (2026-08-02).** Before touching this, grepped the entire frontend
> (`frontend/src/**`) for any WebSocket usage and found zero references —
> confirmed independently by an earlier, already-on-record
> `USER_MANUAL.md` audit finding the identical thing. The only backend
> reference to the route was its own definition; no test file touched it
> either. **Deleted `WS /ws/chat/{tenant_id}/{session_id}` entirely from
> `main.py`** (route handler + the now-unused `WebSocket`/
> `WebSocketDisconnect` imports) rather than adding auth to it, per
> explicit instruction — smaller, safer, permanently removes the attack
> surface instead of hardening a path nothing legitimate depends on.
> Live-verified: backend restarted clean (`Application startup
> complete`), `GET /health` still `200`, and the old WS path now returns
> a plain `404 {"detail":"Not Found"}` — byte-identical to a genuinely
> nonexistent route, confirming it's gone, not just erroring differently.
> Full backend suite: 255 passed, zero regressions (expected — nothing
> real ever called it). See the now-resolved "WebSocket chat auth"
> Known-broken row and the new Status Table row for full detail.
>
> **Migration-drift session (2026-08-02) — done.** Verified
> `alembic upgrade head` against a genuinely empty, isolated scratch
> Postgres 16 container (never the dev DB, never touched or restarted)
> and found real drift: `pipeline_commits`/`pipeline_deployments`
> (`models/cicd.py`) had **no** migration creating them at all — a fresh
> chain died with `UndefinedTable`. Fixed with a new migration,
> `609092d6978f_add_cicd_pipeline_tables.py`, inserted between
> `7e3773979374` and `db3efa7d4d82`, creating both tables plus the 3 enum
> types (`cicdstatus`/`gatedecision`/`deploymentstatus`) that only ever
> existed via `create_type=False` declarations in the model — meaning
> nothing, not even `create_all()`, could ever create them from scratch
> (a real, previously-undocumented manual step this repo has never
> committed anywhere — see the rewritten Alembic Gotcha below). Also
> found and fixed a real, separate gap via an order-independent
> `information_schema` diff (not a raw `pg_dump` text diff): `team_invites`
> was missing its `ix_team_invites_created_at` index in `a4f7c92b1d05`
> even though the model has declared it since Phase 15. The long-suspected
> `incidentstatus` lowercase-vs-uppercase enum drift turned out to be
> **dead code, not live drift** — migration order means the correct
> uppercase type is created first and the wrong-looking later attempt is
> silently swallowed by Postgres's `duplicate_object` exception; corrected
> the misleading value list anyway. **Final proof**: an alembic-upgraded
> schema and a `create_all()`-built "known-good" schema, built on two
> separate empty scratch DBs, diffed via `information_schema` across
> tables/columns/indexes/foreign keys/primary keys/enum labels —
> byte-identical across all 24 tables, zero remaining diff. See the
> Status Table's "Migration-drift verification" row and the rewritten
> Alembic Gotcha for full detail. **The plain-language version of the
> significant finding, stated explicitly per the user's own request:
> neither Alembic nor `create_all()` could bootstrap a genuinely empty
> database — the project was effectively unbuildable from scratch, and
> nothing in the repo would have told anyone that, since the real dev DB
> only ever worked because of an undocumented manual step from
> unknown-when.** **Production domain purchased**: `wunomo.in` (frontend),
> `api.wunomo.in` (API) — recorded in Outstanding items below; nothing is
> configured yet, this was a value-recording pass only, done in the same
> session but kept separate from the migration work.
>
> **Two follow-ups requested and closed in a same-day follow-up pass
> (2026-08-02):** (1) **Idempotency, confirmed for the real deploy case.**
> Re-running `alembic upgrade head` against an already-fully-migrated
> scratch DB (simulating "runs on every deploy, not just the first") is a
> clean no-op — zero SQL executed, exit 0 — because Alembic's own
> `alembic_version` tracking skips every already-applied revision
> entirely; this holds regardless of any individual migration's SQL
> content. Also tested the narrower edge case of *lost* version tracking
> against an already-migrated schema (simulating a corrupted/reset
> `alembic_version` table) — this does fail hard on a full replay, but
> at the very first, oldest, pre-existing migration (`7e3773979374`), not
> the new one this session added; every migration in the chain except
> `db3efa7d4d82`'s two defensively-wrapped statements shares this same
> non-defensive property, so the new migration is no more fragile than
> what was already there. See the new Gotcha for full detail. (2)
> **Drift-detection CI check — proposed and sized, not built**, per
> explicit instruction. A single `compare_metadata()`-based pytest test
> (Alembic's own built-in schema-vs-models diff primitive) against a
> throwaway CI Postgres would have caught this entire migration-drift
> problem months earlier. Estimated **small** — roughly 60-100 lines, one
> new test file, likely no new CI infrastructure since this project's
> test suite already needs a live Postgres. Full proposal, caveats, and
> sizing rationale in the new Gotcha entry right after the idempotency
> one — read it before deciding whether to build it.
>
> **Interleaved YC-demo-prep session (2026-07-23/24, not a Phase 19
> session — the above recommendation still stands for the next real build
> session).** Produced a one-command demo tenant reset
> (`dataops-agent/scripts/demo_reset.mjs` + `demo_unbreak.mjs`), a demo
> runsheet artifact, and `docs/PRODUCT_STATUS.md`. Found and fixed 2 real
> bugs (file connector `rows_processed: 0`; an empty-list tool result
> crashing `POST /chat/` on Groq) and documented 1 critical, unfixed one:
> **`PolicyEngine.execute_approved_action()`'s dispatch is broken for all
> 8 registered actions** — approving a blocked AXIOM action does not
> execute it (see the new Known-broken row). Also removed the topbar's
> decorative notification bell/env dropdown and wired "AXIOM Online" to a
> real `GET /health/db` poll, per explicit user approval. `PRIMARY_LLM_MODEL`
> was temporarily switched to Groq for testing and confirmed restored to
> `gemini-3.5-flash` before ending — see the Gotcha on this pattern.

**Read this file in full before doing any work in this repo.** It is the living contract for how Claude Code operates here. Update it (Status Table + Known Gotchas, at minimum) in the same commit as any fix, feature, or discovery that changes what's true below.

Repo root: `c:\Pratyaksh Personal\My Projects\ai workforce\`. The one real project inside it is `dataops-agent/` — "AXIOM", an autonomous AI DataOps agent (part of the "AI Workforce Systems" product line). Everything else at repo root (`employee_data.csv`, `sales_data.csv`, `pipeline_config.json`, `tree.txt`, `generate_dataops_project.py`, `Document/*.pdf`) is scaffolding/sample/reference material, not application code, **except `FRONTEND_BUILD_PLAN.md`**, which is a real, authoritative planning document — read it before doing any frontend work. `generate_dataops_project.py` is the original scaffold generator that wrote out `dataops-agent/` — treat it as historical, not as the source of truth; the files it once generated have since been hand-edited (see patch scripts in Archive).

This repo now has git history: an initial commit capturing the scaffold as originally built (after cleaning ~11.5k stray `node_modules`/`.next` files out of the index — see Archive), followed by one commit per individually-verified fix. Keep following the one-fix-per-commit rule for everything from here on (Workflow Rule 6).

---

## 0. Current Position (read this first)

**Backend:** feature-complete and verified through Phase 4 of backend testing (see
Status Table below) for the REST API surface — auth, sources, pipelines, quality,
incidents, CI/CD, approvals, governance, transforms are real and live-tested via
direct API calls. **The AXIOM chat agent's tool-calling surface punch list is fully
closed** — the LangChain v0.3→v1.x ecosystem upgrade (`langchain` 1.3.14,
`langchain-core` 1.4.9, `langgraph` 1.2.9, `langchain-google-genai` 3.2.0,
`langchain-groq` 1.1.3, `langchain-community` 0.4.2) landed the `thought_signature`
support Gemini 3 tool-calling needed, and all 22 broken tool call sites across
`governance_tools.py`, `observability_tools.py`, `orchestration_tools.py`,
`quality_tools.py`, `reporting_tools.py`, and `transformation_tools.py` were fixed
file-by-file and live-verified (see the per-file Status Table rows and the resolved
Known-broken entry). Gemini 3.5 tool-calling is proven live as primary with zero
Groq fallback (a real 3-turn, 5-tool-call conversation). Don't trust "the agent can
do X" for anything you haven't personally live-verified through a real chat request
— that discipline is what caught every bug in that punch list.
**Gemini's `limit: 0` dead-end is resolved** — root cause was a deprecated model
(`gemini-2.0-flash`), not billing. Primary model is `gemini-3.5-flash` (switched from
`gemini-3-flash-preview` after hitting a hard daily cap — see the corrected Gotcha
below: **this cap is a flat 20 requests/day tied to this specific key/project, not a
preview-vs-GA distinction** — `gemini-3.5-flash` hits the identical `limit: 20` wall).
The `iteration_count`/`recursion_limit` miscalibration fix and every other hardened
agent mechanism (message bounding, tenant_id force-override, timeout/fallback) are
re-verified live and hold on the current stack. **Both providers' daily quotas are a
real, recurring operational constraint, not a one-off** — Groq's shared org-level TPD
budget and Gemini's 20/day cap have each been independently exhausted mid-session
more than once now. Budget both deliberately in any session doing live-LLM
verification, and when a session's env temporarily points `PRIMARY_LLM_MODEL` at a
different model for testing, always confirm it's restored via a real config check
before ending — `docker compose restart` does not reload `.env` changes, only
`up -d --force-recreate <service>` does (see Gotchas).

**Frontend:** Phase 0 (design sign-off), **Phase 2 (scaffold + design system), and
Phase 3 (auth design) are all complete.** `dataops-agent/frontend/` is a real Next.js 15 App Router project now
(not just reference docs) — locked tokens in `src/styles/tokens.css` (light + dark),
self-hosted real font files in `public/fonts/` (General Sans/Fraunces/JetBrains Mono,
latin subset, no CDN calls), and a 10-component core library in `src/components/ui/`
(Button, Card, Badge, Input/Select, Table, Modal, Toast, Tabs, Progress, Skeleton).
`npm run build` and `npm run dev` both verified clean; component library visually
verified in both themes via Playwright screenshots against a live dev server — this
caught and fixed 3 real dark-mode contrast bugs that code review alone would have
missed (see `tokens.css`/`components.css` comments and the git log). **Phase 3's auth
design is fully locked** — password, Google OAuth, and email-code (passwordless, via
Resend) — see `FRONTEND_BUILD_PLAN.md`'s "Phase 3 decisions" section for the complete
design; zero auth code has been written yet, this was a design-only milestone. Full plan —
19 phases across two parallel backend tracks (auth/profile, and billing/metering/
team) interleaved with frontend build phases — is in **`FRONTEND_BUILD_PLAN.md`**
at repo root; read it before starting any frontend work. Locked decisions:
- Typography: General Sans (UI/body) + Fraunces (display) + JetBrains Mono (code), self-hosted
- Palette: Midnight `#122C4F` / Pearl Perfect `#FBF9E4` / Noir `#000000` / Ocean `#5B88B2` — derived token values in `dataops-agent/DESIGN_TOKENS.md`
- Stack: Next.js 15 App Router, Zustand, Chart.js + react-chartjs-2, ported design-system CSS (not Tailwind), JWT in localStorage for v1

**Next action:** Phase 1 (usage metering foundation) is implemented and mostly
live-verified — see Status Table — with 2 of 3 call sites (`incident_triage`,
`transform_generation`) still needing a live re-check once Gemini/Groq quota
allows (regression-tested and code-identical to the proven path in the meantime).
**Phase 4 (onboarding profile storage) is complete and live-verified.** **Phase 5
("Non-password auth: Google OAuth + email-code") is complete and fully live-verified
end-to-end for all 3 methods** — see Status Table's two rows, including a real user
click-through of Google's actual consent screen (auto-linked onto an existing
email-code-created account, confirming auto-link works from either originating
method). **Phase 6 (Auth + Onboarding UI) is complete and fully live-verified** —
real signup/login/onboarding pages for all 3 auth methods, the login disambiguation
workspace picker, and the non-blocking signup nudge, all wired to the real backend
and live-verified via a Playwright script driving a real browser plus a real user
click-through of Google's consent screen (which surfaced and got a real fix for a
clock-skew bug — see Gotchas). Building this UI also surfaced and fixed a real
backend gap: the workspace-picker resubmit design didn't actually work for
email-code/Google (see Status Table and Gotchas). **Phase 7 (App shell) is
complete and fully live-verified** — sidebar/topbar/command palette
(Ctrl+K)/notifications panel/AXIOM FAB/light-dark theme toggle, wrapping 18
authenticated routes (17 new stubs + the Phase 6 dashboard stub, moved into
a new `(app)` route group) behind its own auth guard. Pure frontend, no
backend changes. **Phase 8 (Chat sessions + tool trace) is complete and
fully live-verified** — `GET /chat/sessions` (private-per-user, no
migration needed) and a real tool-call trace now populate the
previously-unused `ChatMessage.tool_calls` column; the live-LLM round trip
that was blocked by exhausted quota at the end of the Gemini-migration
session was re-attempted successfully once Groq's quota freed up — a real
chat message produced a real Groq tool call (`list_data_sources`, real
tenant_id, real result), correctly stored and retrievable via both the
`POST /chat/` response and `GET /chat/sessions/{id}/history`, and the
session correctly appears in the list. No frontend chat UI was built this
phase — that's Phase 10, which also needs the LangChain v1 upgrade first
for Gemini tool-calling (see Known-broken).
**Phase 9 (Dashboard) is complete and fully live-verified** — 7 KPI cards,
a real-incident-only health banner, two Chart.js trend charts, Recent
Pipeline Runs, a Pending Approvals card wired to real approve/reject
actions, and an AXIOM Activity card reading Phase 8's real session list —
all backed by real endpoints (`GET /analytics`, `/analytics/quality`, and
a new `/analytics/recent-runs`), no fabricated data anywhere. `@tanstack/
react-query` and `chart.js`/`react-chartjs-2` introduced this phase, per
plan. Two real bugs caught by live screenshot verification (not code
review) and fixed in the same commit: `.stats-grid`/`.metric-card`/
`.metric-value`/`.metric-change` were referenced from the prototype but
never actually ported into this project's CSS back in Phase 2 (cards
rendered as unstyled stacked rows); and two KPI cards used
`--midnight-500`/`600` as value text color, the same dark-on-dark
contrast bug documented earlier this project. Also fixed a real
regression this phase would have caused: replacing the dashboard stub
would have removed the only logout affordance in the whole app (Phase
7's topbar avatar only ever showed an identity toast) — added a proper
account dropdown with email/role + Log out.
**Cross-cutting fix, done ahead of Phase 12**: the silent-401-as-empty-state gap
flagged in Phase 9's verification (a Gotcha, not a Known-broken row) is now fixed —
`frontend/src/lib/api.ts`'s shared `request()` function now detects a 401 on any
request that carried a bearer token, clears the session, and redirects to
`/login?expired=1` (toast + URL cleanup on the login page). Live-verified with a
genuinely expired, correctly-signed JWT. See Status Table.
**Phase 12 (Core DataOps screens) is complete and fully live-verified** — Sources,
Pipelines, Quality, Incidents, CI/CD, Approvals (merged), and Governance all built,
replacing their Phase 7 stubs, each wired to real backend endpoints with create/
update/delete/action flows and live-verified via Playwright (both themes
screenshotted, no contrast issues). Pulled ahead of Phase 10 since Phase 10 is
blocked on the LangChain v1 upgrade and Phase 12's backend surface had zero LLM
dependency. Three real bugs found via live verification (not code review) and fixed
in the same commits as the screens that surfaced them — see their own Status Table
rows for detail: `DELETE /pipelines/{id}` 500ing on any pipeline with run history
(found building Pipelines); the merged-approvals endpoint's risk-level bucketing
using a 0-1 scale against a real 0-100 `risk_score` (found building CI/CD); and
Governance's Audit Log tab not invalidating after contract create/validate, showing
a stale pre-mutation snapshot (found building Governance). Also pulled forward
Phase 11's lineage-auto-population half as pre-work (see its own Status Table row)
since Governance's lineage tab needed it to not be empty — Phase 11's remaining
scope (`KpiValue` writes on quality-check runs) is unaffected and still pending.
`npm run build` (production) verified clean after fixing 2 real ESLint errors
(`react/no-unescaped-entities` in Incidents, unused imports in Sources) that the
dev-server-only verification loop for those two screens hadn't caught.
**Phase 10 (AXIOM chat UI) is complete and fully live-verified.** Real 3-panel
screen (`frontend/src/app/(app)/chat/page.tsx` + `src/components/chat/*`) wired to
`GET /chat/sessions`, `GET /chat/sessions/{id}/history`, and a new
`POST /chat/` client — session list, saved prompts (client-side, per the locked
gap-analysis decision), the composer, personality/operation mode selectors, real
tool-call trace rendering, and a real "Attached Context" data-source picker (sends
`context: {source_id, source_name}`, which the agent actually reads — see Status
Table). **Zero-cost checks are fully live-verified** (session list, empty state,
saved prompts, attached-context picker, both themes, via Playwright against the real
backend — no LLM calls needed for any of this). **Live-send checks are only
partially verified**: the graceful-error path is proven live (a real backend 500
degrades to a clear inline message, composer stays usable, no hang — see Status
Table), which also caught and fixed a real dead-code bug (`isError` render branch
was unreachable). **All previously-deferred live-send checks are now closed,
live-verified in a follow-up session** — Gemini's daily quota had reset by then,
confirmed fresh with a cheap probe before spending anything further. Verified live
via a real Playwright-driven browser against a real seeded tenant: multi-turn
continuity (a real 2-turn conversation — turn 2, "how many sources did you find,"
correctly answered by referencing turn 1's tool result with zero re-call of the
tool, confirmed against the real `GET /chat/sessions/{id}/history` payload, not
just the rendered UI); tool-call trace rendering (both the inline per-message block
and the right panel's session-wide aggregate render the same real trace — two
`.chat-tool-name` DOM nodes per call is correct-by-design, one per panel, not a
duplication bug, since `ContextPanel` renders its own `ToolCallBlock` for the
aggregate log); personality/operation mode switching (network-captured the real
`POST /chat/` request payload and confirmed `personality_mode`/`operation_mode`
matched the UI selectors exactly — `founder`/`advisory` selected, `founder`/
`advisory` sent); and the advisory-mode approval-gate flow (a real blocked
`list_data_sources` call under `operation_mode: "advisory"` rendered the correct
"Needs approval" badge and callout copy with a working link to `/approvals`, **and
settled the approval-persistence question with live proof**: a real
`ApprovalRequest` row was written and appeared via both `GET /approvals` and
`GET /approvals/merged` — the prior code-read finding was correct, `api/v1/chat.py`'s
own `ApprovalRequest` creation, not `approval_gate_node` itself, is what persists
these; see the now-resolved Known-broken row). **This same pass caught and fixed a
real, previously-unknown bug**: every blocked call reported `risk_level: "high"`
regardless of its actual tier, because `approval_gate_node` never attached a
`risk_level`/`reason` to the blocked call dict — `chat.py`'s
`pa.get("risk_level", "high")` always hit that hardcoded default. Fixed (now
threads the real `personality.get_risk_level()` per call) and re-verified live: the
same `list_data_sources` call now correctly reports `"low"` with a real reason
string, in both the API response and the DB row. Regression test added
(`test_blocked_tool_call_carries_its_real_risk_level_not_hardcoded_high`,
`test_agent_graph.py`); full suite 103 passed. All Gemini calls in this pass ran on
`gemini-3.5-flash` primary with zero Groq fallback triggered (~5 real Gemini calls
total, well inside the 20/day cap — Groq's own quota was never actually needed for
volume, since Gemini handled every check cleanly). **Phase 10 is now fully complete
and live-verified — no outstanding checks.**
Note: `chart.js`/`react-chartjs-2` installed as of Phase 9 (Dashboard charts, their
trigger point). Zustand still isn't installed — Dashboard's data is all server state via
React Query, no client-side global state complex enough to need it yet; it lands
whenever a phase actually needs it.

**Outstanding on the user's side:** none currently — Google OAuth credentials
(resolved, Phase 5/6/7) and Gemini's `limit: 0` dead-end (resolved, see above) were
the only two open items and both are closed.

**Phase 13 (transform-run persistence + Data Catalog aggregation) is complete and
fully live-verified.** New `TransformRun` table persists every real SQL/pandas
transform execution (never dry-runs/EXPLAIN or generation-only calls) — wired into
`POST /run/sql`/`POST /run/pandas` (`origin: "manual"`) and the chat tools
`execute_sql_transform`(`dry_run=False`)/`run_python_transform`
(`origin: "chat_agent"`), the latter two gaining `user_id`/`session_id` params so
`agent_node`'s existing generic force-override loop covers them automatically (same
pattern already proven for `request_approval`). New `GET /api/v1/catalog/` is a thin,
tenant-scoped aggregation over every `DataSource.schema_snapshot` — no new table —
flattening each source's real per-table-keyed snapshot shape into one normalized
list, including never-profiled sources (`profiled: false`) rather than hiding them.
**Two real bugs found and fixed along the way, both live-verified**: (1)
`TransformGenerator._resolve_schema()` read `snapshot.get("columns", [])` assuming a
flat shape, but `SchemaProfiler` always nests columns under a table-name key with
different column-dict field names (`column_name`/`data_type`/`is_nullable`, not
`name`/`type`/`nullable`) — every NL→SQL/pandas generation had been running
schema-blind regardless of how much real profiling data existed; fixed, and a real
Gemini call now correctly generates SQL referencing real, otherwise-unguessable
column names. (2) `invoke_llm()` (`services/llm_service.py`, a separate call path
from the agent graph — used by `TransformGenerator`, `IncidentManager`, etc.)
crashed on Gemini 3.5's list-shaped structured content blocks, the same failure
class as the agent-graph's `_content_as_text` fix but on an uncovered call path;
fixed by moving that normalization into a shared `content_as_text()` in
`llm_service.py`. All Phase 13 work live-verified against a real CSV file (a
successful filtered `run/pandas` execution and a real syntax-error case, both
correctly persisted; the source's real profiled schema correctly reflected by the
Catalog endpoint). Full suite: 119 passed.

**Phase 14 (Transforms + Data Catalog screens) is complete and fully
live-verified.** A Phase 13 addendum landed first: `GET /api/v1/transformations/runs`
(tenant-scoped, newest-first, `limit`/`offset`/`source_id` filter) — Phase 13 built
`TransformRun` persistence but no read endpoint, and the History tab needed one.
Frontend: `frontend/src/app/(app)/transforms/page.tsx` — 4 tabs (Natural Language,
SQL Editor, Python Editor, History), all wired to real endpoints (`generate/sql`,
`generate/pandas`, `run/sql`, `run/sql/dry-run`, `run/pandas`, `preview/pandas`,
`explain`, and the new `runs` list), a "Send to Editor" handoff from NL results, and
Replay (pure frontend state — loads a past run's code/source back into the right
editor and switches tabs, no backend replay endpoint). `frontend/src/app/(app)/catalog/page.tsx` —
real `GET /catalog/` data, client-side search over table/column/tag names, and a
Sync Metadata action that sequentially calls the existing `POST /sources/{id}/profile`
per unique source with "profiling N of M" progress (Catalog otherwise had no refresh
path). Added a `.code-block` CSS class (`components.css`) — referenced in the master
design prototype but never actually ported, same "prototype class never made it into
our stylesheet" bug class documented elsewhere in this file; used `--midnight-900`/
`--on-dark` per the established permanently-dark-surface pattern. **Live-verified
end-to-end**: a real `employee_data.csv` source uploaded/registered/profiled; a real
Gemini call generated pandas code that correctly referenced real column names
(`department`/`name`/`salary`), confirming Phase 13's schema-grounding fix still
holds on a second, independent source; Execute ran the real code and returned 4 real
filtered rows; the run persisted and appeared in History via the real endpoint;
Replay correctly reloaded the code/source and switched tabs; Catalog showed the real
profiled table with working search, and Sync Metadata's effect was confirmed via the
Last Profiled timestamp genuinely advancing. Both themes screenshotted, no contrast
issues (the dark-mode pure-black page background is intentional — Noir is one of the
4 locked palette colors). `npm run build` (production, Turbopack) clean, 27 routes.

**Phase 15 (Plan/quota + Team, Track 2's largest phase) is complete and fully
live-verified.** Kicked off with a Resend health check (real send confirmed
working, `last_event: "delivered"` via Resend's own API — but the account is still
sandboxed to only the owner's own verified address; real invites to arbitrary
teammates will 403 until a domain is verified at resend.com/domains, a you-side
action) and a real-usage check against the proposed Starter tier before enforcement
shipped, which caught and fixed a real miscalibration before it went live (see
Gotchas and the "Plan/quota enforcement" Status Table row). Six sub-steps, each its
own commit: (1) `require_role()` shared dependency, retrofitted onto CI/CD approve/
reject — closes that Known-broken row; (2) retrofitted onto the rest of the approved
high-priority list (pipeline/source/quality-rule delete, run/sql, run/pandas, cicd
incident resolve); (3) team invites — real `TeamInvite` table, Resend delivery,
accept-into-existing-tenant with a locked role, live-verified with a real delivered
email and a real subsequent login; (4) team member management — list/role-change/
soft-removal, which surfaced and fixed a real gap (`User.is_active` was never
enforced at login, so "removed" members could still log in — now fixed for the
password path, live-verified); (5) plan/quota enforcement — 3 hardcoded tiers
(Starter/Growth/Scale), a documented credit formula, `enforce_quota()` wired onto
the 4 clearest cost/volume drivers, live-verified against *real pre-existing usage
data* (not synthetic seeds) for both the hard-block and soft-warn cases; (6)
`BillingService` interface with Stripe stubbed behind honest `501`s, `change_plan()`
real today as a documented dev-mode stand-in. Also folded in and closed the
long-standing "CI/CD high-risk commits double-book their approval" Known-broken row
(own commit, live-verified with a real non-monkeypatched risk calculation reaching
65/100 through the actual scoring logic). See each sub-step's own Status Table row
for full verification detail — every one was live-verified against the real running
server, not just the pytest suite. All three of the phase's original verification
gates (real invite→accept round trip; a restricted role actually blocked on CI/CD
approve/reject; quota soft-warn/hard-block triggering against real usage data) are
closed.

**Phase 16 (Settings persistence) is complete and fully live-verified.** Five
sub-steps, each its own commit: (1) the `get_agent()` cache re-keying fix + per-tenant
AI model override — closes Phase 0's approved pushback #2, live-verified with two
real, different LLM providers on two real tenants and zero cross-contamination
(the exact test that Gotcha demanded before this could ship); (2) workspace config
(name/timezone/description) via `GET/PATCH /api/v1/settings/`, reusing the
previously-unused `Tenant.settings` JSON column; (3) notification prefs, which
surfaced and fixed a real, separate, deeper bug — `NotificationService` had
referenced entirely nonexistent `AXIOM_*`-prefixed `Settings` fields since it was
written, meaning it could never have sent a single real Slack message or email to
anyone, tenant-specific or global, until this session; (4) theme server-persistence
(`User.theme`, per-user not per-tenant, deliberately not a JWT claim) via
`GET/PATCH /auth/me`; (5) platform API keys — full CRUD (create/list/revoke) with
the raw secret shown exactly once, deliberately scoped short of request
authentication (tracked as its own phase-sized item in Not-yet-built). Building (5)
also caught a second occurrence of Phase 15's `Read`-near-EOF tooling bug, this time
revealing the *original* Phase 15 fix had been incomplete — `TeamInvite.created_at`
had silently carried a real, undocumented index in production since Phase 15
shipped. See each sub-step's own Status Table row for full verification detail —
every one was live-verified against the real running server. Full backend suite:
213 passed.

**Phase 17 (Team, Billing, Settings UI) is complete — all three screens built and
fully live-verified.** The locked spec lives in `FRONTEND_BUILD_PLAN.md` under
"Phase 17 decisions (locked)". **Settings (`/settings`)** — 5 tabs (Workspace,
Notifications, AI Model, Theme, API Keys), all wired to real Phase 15/16
endpoints, no backend changes. Live-verified via two Playwright scripts (owner +
a re-logged-in downgraded viewer) covering every round trip including two
build-session amendments: the `notify_on` explicit-`false` deep-merge survives a
refresh (independently confirmed via raw `curl` first), and the raw API-key
secret is provably absent from both `localStorage`/`sessionStorage` and never
resurfaces in the DOM — see its own Status Table row and the Known-broken row for
the one accepted piece of tech debt (the AI Model tab's allowlist is hardcoded,
not fetched). **Team (`/team`)** — member roster + role change + soft-removal +
invite create/list/revoke, all mutation controls hidden (not just disabled) for
non-Owner/Admin. Building it surfaced a real gap in the original locked spec
(invite *creation* was speced, invite *acceptance* wasn't) — closed with a new
public `/invite/accept` page in the same session, not deferred. Live-verified
end-to-end including a genuine Resend delivery, a real accept round trip through
a clean unauthenticated browser context, 2-owner guard coverage (both the blocked
and the correctly-unblocked direction), and a live JWT-replay-after-removal check
whose result (access persists until expiry, `is_active` isn't re-checked
per-request) is now tracked as a real fix item — **Known-broken, not just a
Gotcha**, with severity and the actual token TTL (`JWT_EXPIRE_MINUTES=60`)
stated, flagged as a Phase 19 candidate. **Billing (`/billing`)** — plan card
with an Owner/Admin-gated upgrade/downgrade picker (`POST /change-plan`) and 4
real usage bars off `GET /billing/usage` colored by the backend's own `status`
field, Stripe-stubbed checkout/invoices honestly labeled "Coming soon." Building
it found and live-confirmed a real backend gap (also tracked in Known-broken, not
just documented): `change_plan()` allows any downgrade unconditionally, even one
that immediately puts current usage over the new plan's limits — no payment
gate either. The UI reflects this honestly (a non-blocking warning naming the
specific over-limit resource, not a fake block) rather than hiding it. All of
Phase 17's own verification gates are closed: invites (Resend delivery + full
accept round trip), roles (guard mirroring in both directions), plan/quota
display (real seeded usage, not all-zero), and every settings tab. No new
backend/migration work was needed anywhere in this phase — every endpoint it
wired to already existed and was live-verified (Phase 15/16).

**Phase 18 (AI Employees + Landing page) is complete — both screens built and
fully live-verified.** AI Employees: 6 tiles (AXIOM real/active + 5
honestly-locked employees), zero backend calls, three deliberate deviations from
the literal master design (no waitlist button, no marketplace button, no
per-tile pricing — deferred until real Stripe pricing exists). Landing page:
replaces the Phase 2 component-showcase scaffold that had silently occupied
root `/` since Phase 2; platform-first copy proposed and signed off in full
before any code was written; social-proof section omitted entirely (no
fabricated testimonials/logos/counts); roster copy shares a single source of
truth with `/ai-employees` via new `lib/employees.ts`/
`components/shared/EmployeeCard.tsx` so the two pages structurally cannot say
different things; auth-aware CTAs (nav + AXIOM tile) verified in both
directions with a real registered user. A real routing mistake (`/register`
instead of the actual `/signup` route) was caught by checking the real `app/`
directory before running any live test, not by a failed assertion. Verification
covered every item both build sessions' standards required: explicit
rendered-text audits (not spot-checks) on both pages, both themes × both
desktop/mobile widths on the landing page, a genuinely clean logged-out
context, every link fetched and confirmed to resolve, and the already-known
app-wide hydration-mismatch warning (root layout's theme pre-paint script,
found during the AI Employees build, confirmed — not re-discovered — on the
landing page too). One small isolated bug (AI Employees' "Coming Soon" scrim
being nearly invisible) was found and fixed in the same session it was found.
**Deployment was explicitly out of scope** — both screens are built and
verified locally only; real production deployment stays gated on the domain
purchase (see Outstanding items), and a new Not-yet-built entry now also flags
Privacy Policy/Terms of Service pages as required before any real production
signup traffic, grouped with that same domain-purchase cluster.

**Next action:** Phase 18 is done. Phase 19 (Polish) is the only phase left;
its full execution plan is locked in `FRONTEND_BUILD_PLAN.md`'s "Phase 19
plan (locked, 2026-07-22)" section. **P0 item 1 (the unified permission
spec) is done and fully live-verified** — see its Status Table row below.
Before continuing to item 2, the commit was checkpointed against the
user's own 3-point confirmation (demote-vs-deactivate distinction proven
live with a real never-reissued JWT; the cross-consumer test pasted and
read, not just reported green; a real SQL-echo capture confirming exactly
1 `users`-table query per request across a multi-capability-check
request) — all 3 held, no gap found. **P0 item 2 (file-upload UI for
CSV/Excel) is now also done and fully live-verified** — see its own
Status Table row below, including a real permission-spec gap it
surfaced and closed (`POST /uploads/register` had no `require_permission`
gate at all). **P0 item 3 is now fully done** — 3(a) (`resolve_identity()`'s
missing `is_active` filter) shipped and live-verified this session, see
its own Status Table row; 3(b) shipped earlier with item 1. **P0 item 4
(invite email link path fix) is now also done and fully live-verified**
— see its own Status Table row, including a real end-to-end proof (a real
Resend-delivered invite email's link opened in a real browser and
correctly resolved). **P0 item 5 (scheduled pipelines cluster) is now also
done and fully live-verified — this closes out P0 entirely.** See its own
Status Table row for the full detail; the short version: the arg-mismatch
bug turned out to be the smaller half of a bigger finding — the entire
dynamic per-pipeline scheduling design (`Scheduler.register()`/`sync_all()`)
was never actually reachable from the running app at all (nothing calls
it, and even if something did, it mutates a schedule dict that isn't
shared across the `backend`/`celery_beat` process boundary). Replaced with
a real, live, static-polling beat task (`check_scheduled_pipelines()`,
every 60s, same proven pattern as the existing `check_all_freshness()`)
that now genuinely fires scheduled pipelines — live-verified against a
real `* * * * *` schedule landing 3 real, correctly-timed runs one minute
apart, exactly the locked plan's own verification gate. Also added
save-time cron validation and real next-run/last-run visibility on the
Pipelines screen. **All 5 P0 items are now done.** See
`FRONTEND_BUILD_PLAN.md` for the full capability table, the P0/P1/P2
partition (now kept current as a **Phase 19 partition status** — see that
file's own top-of-section note), the verification plan (including a
cross-consumer regression test proving REST and chat share one map, not two
copies that happen to agree today). `dataops-agent/USER_MANUAL.md` itself is the
authoritative, code-derived reference for what every screen actually does
today — read it alongside this file for any Phase 19 work.

**Dark theme redesign is started but NOT complete** — the token change
itself landed (`tokens.css`, warm near-black neutral surfaces replacing
the old pure-black-page/Midnight-navy-card pairing, Ocean blue restricted
to accents only, measured 4.5:1+ contrast on every text pair) but that
commit's own message says its required next step — **a screenshot
verification pass across every screen, both themes** — was deferred, and
it still hasn't happened. Two real, unrelated bugs were found and fixed
while starting that verification (own commits, see Status Table): Chart.js
dashboard trend colors were silently never resolving CSS custom properties
at all in *either* theme (Canvas 2D silently no-ops on an unresolved
`var()`, not an error — pre-existing, not introduced by the redesign), and
a pre-existing `react/no-unescaped-entities` production-build failure on
the Team screen (unrelated to dark theme, just surfaced by running
`npm run build` during this work). **Next session's dark-theme work is
exactly that deferred screenshot pass** — not a design decision, a
verification gate that's still open. See `FRONTEND_BUILD_PLAN.md`'s "Dark
theme legibility" section for the token mapping and this file's Status
Table for the three dark-theme-adjacent commits.

**Separately, this session also did a Phase 19 polish pass** (unplanned,
opportunistic — not part of the original P0/P1/P2 partition): fixed the
landing page silently defaulting to dark mode for dark-OS visitors (light
is the signed-off public default); found and fixed a real race condition
in chat's "New Chat" button (not just a missing-feedback UX gap as
originally diagnosed — a stale in-flight response could leave the
composer locked or silently merge into the wrong session); swept the
app's interactive controls for other silent no-ops (none found beyond
New Chat — the remaining decorative elements are all self-disclosing or
already tracked in `FRONTEND_BUILD_PLAN.md`'s P1 list); rebuilt the
landing page in full (real logo, a real screenshot carousel from a
genuinely seeded demo tenant, real About/Contact sections); and
re-verified live, from scratch, the headline claims of the unified
permission spec (demote-vs-deactivate distinction, the cross-consumer
test, the one-query-per-request SQL echo). See each item's own Status
Table row.

---

## 1. Architecture Summary

**Stack:** FastAPI (async, Python 3.11/3.13) + SQLAlchemy 2.0 async + PostgreSQL 16 + Redis + Celery (worker + beat) + LangGraph/LangChain v1.x agent (`gemini-3.5-flash` primary, Llama 3.3 70B via Groq fallback) + Next.js 15/React 19 frontend (source present but incomplete — see §2).

**Services** (`dataops-agent/docker-compose.yml` and `backend/docker-compose.yml` — two near-duplicate compose files exist, see Gotchas):
- `backend` — FastAPI app (`backend/main.py`), port 8000, mounted as a live volume (`./backend:/app`) so container edits are picked up without rebuild.
- `postgres` — Postgres 16-alpine, port 5432, db `dataops`.
- `redis` — Redis 7-alpine, port 6379. DB 0 = general cache, DB 1 = Celery broker, DB 2 = Celery result backend (`config.py`).
- `celery_worker` / `celery-worker` — runs `celery -A services.celery_app worker --concurrency=4`.
- `celery_beat` / `celery-beat` — runs the periodic scheduler (static + dynamically-registered per-pipeline schedules).
- `frontend` — **commented out** in root `docker-compose.yml`. Not started by `docker compose up` even if the source existed and built.

**Core end-to-end flow (chat → agent → tool → approval):**
1. Client calls `POST /api/v1/chat/` (JWT-authed) — the only chat entry point as of the public-launch risk cluster session (2026-08-02): the unauthenticated `WS /ws/chat/{tenant_id}/{session_id}` route this line used to also describe was confirmed dead (zero frontend usage) and deleted outright rather than fixed — see §2/§3 and the now-resolved "WebSocket chat auth" Known-broken row.
2. `agent/dataops_agent.py` runs a LangGraph `StateGraph`: `inject_system` → `agent` (LLM bound to all tools via `.bind_tools`) → `approval_gate` → conditionally `tools` (a stock `ToolNode`) → loops back to `agent`, or `END`. Hard cap of 20 iterations, no config knob.
3. `personality.py` supplies the system prompt per `PersonalityMode` (engineer/founder/analyst/auditor) and gates tool calls per `OperationMode` (advisory/assisted/autonomous/audit) via `requires_approval()`, which checks a hardcoded `RISK_ACTIONS` risk table (low/medium/high).
4. If a tool call requires approval, `approval_gate_node` blocks it and injects an "Approval Required" message into the chat — but (see Gotchas) this in-graph gate is **not the same system** as the DB-backed approval queue read by `GET/POST /api/v1/approvals`.
5. The real, DB-backed approval workflow lives in `modules/governance/policy_engine.py` (`PolicyEngine`): a request is created, an admin/owner calls `POST /approvals/{id}/approve`, which calls `execute_approved_action()` → looks up the action in a hardcoded `TOOL_REGISTRY` dict → dynamically imports and invokes the real function via `importlib`.
6. Tool implementations live in `agent/tools/*.py`, one file per domain (ingestion, transformation, quality, orchestration, observability, governance, reporting, cicd), and call into `modules/*` for the actual work.

**Data model & tenancy:** Postgres, 17 SQLAlchemy models across `models/all_models.py` (13 models: Tenant, User, DataSource, Pipeline, PipelineRun, QualityRule, Incident, LineageNode, LineageEdge, AuditLog, ApprovalRequest, ChatMessage, DataContract, UsageMetric, KpiValue) and `models/cicd.py` (3 models: PipelineCommit, PipelineDeployment, CICDIncident). This is **multi-tenant by convention, not by enforcement**:
- Every model except `Tenant` carries a `tenant_id` column, but only `User`, `DataSource`, and `Pipeline` make it a real FK to `tenants.id` — the other 13 use a bare `String` with no FK constraint.
- Tenant scoping is entirely manual: `get_current_user()` decodes the JWT and returns `tenant_id` straight from client-supplied claims (no DB re-check — **this is still true for `tenant_id` specifically**; as of Phase 19's unified permission spec, `get_current_user()` *does* now re-read `is_active`/`role` fresh from the DB every request, see the new Status Table row, but `tenant_id` itself is not re-verified against anything); every endpoint/manager class is individually responsible for adding `.where(Model.tenant_id == tenant_id)`. There is no base query class, no session-level hook, no middleware, and **no Postgres RLS** (`create_enums.sql` only defines enum types).
- Spot-checked call paths (`dag_manager.py`, `policy_engine.py`, `run_tracker.py`, `rule_engine.py`, `connector_manager.py`, `schema_profiler.py`, `sql_runner.py`, all of `api/v1/`) do filter correctly today. The invariant holds only as long as every future query remembers to add the filter — nothing in the framework enforces it. Treat "did you scope this query by tenant_id?" as a mandatory review question for every new query in this codebase.

**Background jobs (Celery):** static beat schedule in `services/celery_app.py` — freshness check every 15 min (real), anomaly detection hourly (stub, logs only), daily reports every 24h (stub, logs only), CI/CD post-deploy health check every 60s (real), **scheduled-pipeline check every 60s (real, Phase 19 — see below)**. `modules/orchestration/scheduler.py`'s `Scheduler` class was originally *designed* to inject dynamic per-pipeline schedules into `celery_app.conf.beat_schedule` at runtime — **this was never actually the live mechanism and is not one now**: confirmed via a repo-wide grep that `Scheduler.register()`/`unregister()`/`sync_all()` are never called from anywhere in the running app (not at pipeline create/update, not at container startup), and even if they were, mutating `celery_app.conf.beat_schedule` from the FastAPI `backend` process has no effect on the actual, separate `celery_beat` process's own in-memory schedule (Celery's default `PersistentScheduler` isn't shared across OS processes) — a genuinely shared store like RedBeat would be needed to make that design work at all. `Scheduler` is kept in place, not deleted (its own logic is internally correct post an earlier enum-case fix, and has its own test coverage), but is dead code relative to what actually runs pipelines on schedule today. **The real, live mechanism (Phase 19)** is `services/tasks.py`'s `check_scheduled_pipelines()` — a static, always-on beat entry (matching the proven pattern of `check_all_freshness()`) that polls every `ACTIVE` pipeline with a `schedule_cron` directly against the DB each tick and fires any whose cron matches the current UTC minute via `croniter.match()` (the already-installed `croniter` dependency, genuinely used now — the old `_parse_cron()` hand-roll in `scheduler.py` is only reachable through the still-dead `Scheduler` path). Each firing creates its own fresh `PipelineRun` row (`triggered_by="scheduled"`) before delegating to the same execution core (`_execute_run()`) the manual/API trigger path uses. Save-time cron validation (`croniter.is_valid()`) is enforced in `api/v1/pipelines.py` on every write path (`create_pipeline`/`update_pipeline`/`set_schedule`), and `list_pipelines()` now reports a computed `next_run_at` (via `croniter`, `None` for anything not `ACTIVE`+scheduled) and the real `last_run` summary per pipeline. Pipeline "DAG" execution (`modules/orchestration/dag_manager.py`) is **not actually a DAG** — no step graph, no topological sort; a "run" is a fixed linear sequence (sync source → run quality checks → mark result).

---

## 2. Status Table

### ✅ Done and verified (real implementation, exercised or clearly wired end-to-end)
| Area | What | How verified |
|---|---|---|
| **Item 6 stage 1: `Task`/`TaskStep` schema + migration + `tasks.manage_all`** | New `Task`/`TaskStep` models (`models/all_models.py`) and migration `509a9b8715f2_add_tasks_and_task_steps.py` (down_revision `c8f3a7d52e19`, the real current head) — see `docs/PRODUCT_AUDIT.md` section 1.9 for the full design this schema implements. Schema only, zero behavior yet (no execution loop, no endpoints, no UI). Key invariants baked in per the design review: `Task` carries **no** role/is_active column anywhere (role is re-read fresh per step at execution time, never snapshotted — amendment 3); `Task` has real plan-provenance fields (`plan_approved_by`/`plan_approved_at`/`plan_edited`); `TaskStep.source` distinguishes `llm_planned`/`human_edited`/`system_inserted` so a Q2 verify step can never render as something AXIOM chose to do; `TaskStep.approval_request_id` reuses the existing, already-tested `ApprovalRequest` rather than a parallel mechanism; `task_shape` is a 3-value enum, rejecting anything outside the narrow v1 set (amendment 4). Both `Task.tenant_id`/`Task.user_id` are real FKs (matching the `TeamInvite`/`ApiKey` precedent, not the majority bare-String convention — a first-class tenant-owned entity, not a log table). New `PERMISSIONS["tasks.manage_all"]` (Owner/Admin) in `services/rbac.py`, exempted in `test_permission_matrix.py`'s matrix-completeness guard (no endpoint exists yet to gate — move it into `REPRESENTATIVE_ENDPOINTS` when stage 7 ships one). | **Migration verified against a genuinely empty scratch Postgres DB, not the dev DB**: `test_schema_drift.py` (Alembic's own `compare_metadata()` against a throwaway sibling database, never `dataops` itself) passed clean — the migration builds a schema byte-identical to what the models declare, from empty. Confirmed the real dev `dataops` DB was untouched by that test (`\dt` before/after). Separately, `docker compose restart backend` (triggering `create_all()`, this project's actual real schema source per the documented Alembic gotcha) confirmed both tables land cleanly with the right columns/enums/FKs (captured directly from the container's own SQL echo). New `test_task_models.py` (5 tests): sane defaults (`draft_plan`, no provenance set); a structural guard that `Task.__table__.columns` genuinely has no `role`/`is_active`/`user_role` column (guards the amendment-3 invariant against regression); real tenant scoping (two tenants' tasks don't cross-contaminate a scoped query); `TaskStep` provenance + FK integrity (an `llm_planned` step and a `system_inserted` verify step both correctly attached to the same real `Task` row); the 3-value `task_shape` enum. Full backend suite: 277 tests, 276 passed — the one failure (`test_notification_wiring.py`'s freshness test) is pre-existing cross-tenant test-isolation leakage from other tests' leftover data in the same run (unrelated to this change), confirmed by re-running that file in isolation: 4/4 passed clean. |
| Auth | Register/login/JWT (`api/v1/auth.py`) | bcrypt hashing w/ 72-byte truncation, HS256 JWT with `sub`/`tenant_id`/`email`/`role`/`exp`; `test_register_and_login` exercises register→login round trip |
| Core CRUD API | sources, pipelines, quality, incidents, governance, analytics, approvals, transformations, cicd | 40+ endpoints read directly in `api/v1/*.py`, all tenant-scoped via JWT except cicd webhook |
| Agent chat loop | LangGraph graph in `agent/dataops_agent.py` | read in full; graph wiring (inject_system→agent→approval_gate→tools) confirmed |
| Agent message-history bounding | `agent/dataops_agent.py` nodes now return only reducer deltas, not `state["messages"] + [new]` | Regression tests in `tests/test_agent_graph.py` prove a one-tool-call turn produces exactly 4 messages (was ballooning 3→15→63 before); verified live against a real tool-calling chat request |
| Tool-output capping | `agent/tools/_utils.py:cap_tool_result()`, applied to ingestion/transformation/quality/observability tools | large payloads (schema profiles, previews, transform results) truncated to 4000 chars before entering agent chat history |
| Tenant-id injection (chat/tool-calling path) | `agent/dataops_agent.py`: `run_agent()` forces tenant_id/user_id/session_id from the JWT-derived caller args onto context (never merges client-supplied values); `agent_node` force-overwrites the `tenant_id` arg on every outgoing tool call | 2 regression tests (real tenant_id used even if LLM guesses wrong; client-supplied context override ignored); verified live — a tenant with 3 real sources got "0 sources" from chat before the fix, correct 3 after, and a request trying to smuggle a fake tenant_id via `context` still returns the real tenant's data |
| `get_cicd_status` tool tenant scoping | Was the only tool with no `tenant_id` param — called back into the app's own `/api/v1/cicd/status/summary` over unauthenticated `httpx`, which always 401'd, but `httpx` doesn't raise on 4xx so it silently returned fake empty defaults instead of an error. Rewritten to `get_cicd_status(tenant_id: str, query: str = "")`, calling shared `services/cicd_service.py:get_status_summary(db, tenant_id)` in-process (same function the REST endpoint now uses too) — no HTTP, automatically covered by the existing tenant_id force-override in `agent_node` | New regression test; verified live — chat tool output now matches `GET /api/v1/cicd/status/summary` exactly for the same tenant (1 running/6 passed commits, 1 active deployment, 2 rollbacks, 1 pending approval, degraded health), and a fake-tenant_id-via-context attempt is ignored; grepped repo to confirm no other tool calls back into this app's own API |
| LLM fallback | `services/llm_service.py`: `_TimeoutFallbackChatModel` (replaced `primary.with_fallbacks()`), Gemini→Groq, confirmed routing fix (see Archive) is in place. **The fallback model must be tool-call-capable**: `llama-3.1-8b-instant` reliably fails real tool-calling requests with Groq's `tool_use_failed` 400 error (it can't format function calls correctly); `.env`'s `FALLBACK_LLM_MODEL` must stay at `llama-3.3-70b-versatile` (matching `config.py`'s own default) or an equally capable model. Primary model is now `gemini-3.5-flash` (see the LangChain v1.x upgrade row — switched from `gemini-3-flash-preview` after hitting its 20/day free-tier cap) — `PRIMARY_LLM_TIMEOUT_SECONDS` stays at 30s, real measured latency on `gemini-3.5-flash` is 1.1-2.8s (well inside the ceiling; not lowered further without more samples — see Gotchas) | **All previously-pending checks now closed, live-verified:** (1) healthy-primary-no-regression — multiple real Gemini successes confirmed (`provider: "gemini"` in real `/chat/` responses); (2) mid-conversation fallback + switch-back — a single real session showed `groq`→`gemini`→`groq` across consecutive turns, proving the mechanism switches back to a healthy primary rather than sticking on fallback once triggered; (3) Groq long-session headroom — a real 9-10 turn mixed session (plain + tool-calling) completed with 10 real LLM calls and zero crashes. **Gemini 3 tool-calling itself is now fully working as primary** — see the LangChain v1.x upgrade row below; the `iteration_count`/`recursion_limit` cap (next row) remains fixed and re-verified on the new stack. |
| **LangChain v1.x upgrade — Gemini 3 tool-calling now fully working as primary** | Upgraded `langchain` 0.3.25→1.3.14, `langchain-core`→1.4.9, `langgraph` 0.2.73→1.2.9, `langchain-google-genai` 2.1.4→3.2.0 (deliberately not the newer 4.x line — see Gotchas), `langchain-groq` 0.3.2→1.1.3, `langchain-community` 0.3.25→0.4.2 (`requirements.txt`). Verified compatibility *before* touching the running container: dry-run resolution, then an isolated `pip --target` install exercising the actual runtime patterns (`ChatGoogleGenerativeAI(thinking_budget=0)` construction, `AIMessage.additional_kwargs` mutation, a real `StateGraph`+`ToolNode` compile) — all passed. Normalized `agent/tools/cicd_tools.py`'s `from langchain.tools import tool` to `langchain_core.tools`, matching every other tool file (langchain 1.x split most of the top-level package into `langchain-classic`). Two real regressions surfaced only once actually driving real Gemini 3.5 tool calls (not caught by the isolated smoke tests) and fixed in `agent/dataops_agent.py`: (1) `AIMessage.content` can now be a list of structured content blocks (`[{"type": "text", "text": "...", "extras": {"signature": "..."}}]`) instead of a plain string — crashed **every** Gemini chat response, tool call or not, on insert into the VARCHAR `chat_messages.content` column; fixed with `_content_as_text()`, applied where `run_agent()` extracts the final response string. (2) dict/list-typed tool arguments (`register_data_source`'s `connection_config`, `create_data_contract`'s `schema_expectations`, `create_quality_rule`'s `rule_config`, etc. — 7 tools across 5 files) arrive from Gemini via `langchain-google-genai` as JSON-encoded strings rather than native objects; root-caused to the library's own function-call parsing layer (a raw Gemini API call bypassing LangChain entirely returns a real nested object for the identical request — confirmed twice, including with an explicit unambiguous prompt). Fixed with `_coerce_stringified_object_args()` in the same tenant_id-force-override loop in `agent_node`, using each tool's own declared schema (`tool.args`) as the source of truth for which params are object/array-typed — centralized, not per-tool. Also switched `PRIMARY_LLM_MODEL` from `gemini-3-flash-preview` to `gemini-3.5-flash` mid-session after hitting the preview model's 20/day free-tier cap (see Gotchas). | **Definitive live proof, not just a passing test**: a real 3-turn conversation against a fresh tenant — turn 1 (`list_data_sources`, 1 tool call), turn 2 ("add a source, then list again" — 3 tool calls in one graph invocation: 2× `register_data_source`, 1× `list_data_sources`), turn 3 ("how many sources now, use the tool not memory" — 1 tool call, correctly referencing turns 1-2 via DB-reloaded history) — **all 5 tool calls and all 3 turns ran on `provider: "gemini"`, zero fallback to Groq at any point.** The dict-arg fix was independently live-verified against the real `register_data_source` tool: a malformed first attempt (non-JSON string) was gracefully rejected by LangChain's own schema validation (no crash), the agent retried, and the second attempt (a real nested `{"path": "realfix.csv"}` object) succeeded and persisted a real `DataSource` row. Regression tests: `test_stringified_dict_arg_is_coerced_back_to_a_real_dict`, `test_malformed_json_string_arg_is_left_for_the_tool_to_reject` (both in `test_agent_graph.py`) plus the full existing suite (message-bounding, tenant_id force-override ×2, timeout/fallback ×4, recursion-cap) — 94 passed, zero regressions from the upgrade itself. |
| **22-site tool punch list — `governance_tools.py` (5/5 fixed)** | `get_lineage` was already correct (not broken). `get_audit_trail`: `AuditTrail.query()` → real `get_audit_trail(resource_type=, resource_id=, limit=)`, now wraps the returned list as `{"entries": [...], "count": N}`. `create_data_contract`/`validate_data_contract`: real logic never existed on `LineageTracker` at all — it lived inline inside `api/v1/governance.py`'s endpoint handlers. Extracted to a new `modules/governance/contract_service.py` (`create_contract()`, `validate_contract()`, `serialize_contract()`), matching the `cicd_service.py` shared-service pattern — the REST endpoints now call the same functions the tools do, no duplicated logic, no tool calling back into the REST API. `request_approval`: `PolicyEngine.request_approval()` → real `create_request()`; also was missing `user_id`/`session_id`, which `create_request()` requires — these are LLM-unknowable the same way `tenant_id` is, so `agent_node`'s existing tenant_id-force-override loop was extended to also force `user_id`/`session_id` on any tool call that declares them (currently just this one). | 4 new regression tests: `test_agent_forces_real_user_id_and_session_id_too` (`test_agent_graph.py`) plus 4 in new `test_contract_service.py` (REST behavior unchanged after extraction, 404 on unknown source, tenant scoping, real validation-violation path). **Live-verified all 5 tools through real chat tool calls** on a fresh tenant: `create_data_contract` created a real `DataContract` row; `get_audit_trail` returned that exact `contract.created` entry moments later; `validate_data_contract` ran the real violation check (correctly flagged `schema.no_snapshot` since the source hadn't been profiled); `request_approval` created a real `ApprovalRequest` row — cross-checked directly against the DB that `user_id` matched the real JWT `sub` claim exactly, not an LLM-supplied value. Full suite: 99 passed. |
| **22-site tool punch list — `observability_tools.py` (3/6 fixed this pass, 3 already correct)** | `check_freshness`: `ObservabilityMonitor` doesn't exist, real class `Monitor`; real `check_freshness()` also takes no `source_id` parameter at all (it always checks every source) — the tool now calls the real parameterless method and post-filters by `source_id` in Python if the caller supplied one, rather than pretending the underlying method supports it. `detect_anomalies`: `AnomalyDetector.detect()` → real `detect_anomalies(pipeline_id)`. `get_system_health`: `ObservabilityMonitor.system_health()` → real `Monitor(tenant_id).get_system_health()` (class **and** method both wrong). `list_open_incidents`/`triage_incident`/`resolve_incident` were already fixed correctly in an earlier session (see the "Observability" Status Table row) — not touched here. | Groq's daily token quota was exhausted mid-verification (hit `100000 TPD` limit from this session's cumulative testing) and Gemini's own request budget was being conserved, so `check_freshness` was live-verified through a real chat tool call (`provider: "gemini"`, correct zero-stale-sources answer) but `detect_anomalies`/`get_system_health` were verified by directly invoking the tool functions (`.ainvoke()`) against a real seeded tenant/pipeline/runs, bypassing the LLM layer — same pattern already established elsewhere in this project for exactly this quota-conservation reason, and equally valid since the fix here is about the module call shape, not the LLM integration. Both returned real, correct DB-derived results. Full suite: 99 passed (no new regression tests — all 3 sites are pure rename/signature fixes calling already-tested `Monitor`/`AnomalyDetector` methods). |
| **22-site tool punch list — `orchestration_tools.py` (3/6 fixed this pass, 3 already correct)** | `create_pipeline`: `DAGManager.create()` → real `create_pipeline(name=, source_id=, pipeline_config=)`; also renamed the tool's own `config` parameter to `pipeline_config` — `config` is a reserved name in LangChain's `Runnable`/`Tool` interface (see the new Gotcha) and was silently dropped before this fix, independent of the class/method rename. `pause_pipeline`: `.pause()` → real `.pause_pipeline()`. `set_pipeline_schedule`: `PipelineScheduler` doesn't exist at all (real class `Scheduler` has no `set_schedule` method either) — switched to the real owner of this operation, `DAGManager(tenant_id).set_schedule(pipeline_id, cron)`. `run_pipeline`, `get_pipeline_run_history`, `backfill_pipeline` were already correct — not touched. | Verified by direct tool invocation against real seeded data (same quota-conservation reasoning as `observability_tools.py` above): `create_pipeline` created a real `Pipeline` row (and, as a side effect, correctly triggered this session's earlier lineage auto-population — a real 3-node/2-edge graph appeared for it), `pause_pipeline` correctly set status to `PAUSED`, `set_pipeline_schedule` correctly persisted the cron string. Full suite: 99 passed. |
| **22-site tool punch list — `quality_tools.py` (5/5 fixed)** | `run_quality_checks`: was calling `QualityTestRunner.run_all()`, a documented no-op stub that always returns `passed: True` without inspecting data — redirected to the real check path, `QualityRuleEngine(tenant_id).run_checks(pipeline_id)`. `create_quality_rule`: `RuleEngine` → real `QualityRuleEngine`; the tool's own signature was also missing `name`, a required positional arg on the real `create_rule()` — the LLM was never even asked for it. `get_quality_report`: called `RuleEngine.get_report()`, which doesn't exist anywhere — redirected to `list_rules(pipeline_id)`, the closest real substitute (per-rule pass/fail counts), with the tool's docstring updated to describe what it actually returns now, not the original imagined "report" contract. `validate_business_rule`: **redesigned, not just renamed** — the original contract (`rule_name`, `dataset_id` → run one named rule against an arbitrary dataset) has no real equivalent; `BusinessRules` only supports running *all* rules for a *pipeline*. New signature `(tenant_id, pipeline_id)` → `BusinessRules(tenant_id).run_all(pipeline_id)`, docstring rewritten to match. `list_business_rules`: `BusinessRuleLibrary` → real `BusinessRules` (method name was already right). Also fixed the `list_rules()` dict/list bug this file's `validate_business_rule`/`list_business_rules` both depend on — see its own now-resolved Known-broken row (with a second, deeper, separate bug found and flagged, not fixed: `create_rule` can't actually create any of `BusinessRules`' own 8 supported rule types). | 3 new regression tests in `test_business_rules.py`. **Live-verified all 5 tools via direct invocation** against a real seeded pipeline: `create_quality_rule` created a real rule with the now-required `name`; `run_quality_checks` correctly evaluated it (`score: 100.0`, real pass/fail); `get_quality_report` returned the same real rule with pass/fail counts; `validate_business_rule`/`list_business_rules` both ran cleanly against real (empty) data — no crash, which is the whole point of the `list_rules()` fix. Full suite: 102 passed. |
| **22-site tool punch list — `reporting_tools.py` (5/5 fixed)** | `ReportGenerator` was already the correct class name — only method names and arg lists were wrong. `generate_status_report`: `.status_report(mode, period)` → real `.generate_status_report(mode)` — dropped `period`, which the real method doesn't accept at all. `generate_incident_report`: `.incident_report(incident_id, audience)` → real `.generate_incident_report(incident_id)` — dropped `audience`, same reason. `send_alert`: `NotificationService.send(channel, recipient, subject, message)` → real `send_alert(channel, message, severity, title)` — real method has no `recipient` param at all (delivery target comes from tenant-configured webhook/SMTP settings, not a per-call argument) and `subject` is really called `title`. `export_dataset`: `ReportGenerator.export()` has no real equivalent anywhere in the codebase — no per-tool redesign was possible (unlike `validate_business_rule`, there's nothing to redirect to), so it now returns a clear `{"error": "not implemented..."}` explicitly instructing the LLM not to claim success, rather than crashing or silently lying. `get_kpi_summary`: `.kpi_summary()` → real `.get_kpi_summary()`. | No new regression tests (all 5 are call-shape fixes against already-tested `ReportGenerator`/`NotificationService` methods). **Live-verified all 5 via direct invocation** against real tenant data: `generate_status_report` returned real counts (2 pipelines, 1 open incident, 1 stale source); `generate_incident_report` correctly referenced a real incident created for this test; `send_alert` degraded gracefully with a clean error when no Slack webhook is configured in this dev environment (matching the module's own "never raise" contract — proves the fix doesn't newly crash on an expected real-world config gap); `export_dataset` correctly refuses rather than lying about success; `get_kpi_summary` returned real (empty) data with no crash. Full suite: 102 passed. |
| **22-site tool punch list — `transformation_tools.py` (4/4 fixed) — LAST FILE, ALL 22 SITES NOW CLOSED** | `generate_sql_transform`: real `TransformGenerator.generate_sql()` takes a single `request: str` plus `source_id`/`pipeline_id`, not `source_tables: List[str]`/`transformation_goal`/`output_table_name` — redesigned the tool's own signature to `(tenant_id, transformation_goal, source_id=None, pipeline_id=None)` to match what the real method actually supports, folding the goal directly into `request`. `execute_sql_transform`: `SQLRunner(tenant_id, source_id)` → real `SqlRunner` (capitalization) takes only `tenant_id` in its constructor, with `source_id` passed per-call to `.dry_run(source_id, sql)` or `.run_on_source(source_id, sql)` depending on the `dry_run` flag — routes to whichever the tool's own `dry_run` arg selects. `run_python_transform`: `PythonRunner(tenant_id, pipeline_id).run(...)` → real `PythonRunner(tenant_id)` (constructor takes only `tenant_id`) with `.run_on_source(source_id, code)` — tool's own `pipeline_id`/`input_data` params dropped (real `PythonRunner` only loads from a `source_id` directly, no pipeline-based or raw-data-passing entry point exists) and replaced with `source_id`. `standardize_dataset`: `TransformGenerator.standardize()` has no real equivalent anywhere — same treatment as `export_dataset`, returns a clear not-implemented error pointing the agent at `generate_sql_transform`/`run_python_transform` as the real alternatives instead of crashing or lying. | No new regression tests (all 4 are call-shape fixes against already-tested `SqlRunner`/`PythonRunner`/`TransformGenerator` methods). **Live-verified all 4**: `execute_sql_transform` and `run_python_transform` correctly reached their real underlying methods and returned real *domain* errors (unsupported source type for a CSV source on `SqlRunner`; file-not-found for a fake test file path on `PythonRunner`) rather than the *implementation* errors (`AttributeError`/`TypeError` from a wrong class/method name) that would prove the fix didn't work — a domain-level error is exactly the correct-routing signal here. `generate_sql_transform` verified with a real LLM call (Gemini hit its daily quota mid-call — see the corrected Gotcha below — fell back to Groq cleanly and returned real, well-formed SQL). `standardize_dataset` correctly refuses. Full suite: 102 passed. **All 22 originally-catalogued tool call sites are now fixed and live-verified — the punch list from the earlier static sweep is closed.** |
| Agent recursion-limit miscalibration | `agent/dataops_agent.py`: `iteration_count` (agent_node's own graceful-stop check) and LangGraph's `recursion_limit` count different things — iteration_count increments once per agent_node visit, but each tool-calling round costs 3 LangGraph supersteps. The old hardcoded `> 20` check vs. LangGraph's default `recursion_limit=25` meant the hard limit was always reached first (~8 real iterations), so the "graceful" cap was dead code — any runaway tool-calling conversation crashed with an unhandled `GraphRecursionError` instead of degrading gracefully. Replaced with explicit, mutually-consistent constants: `MAX_AGENT_ITERATIONS=10`, `AGENT_RECURSION_LIMIT=50` (needed: 36, real margin, not just-barely-above) passed via `agent.ainvoke(..., config={"recursion_limit": AGENT_RECURSION_LIMIT})` | Found live: a real tool-calling conversation hung 2+ minutes then crashed, zero response persisted to `chat_messages`. Fixed and verified via a new regression test (`test_runaway_tool_calling_hits_graceful_cap_not_a_crash`) using an LLM stand-in that always returns another tool call (never terminates) — confirms the real compiled LangGraph object hits the graceful message with no exception, and asserts the actual supersteps-vs-recursion_limit numbers stay comfortably apart so a future change to either constant without re-deriving the other fails loudly. Full suite: 68 passed. |
| Usage metering foundation (Phase 1) | New `LlmUsageEvent` table (`models/all_models.py`), migration `fe6e4b941fce_add_llm_usage_events.py`, `log_llm_usage()` helper in `services/llm_service.py`, instrumented at both LLM call sites (`invoke_llm()` for direct completions, `_TimeoutFallbackChatModel.ainvoke()` for the agent graph — the latter has no tenant context itself since it's cached/shared across tenants, so it stashes a usage dict on the response's `additional_kwargs` and `agent_node` in `dataops_agent.py` pops+logs it, since that's where tenant context actually lives). `provider` field added to `POST /chat/`'s response, sourced from the last AI message that actually made an LLM call (not a synthetic approval-gate message). `incident_manager.py` and `transform_generator.py` now pass `tenant_id`/`request_type` through so their `invoke_llm()` calls attribute correctly (`incident_triage`, `transform_generation`) instead of just the agent-chat path (`agent_chat`). A `reasoning_tokens` column (migration `01b084fa094c`) tracks Gemini 3's "thinking" tokens separately from `total_tokens` (which already includes them) — likely priced differently for credits later. | **Verified live for the `agent_chat` path**: real chat requests produced real `llm_usage_events` rows for both providers, response body's `provider` field matched each time — see the LLM fallback row above for the specific numbers now that Gemini's quota dead-end is fixed. **`incident_triage`/`transform_generation` paths**: share the identical `invoke_llm()`→`log_llm_usage()` code proven live above, plus a passing regression test that writes a real DB row (`test_invoke_llm_writes_a_real_usage_event`), but still not independently live-tested with a real LLM call — no code reason to doubt them, just not yet exercised end-to-end; re-attempt when next touching either path. `reasoning_tokens` extraction verified two ways: live (a real Gemini call with `GEMINI_THINKING_BUDGET=0` correctly wrote `NULL`, confirming no reasoning tokens to report when thinking is off) and via a new regression test (`test_invoke_llm_captures_reasoning_tokens_separately`) proving the extraction pulls the right value when `output_token_details.reasoning` is present, using the exact shape confirmed live against the real API before thinking was capped. 6 regression tests total in `test_llm_usage_metering.py`. |
| Frontend scaffold + design system (Phase 2) | Next.js 15 App Router under `dataops-agent/frontend/` (real project, not just reference docs), `src/styles/tokens.css` (locked Midnight/Pearl Perfect/Noir/Ocean palette, light+dark), `src/styles/fonts.css` + `public/fonts/*.woff2` (real self-hosted General Sans/Fraunces/JetBrains Mono files, latin subset, no CDN calls at runtime), `src/styles/components.css` + `src/components/ui/*` (10-component core library: Button, Card, Badge, Input/Select, Table, Modal, Toast, Tabs, Progress, Skeleton) | `npm run build` (production, Turbopack) and `npm run dev` both verified clean, zero TypeScript/ESLint errors. Visually verified — not just code-reviewed — via Playwright screenshots of the component showcase page (`src/app/page.tsx`) in both themes across all 10 components; this caught and fixed 3 real dark-mode bugs a code read would have missed (see Gotchas: `--surface`-as-text-color, `--ocean-50` light-only tint, `.input-hint`/`.text-danger` specificity collision). No Zustand/routing/state/API-client yet — out of Phase 2's actual scope, not an oversight. |
| Auth design (Phase 3) | Full three-method design locked in `FRONTEND_BUILD_PLAN.md` — password, Google OAuth, email-code (passwordless, via Resend) — covering JWT claims shape, required `User` schema changes, `(email, tenant)`-scoped identity model, tenant-resolution rules for all 3 methods across new-signup/existing-tenant-login/invite-join, login disambiguation, the non-blocking signup nudge, and full rate-limiting design for the email-code method | **Design approved and complete, zero code written yet** — this is a documentation-only milestone (Workflow Rule 4: propose and sign off before code on anything touching auth). Grounded in a full read of `api/v1/auth.py`, which surfaced 2 real pre-existing bugs now in Known-broken below. Ready for implementation whenever its phase arrives in the sequence — see `FRONTEND_BUILD_PLAN.md` for the complete design before writing any of it. |
| Onboarding profile storage (Phase 4) | New `OnboardingProfile` table (`models/all_models.py`, migration `3186398dc663_add_onboarding_profiles.py`), `POST/GET /api/v1/onboarding/` (`api/v1/onboarding.py`) — `tenant_id` (unique, real FK) + `user_id` (real FK) + role/industry/company_size (free-text strings, matching this codebase's existing `QualityRule.severity`-style precedent over enums) + use_cases/data_stack (JSON lists, matching `DataSource.tags`'s style) + `completed_at` as the done/not-done signal. POST upserts (one row per tenant, resubmission updates rather than duplicates) | Verified live: registered a fresh tenant, confirmed `GET /onboarding/` returns `{"completed": false}` pre-submission, `POST` persists real data, `GET` reflects it, a resubmission updates the same row (confirmed via direct DB query — still 1 row), a second tenant's `GET` returns `{"completed": false}` (tenant isolation), and `register()`/`login()` both work identically to before (explicit regression test, plus this table's own existence required zero changes to either endpoint — by construction, per the separate-table design). 5 regression tests in `test_onboarding.py`, full suite (43 tests) passes. |
| Non-password auth, part 1: login-fix + email-code (Phase 5) | New `services/auth_service.py` holds the shared logic all 3 auth methods use: `resolve_password_login()` (replaces the old unscoped, arbitrary-match `login()` query — now checks the password against every `(email)`-matching row across tenants, returns a `choose_workspace` disambiguation response with no token if more than one matches), `resolve_identity()` (shared post-proof resolver for email-code and, next, Google — auto-links per the Phase 3 rules), `create_new_tenant_and_user()` (shared fresh-signup path), and the full email-code stack (`EmailLoginCode` generation/hashing/Resend delivery/Redis-backed rate limiting exactly per the locked design). `register()` now runs the non-blocking existing-workspace nudge. `User.hashed_password` nullable + `google_id`/`email_verified` columns added via a real Alembic-applied migration (see Gotchas: this dev DB's Alembic tracking was stamped to reality first, since `ALTER TABLE` on an existing table needed real migration application, unlike every prior `create_all()`-only change this project). | **Verified live with a real, fully-automated Resend round-trip** — not mocked: requested a real code, retrieved the actual delivered text via Resend's own `GET /emails/{id}` API (confirms genuine delivery, not just that the send call didn't error), extracted the real 6-digit code, submitted it to the real public verify endpoint, got a real JWT back with `auth_method: "email_code"` and `email_verified: true` persisted. Also live-verified: returning-user login resolves to the same tenant; replay of a consumed code fails; 5 wrong attempts invalidate the code; the 60s resend cooldown blocks an immediate second request. One real bug found and fixed *during* live verification: an unconfigured/failing Resend call crashed with a raw 500 instead of failing gracefully — now caught and logged, response stays generically "sent" either way (enumeration safety). 20 regression tests across `test_password_login_disambiguation.py` and `test_email_code_auth.py` (Resend mocked here, since live delivery is already proven above and sandbox mode restricts real sends to the account owner's own address); full suite (58 tests) passes. |
| Non-password auth, part 2: Google OAuth (Phase 5) | `GET /auth/google/login-url` (builds the real Google authorization URL, `openid email profile` scope, CSRF `state` stored single-use in Redis with a 10 min TTL — same pattern as email-code's rate-limit keys) and `POST /auth/google/callback` (validates+consumes state, exchanges the real authorization code with Google's token endpoint, verifies the ID token's signature/audience/issuer via `google.oauth2.id_token.verify_oauth2_token` off the event loop via `asyncio.to_thread`, then reuses the exact same `resolve_identity()` resolver email-code uses — new-workspace/existing-tenant/general-login all behave identically to email-code's, plus the `blocked_unverified` case that's Google-specific per the locked auto-link rule) | **Verified as far as possible without a human browser session** (Google's consent screen requires real user interaction — cannot be automated by the agent). Confirmed live against the *real* Google endpoints: `login-url` produces a well-formed, correctly-encoded authorization URL with the registered redirect URI and a real Redis-backed state; an invalid state is rejected; a valid state with a fake code is rejected *by Google's real token endpoint* (proves `exchange_google_code()` genuinely calls Google, not just that error-handling looks right in the abstract) with a clean 400, not a crash; state is correctly single-use (replay after a failed exchange still fails, since state is consumed before the exchange attempt). The full success path (new-tenant creation, auto-link when Google's `email_verified: true`, the `blocked_unverified` rejection when `false`, returning-user login skipping re-verification, multi-tenant disambiguation) is covered by 8 regression tests with `exchange_google_code`/`verify_google_id_token` mocked at the router's import site — exercises the real `resolve_identity()`/`create_new_tenant_and_user()` logic end-to-end, only the two calls to Google itself are stubbed. **Full live round-trip completed** — the user clicked through Google's real consent screen, the resulting authorization code was exchanged and verified for real, and the flow correctly auto-linked the Google identity onto an already-existing account (created earlier the same session via email-code, not password — confirming auto-link works from either originating method, not just the password case the mocked tests covered) rather than creating a redundant new tenant, exactly per the `resolve_identity()` design: real JWT issued with `auth_method: "google"`, DB confirmed `google_id` populated and `email_verified: true` on the correct existing row. Phase 5 is fully live-verified end-to-end for all 3 methods. Full suite (66 tests) passes. |
| Choose_workspace resolution-token handoff (`POST /auth/resolve-workspace`) | Discovered while building Phase 6's workspace-picker UI: the originally-designed continuation for `choose_workspace` (resubmit the same proof with a `tenant_id`, the pattern that already works for password login) doesn't work for email-code or Google, because both proofs are single-use and already consumed by the time `resolve_identity()` returns `"choose"` — `verify_login_code()` marks the code consumed on its first successful match, and Google's authorization code/state are single-use at Google's own end too. Added `store_pending_identity()`/`resolve_pending_identity()` (`services/auth_service.py`, Redis-backed, 5 min TTL, single-use like oauth state) — on `choose`, the router stashes the already-proven identity and returns a `resolution_token` alongside `options`; the new endpoint finalizes the tenant pick against that token instead of the original proof | Verified live via pytest: added a replay assertion to both existing `choose_workspace` tests (proving the old code/state really is dead by that point, which neither test had actually exercised before — they only asserted `choose_workspace` was returned, never exercised finalizing the pick), then a `resolve-workspace` round trip + a second single-use replay check on the resolution token itself. Full suite: 66 passed. Later also exercised live through the real Phase 6 UI (workspace picker → pick → completes login) via Playwright. |
| Google OAuth clock-skew tolerance | Found live during Phase 6's UI click-through: a real, valid Google consent flow failed with a generic "Google sign-in failed" — the router's `except Exception` swallowed the real error with zero logging, added `structlog` error logging first to see it (`google_auth_failed` with the real exception). Real cause: `google.oauth2.id_token.verify_oauth2_token()` defaults to `clock_skew_in_seconds=0`, and the backend container's clock was a second or so behind Google's at that moment (common in Docker/WSL2 dev environments after host sleep; not guaranteed to be exactly zero in production either without perfect NTP sync) — failed with "Token used too early". Fixed by passing `clock_skew_in_seconds=10` (`_verify_google_id_token_sync()`, `services/auth_service.py`) — standard JWT practice, doesn't weaken signature/audience/issuer checks | Verified live: the same real consent-screen round trip that failed before the fix succeeded immediately after (confirmed by the user). Regression test mocks `google.oauth2.id_token.verify_oauth2_token` itself (not the wrapper) to prove the real function now passes a nonzero `clock_skew_in_seconds` through, rather than just asserting behavior that happens to pass when the clock is already synced. Full suite: 67 passed. |
| Auth + Onboarding UI (Phase 6) | Real Next.js pages for all 3 auth methods (`/login`, `/signup` — password fields + inline email-code swap + Google button), the login disambiguation workspace picker (`src/components/auth/WorkspacePicker.tsx`), the non-blocking signup nudge (a real confirm step requiring a click to proceed, not a banner that flashes and immediately redirects away — the initial cut did exactly that until live verification caught it), the Google OAuth callback page (`/api/auth/callback/google` — a plain client page, not a Route Handler; Next.js allows this as long as no `route.ts` shares the segment), the 4-step onboarding wizard (`/onboarding`, wired to Phase 4's real endpoint), and a minimal authenticated `/dashboard` stub (not the real dashboard — Phase 9 — just a real post-auth landing point so the auth loop has somewhere to close). `src/lib/api.ts` is the typed client for every endpoint used | Verified live end-to-end via a Playwright script driving a real Chromium instance against the real running stack (not mocked): password signup→onboarding→dashboard→logout→login (confirms persisted onboarding answers, confirms returning login skips onboarding); two real API-created tenants sharing an email→UI login→workspace picker renders both tenants→picking one completes login; email-code login with a real Resend send-and-retrieve round trip (same automation technique as Phase 5's backend verification, now driving the UI); the non-blocking nudge banner on a real duplicate-email signup, including its own "Continue" click. Google OAuth: structurally verified by script (button reaches real `accounts.google.com` with correct `client_id`/`redirect_uri`) plus a full manual click-through by the user through Google's real consent screen (surfaced and got the clock-skew fix above, then succeeded). Screenshots captured in both light and dark themes for every screen (signup, login, onboarding step 1, workspace picker, email-code entry, dashboard, signup nudge) — no dark-mode contrast bugs found this time (Phase 2's `--on-dark`/compound-selector fixes already cover the shared component library these pages reuse). `npm run build` clean, full backend suite 67 passed. |
| App shell (Phase 7) | `src/app/(app)/layout.tsx` — a Next.js route group wrapping every authenticated page in Sidebar + Topbar + AxiomFab + CommandPalette, with its own client-side auth guard (redirects to `/login` if `getToken()` is empty), scoped only to this group so `/login`/`/signup`/`/onboarding` stay chrome-free. `src/components/shell/`: `Sidebar.tsx` (18-item nav driven by `navItems.tsx`, collapse toggle, workspace-selector stub), `Topbar.tsx` (route-derived breadcrumb, search bar opening the command palette, `ThemeToggle.tsx`, notification bell + `NotificationsPanel.tsx`, avatar), `CommandPalette.tsx` (global `Ctrl+K`, filterable, navigates via `next/navigation`), `AxiomFab.tsx` (hides itself on `/chat`). `decodeUserFromToken()` (`lib/api.ts`) client-side-decodes the JWT payload (display only, no signature check needed) for the sidebar footer/topbar avatar — no new backend endpoint needed. 17 new stub routes (`ai-employees`, `chat`, `sources`, `catalog`, `pipelines`, `transforms`, `quality`, `incidents`, `governance`, `automations`, `cicd`, `approvals`, `analytics`, `audit`, `team`, `billing`, `settings`) render a shared `StubPage` placeholder naming which later phase wires each one up; `/dashboard` (Phase 6's stub) moved into the group. Root `layout.tsx` gained a pre-paint inline script that reads the same `localStorage` key `ThemeToggle` writes, avoiding a flash of the wrong theme — applies globally, so the pre-auth pages also honor a previously chosen theme even though the toggle button itself only lives in the authenticated shell. Deliberately omitted the prototype's numeric sidebar badges (13 pipelines, 2 incidents, 3 approvals) and its credit-balance number, since nothing wires real values to them yet and they'd read as live data; kept the AXIOM "Live" status badge and the AI-status/env-select/credits-badge chrome since those are cosmetic, not specific fabricated metrics | Verified live via a Playwright script driving a real Chromium instance against the real running stack: all 18 nav routes render inside the shell with the correct active sidebar item; sidebar collapse toggles width; `Ctrl+K` opens the palette, filters by query, navigates on selection, closes on Escape and after selecting; notifications bell opens/closes (including click-outside-to-close); the AXIOM FAB navigates to `/chat` and hides itself there; theme toggle flips `document.documentElement.dataset.theme`, persists to `localStorage`, and survives a real page reload; visiting any shell route with no token redirects to `/login`. Screenshots captured in both themes for the full shell, a stub page, the collapsed sidebar, the command palette, and the notifications panel — no dark-mode contrast issues found. Caught and fixed one before verification even ran: two new spots (`.credits-badge`, `.notif-item.unread`) initially used `--midnight-50`/`--midnight-100` for backgrounds behind theme-flipping text, repeating Phase 2's documented light-only-tint bug — switched to the dark-mode-safe `--ocean-50`/`--ocean-100` pair with text pinned to `--text-primary`. `npm run build` clean (27 routes). Pure frontend — no backend changes this phase. |
| Chat sessions + tool trace (Phase 8) | `GET /api/v1/chat/sessions` (new) — private-per-user AND tenant-scoped list grouped from `chat_messages` (no migration: `user_id` was already a required column on every row), returning `session_id`/`title` (first user message, truncated)/`started_at`/`last_activity`/`message_count`. Fixed a related privacy gap found while touching this code: `GET /chat/sessions/{id}/history` only checked `tenant_id`, not `user_id` — any tenant member could fetch another member's session by guessing its UUID; now scoped by both. `ChatMessage.tool_calls` (a JSON column that already existed but was never populated) is now filled in `POST /chat/` from the real LangGraph message list `run_agent()` returns for that turn — every `AIMessage.tool_calls` paired with its `ToolMessage` result by `tool_call_id`, plus any tool calls the approval gate blocked (`status: "blocked_pending_approval"`, no result, so the user sees AXIOM *wanted* to do something rather than silently stopping). Tool outputs are already capped by `cap_tool_result()` before they reach this list — no new truncation logic needed. Included in both the `POST /chat/` response and the history endpoint | Verified via 5 new regression tests hitting the real HTTP endpoints (real auth, real DB persistence, real `ChatMessage` rows) with a deterministic fake-LLM stand-in for the model response, the same pattern as `test_agent_graph.py`: tool trace shape/content correct, blocked calls appear correctly, session list is genuinely scoped per-user (two different users' sessions never leak into each other's list), title/timestamp/count accuracy, history includes each message's own trace. Full suite: 74 passed. **Live verification with a real LLM completed in a later session** once Groq's quota freed up: a real chat message ("List my data sources.") produced a real Groq tool call (`list_data_sources`, real tenant_id, real result referencing an actually-seeded source), correctly persisted and returned in the `tool_calls` trace via both `POST /chat/` and `GET /chat/sessions/{id}/history`, and the session correctly appeared in `GET /chat/sessions` with the right title/timestamps/message_count. Phase 8 is fully live-verified. |
| Dashboard (Phase 9) | Real dashboard replacing the Phase 6/7 stub — 7 KPI cards (System Health, Pipelines Active, Quality Score, 7-Day Runs, Data Sources, Open Incidents, Pending Approvals) from `GET /analytics` + `GET /approvals`; a health banner that only renders for a real open incident (`GET /incidents/`), no always-on demo banner; two Chart.js trend charts both driven by `GET /analytics/quality`; Recent Pipeline Runs from the new `GET /analytics/recent-runs` (`api/v1/analytics.py` — tenant-scoped join of `PipelineRun`+`Pipeline`, no prior tenant-wide "all runs" endpoint existed); a Pending Approvals card wired to real `POST /approvals/{id}/approve`/`reject` via React Query mutations, not stubs; an AXIOM Activity card reading Phase 8's real `GET /chat/sessions`. Deliberately dropped the prototype's "AI Credit Balance" KPI card (no real billing/credits system exists). `@tanstack/react-query` (wraps the `(app)` group via a new `QueryProvider`) and `chart.js`/`react-chartjs-2` installed this phase, matching their planned trigger point | Verified live end-to-end via a Playwright script against the real running stack with a real seeded tenant (real source, pipeline, 5 runs of varying status, 1 open incident, 1 pending approval): all 7 KPI values, the health banner, both charts, the recent-runs list, and the approvals card render real data; clicking Approve actually calls the real endpoint (approval disappears, KPI count updates); Refresh re-fetches without crashing. Screenshots in both themes. **Two real bugs caught by live screenshot verification, not code review, and fixed in the same commit**: `.stats-grid`/`.metric-card`/`.metric-value`/`.metric-change` were referenced from the master design prototype but never actually ported into this project's `components.css` back in Phase 2 — KPI cards rendered as unstyled stacked rows instead of a grid; and 2 of the 7 KPI cards used `--midnight-500`/`--midnight-600` as value text color, the same dark-on-dark contrast bug documented earlier this project (those tokens don't flip for dark mode) — switched to `--text-primary`. Also fixed a real regression this phase would otherwise have shipped: removing the dashboard stub would have removed the *only* logout path in the app (Phase 7's topbar avatar only ever showed an identity toast, no action) — added a proper account dropdown to `Topbar.tsx` (email/role + Log out, click-outside-to-close). `GET /api/v1/analytics/recent-runs` also has 3 backend regression tests (ordering/shape, limit, tenant isolation). Full backend suite: 77 passed. `npm run build` clean. |
| Ingestion connectors | postgres, mysql, csv, file, api, gsheets (`modules/ingestion/connectors/`) | real drivers (asyncpg, pymysql/aiomysql, pandas/pdfplumber/python-docx, httpx, gspread+google-auth real OAuth) |
| Quality rule engine | `modules/quality/rule_engine.py` (`QualityRuleEngine`) | real evaluators for not_null/unique/accepted_values/range/regex/row_count/custom_sql against live data |
| Business rules (`_check_pipeline_success_rate` only) | `modules/quality/business_rules.py` | Fixed 2 bugs: module-level `from modules.quality.rule_engine import RuleEngine` (no such class — real name `QualityRuleEngine`, made the whole module unimportable) and `RunStatus.success`/`.failed` lowercase-attribute access (real: `.SUCCESS`/`.FAILED`). Regression test in `test_observability_enum_regressions.py`; verified live against a real pipeline/run. **The other 7 rule types (`reconciliation`, `kpi_sanity`, `cross_source_count`, `freshness_sla`, `column_sum_match`, `kpi_trend`, `data_volume_growth`) plus `create_rule`/`list_rules`/`delete_rule`/`run_all` are untouched by this fix and still have at least one further bug — see Known-broken.** |
| Observability | `modules/observability/monitor.py` (`check_freshness`, `get_system_health`, `get_pipeline_stats`), `anomaly_detector.py` (`detect_anomalies`, `detect_all_anomalies`), `incident_manager.py` (`list_incidents`, `triage_incident`, `resolve_incident`) | **Previously marked done off a code read, not a live test — it wasn't. All of it was broken.** `incident_manager.py` imported a nonexistent `LLMService` (ImportError on construction) and compared against `IncidentStatus.open/investigating/resolved` (lowercase — real members are uppercase); `agent/tools/observability_tools.py` called `IncidentManager.list_open()/.triage()/.resolve()`, none of which exist (real: `list_incidents`/`triage_incident`/`resolve_incident`); `monitor.py` and `anomaly_detector.py` both compared against `RunStatus.success/.failed` and `PipelineStatus.active` (also lowercase, also wrong). Every one of these was silently swallowed by each method's own `try/except Exception → {"error": ...}`, so nothing crashed — it just always returned empty/wrong data. Fixed and verified: `list_open_incidents`/`triage_incident`/`resolve_incident` live end-to-end through the real chat agent (real LLM triage via Groq after Gemini's known-zero quota fell back, confirmed via DB read after); `check_freshness`/`get_system_health`/`get_pipeline_stats`/`detect_anomalies`/`detect_all_anomalies` invoked directly against a real tenant/pipeline/run/incident in the running container (bypassing the LLM layer, which is orthogonal to these specific fixes and already proven above). Regression tests in `test_incident_manager.py` and `test_observability_enum_regressions.py`. |
| Governance | `audit_trail.py`, `lineage_tracker.py` (real BFS graph traversal), `policy_engine.py` approve/reject/execute flow | read in full, DB-backed, dynamic dispatch via hardcoded (non-attacker-controlled) `TOOL_REGISTRY` |
| Lineage auto-population + backfill (Phase 11's lineage half, pulled forward into Phase 12) | `LineageTracker.sync_tenant_lineage()` (new) — idempotently ensures every `DataSource`/`Pipeline` a tenant has is represented in the lineage graph, reusing the pre-existing-but-previously-uncalled `auto_register_pipeline_lineage()` (source→pipeline→output, `feeds`/`produces` edges) for pipelines with a `source_id`. Hooked into `ConnectorManager.register_source()` and `DAGManager.create_pipeline()` (both wrapped in try/except so a lineage-registration failure never blocks the actual source/pipeline creation) for the going-forward path; also called at the top of `GET /governance/graph` and `GET /governance/lineage/{asset_name}` so **pre-existing tenants self-heal on read** rather than needing a one-off backfill migration — `add_node`/`add_edge` were already idempotent (checked by tenant+name+type before inserting), so repeated sync calls on an already-in-sync tenant are just cheap lookups | 5 regression tests in `test_lineage_sync.py` (creation hooks, tenant isolation, idempotency, and a dedicated backfill test that inserts a source+pipeline directly via the DB — bypassing `register_source`/`create_pipeline` entirely — to simulate pre-existing data). **Live-verified twice**: a fresh tenant's real source+pipeline creation produced a real 3-node/2-edge graph immediately; and — the real backfill case — an actual pre-existing tenant from earlier this project ("Wunomo QA", 3 sources/2 pipelines created weeks before this feature existed, with only 2 unrelated manually-created nodes in its graph) went from that stale 2-node graph to a real 9-node/5-edge graph on a single `GET /governance/graph`, with the original manual nodes preserved untouched. **Scope note: this pulls forward only Phase 11's lineage half — the other half (writing a `KpiValue` point on every quality-check run) is still pending, unrelated to Governance/Phase 12's screens, and not attempted this session.** |
| Approvals-merge aggregation endpoint (`GET /api/v1/approvals/merged`) | Combines the two real, separate pending-approval queues (`PolicyEngine.list_pending()` and `PipelineCommit.gate_decision == "pending_approval"`) into one tenant-scoped, newest-first list, per Phase 0's locked design (a backend aggregation endpoint, not a frontend-side merge). Read-only — each item is tagged `source: "policy_engine" | "cicd_deployment"` so the client routes approve/reject to the correct existing, already-role-gated endpoint rather than this endpoint inventing a second mutation path. Excludes the orphaned `cicd_pipeline_deployment` policy_engine mirror (see the new Known-broken row) so the list only ever shows genuinely actionable items. | 5 regression tests in `test_approvals_merged.py` (both sources present, non-pending commits excluded, newest-first ordering, tenant isolation, the orphan-mirror exclusion). Live-verified against a real tenant with real CI/CD approval history — before the exclusion fix, the same commit appeared twice under different `source`s (`count: 3` for what was really 1 actionable + 1 stuck-forever duplicate + 1 unrelated stuck duplicate); after, `count: 1`, exactly the one real pending deployment. |
| Centralized 401 handling (`frontend/src/lib/api.ts`) | A 401 on any authed request now calls `clearSession()` and hard-redirects to `/login?expired=1`, instead of the caller's React Query hook quietly rendering the failed fetch's `undefined`/fallback data as if it were real zeros. Implemented once in the shared `request()` function every API call funnels through (`authedRequest` and the handful of authed calls that build headers inline both go through it) — keyed off "did this request carry an `Authorization` header," which is what distinguishes an expired session from a legitimate 401 on login/register/email-code/Google (none of which send a bearer token, so none of them trigger this path). `/login` reads the `?expired=1` marker on mount, shows a toast ("Session expired — please log in again."), and strips the param via `router.replace`. | No frontend unit-test framework exists in this project (confirmed via `package.json` — every prior frontend phase has relied on live Playwright verification instead, not committed tests), so verification here follows the same established pattern. **Live-verified**: minted a genuinely-expired JWT (real `exp` in the past, signed with the real `JWT_SECRET`, confirmed independently via a direct `curl`/`fetch` returning real `401 Invalid token`), injected it into `localStorage` via Playwright, navigated to `/dashboard`, and confirmed all four: redirected to `/login`, the session-expired toast rendered, `localStorage`'s token was cleared, and the `?expired=1` marker was stripped from the URL afterward — not just "no crash," the actual old silent-all-zeros failure mode is gone. |
| Core DataOps screens (Phase 12) | Sources, Pipelines, Quality, Incidents, CI/CD, Approvals (merged), Governance — all 7 built in `frontend/src/app/(app)/*`, replacing their Phase 7 stubs. Each wired to real backend endpoints (see `frontend/src/lib/api.ts`'s Phase 12 section) with real create/update/delete/action flows via React Query mutations, not just read views: Sources (create/sync/profile/delete), Pipelines (create/trigger/pause/activate/delete + a run-history modal), Quality (create rule/run checks/delete), Incidents (log/resolve), CI/CD (status summary + Commits/Deployments tabs + approve/reject), Approvals (the merged list, approve/reject routed to the correct real endpoint per item's `source`), Governance (Lineage/Contracts/Audit Log tabs — Lineage renders the real auto-populated graph, Contracts create+validate against real sources, Audit Log reads real `AuditTrail` entries) | Live-verified per screen via Playwright against a real running backend (dev server, not mocked) — empty states, real data after creation, real action side-effects (status transitions, deletions, approvals), screenshots in both themes for each screen with no contrast issues found. `npm run build` (production) clean. **3 real bugs found via this verification, not code review, each fixed in the same commit as the screen that surfaced it**: `DELETE /pipelines/{id}` 500ing on any pipeline with run history (own Status Table row); merged-approvals' risk-level bucketing using the wrong scale (own Status Table row, under Approvals-merge); Governance's Audit Log tab showing a stale pre-mutation snapshot because contract create/validate didn't invalidate its query — fixed by adding that invalidation, reverified live (both `contract.created` and `contract.validated` entries appear correctly after the fix). |
| **AXIOM chat UI (Phase 10) — done, fully live-verified** | Real 3-panel screen replacing the Phase 7 stub: `frontend/src/app/(app)/chat/page.tsx` orchestrates `src/components/chat/{SessionList,MessageThread,ContextPanel,ToolCallBlock,useSavedPrompts}.tsx`, wired to `GET /chat/sessions`, `GET /chat/sessions/{id}/history`, and a new `sendChatMessage()`/`POST /chat/` client (`lib/api.ts`). Left: real session list + client-side-only Saved Prompts (localStorage, namespaced per tenant_id — per the locked gap-analysis decision, not a new backend table). Middle: composer (Enter-to-send/Shift+Enter), personality/operation mode selectors (real `PersonalityModeEnum`/`OperationModeEnum` values sent per request), a "still thinking… Ns" loading indicator past 5s, and real tool-call trace rendered inline per assistant turn. Right: "Tool Calls This Session" (session-wide aggregated trace) and a real "Attached Context" data-source picker — sends `context: {source_id, source_name}` on the next message, which `run_agent()` genuinely injects into the system prompt (`agent/dataops_agent.py:132`, confirmed by reading the code, not assumed) — turning the master design's decorative fake "Active Source" stats into a real minimal feature, per the approved plan. Cut per explicit instruction: Agent Memory, Confidence %, the fake "Online · Groq Llama 3.1-8b" status text, and the file-attach button (no real chat-attachment upload path exists). A blocked (`blocked_pending_approval`) tool call renders a distinct callout naming the risk level/reason (when known) and a link to the Approvals screen, not just a status badge — per explicit instruction not to leave that implicit. | **Zero-cost checks are fully live-verified** via Playwright against the real backend (no LLM calls — session list, history load, and saved-prompt/attached-context UI are all pure reads/local state): empty state, session-list empty note, saved-prompt save/reload, attached-context source picker and chip, both themes, no contrast issues. **Live-send checks are partially verified**: the empty-message send guard works (Send disabled, Enter no-ops); the loading indicator appears; and — found opportunistically when both providers hit real quota walls mid-test — the graceful-error path is genuinely proven live: a real backend 500 (Groq TPD exhausted, same failure shape as the documented malformed-tool-call bug) degraded to a clear inline message with the composer re-enabled and no hang, not a crash or silent freeze. That same test caught a real bug: `MessageThread`'s `isError`/`.chat-bubble-error` branch was dead code (the actual error render path is a state variable near the composer, never reachable via a `messages` array item) — removed the unused branch and CSS rule. **All previously-deferred live-send checks are now closed**, verified in a follow-up session via a real Playwright-driven browser against a real seeded tenant (Gemini's daily quota had reset, confirmed fresh before spending): multi-turn continuity (turn 2 correctly referenced turn 1's real tool result with zero re-call, confirmed against the real `GET /chat/sessions/{id}/history` payload); tool-call trace rendering (inline block and the right panel's session-aggregate both render the same real trace — the two `.chat-tool-name` nodes per call are correct-by-design, one per panel); mode-switching (network-captured the real `POST /chat/` payload, confirmed `personality_mode`/`operation_mode` matched the UI selectors exactly); and the advisory approval-gate flow (a real blocked `list_data_sources` call rendered the correct badge/callout/link, **and settled the approval-persistence question with live proof** — a real `ApprovalRequest` row was written and appeared via `GET /approvals`/`GET /approvals/merged`, confirming the code-read finding was correct: `api/v1/chat.py`'s own `ApprovalRequest` creation, not `approval_gate_node`, is what persists these; see the now-resolved Known-broken row). **This pass also caught and fixed a real, previously-unknown bug**: blocked calls always reported `risk_level: "high"` regardless of actual tier, since `approval_gate_node` never attached a real `risk_level`/`reason` to the blocked call dict — fixed to thread `personality.get_risk_level()` through, re-verified live (the same call now correctly reports `"low"` with a real reason, in both the API response and the DB row). Regression test: `test_blocked_tool_call_carries_its_real_risk_level_not_hardcoded_high` (`test_agent_graph.py`); full suite 103 passed. All verification calls ran on `gemini-3.5-flash` primary with zero Groq fallback (~5 real Gemini calls total, well inside the 20/day cap). `npm run build` (production, Turbopack) clean. |
| Reporting | `notification_service.py` (real Slack webhook + SMTP email), `report_generator.py:generate_status_report()` (all 4 personality modes) | **Same "marked done off a code read" problem as Observability.** `generate_status_report()` compared against `RunStatus.success/.failed`, `PipelineStatus.active`, and `IncidentStatus.open/investigating/resolved` (all lowercase, all wrong) — every call for every mode raised `AttributeError`, swallowed into `{"error": ...}`. Fixed; verified live (all 4 modes invoked directly against real data, correct `active_pipelines`/`total_runs_7d`/etc.). Regression test in `test_observability_enum_regressions.py`. `generate_incident_report()` had the same `IncidentStatus.investigating/.resolved` bug and got the same fix in the same commit, but was **not** independently live-verified (no test/live call exercises it yet) — treat it as fixed-but-unverified, not fixed-and-verified. `get_kpi_summary()` wasn't touched (no enum comparisons in it). |
| SQL sandboxing (safe path) | `modules/transformation/sql_runner.py` | `BLOCKED_KEYWORDS`, SELECT/WITH/EXPLAIN-only, parameterized `text()` execution, LIMIT injection, row cap |
| CI/CD pipeline | webhook → `cicd_service` → `cicd_tasks.run_ci_pipeline` (4 checks, risk scoring) → auto-deploy or approval gate; post-deploy monitor + auto-rollback | Full customer-simulation pass (fresh tenant, real webhook, real `pipeline_config.repo_url` match, real quality-check failure → `pending_approval` → manual approve → real active deployment) run end-to-end live — see also `get_pipeline_for_repo()` fix below |
| `get_pipeline_for_repo()` (`services/cicd_service.py`) | Referenced nonexistent `Pipeline.is_active`/`Pipeline.source_config` columns (real ones: `status`, `pipeline_config`) — confirmed via live call: raised `AttributeError`. This function was dead code (never called by the actual webhook handler, which has its own already-correct inline matching logic), so it was not an active blocker despite looking like one during initial testing | 2 regression tests in `test_cicd_service.py` against the real `Pipeline` model (not a mock); verified live against the real DB |
| Pipeline-run status reporting | `_execute_run()` in `services/tasks.py` now checks `ConnectorManager.sync()`'s returned dict for an `"error"` key and raises (marking the run `FAILED` with the real message) instead of unconditionally marking `SUCCESS` regardless of whether the sync actually happened | 2 regression tests in `test_pipeline_run_status.py` using real `Pipeline`/`DataSource`/`PipelineRun` rows; verified live |
| `DELETE /pipelines/{id}` 500s on any pipeline that's ever been run | `PipelineRun.pipeline_id`/`QualityRule.pipeline_id`/`Incident.pipeline_id`/`Incident.run_id` are real FKs into `pipelines`/`pipeline_runs` with no `ON DELETE CASCADE` (unlike the CI/CD tables' `PipelineCommit`/`PipelineDeployment`, which do have it) — a bare `db.delete(pipeline)` raised an unhandled `IntegrityError` the moment the pipeline had any run/rule/incident history, which is the normal case (any real `trigger` creates a `PipelineRun`), not an edge case. In the browser this surfaced as a **CORS error in the console, not a visible 500** — the error response path doesn't carry CORS headers, so Chrome reports "blocked by CORS policy" and the real cause (an unhandled backend exception) is invisible unless you inspect the actual HTTP status via a non-browser client. Fixed in `api/v1/pipelines.py:delete_pipeline()`: `Incident` rows are historical audit records, not pipeline-owned, so they're preserved with `pipeline_id`/`run_id` cleared (not deleted) rather than blocking the delete; `PipelineRun`/`QualityRule` rows are genuinely pipeline-scoped and are deleted with it. `Incident.run_id` is cleared before `PipelineRun` rows are deleted, since it also FKs into `pipeline_runs.id`. Found live while verifying Phase 12's Pipelines screen (create → trigger → pause → activate → delete), not from a code read. | 4 regression tests in `test_pipeline_delete.py` (run history, quality rule, incident-history preservation with refs cleared, tenant scoping); full suite 91 passed. Live-verified via Playwright: the exact create→trigger→pause→activate→delete sequence that previously left a CORS error in the console and the row still in the table now completes cleanly with the row removed. |
| Shared upload volume | Added a named `upload_data` volume mounted at `/root/dataops_uploads` in both `backend` and `celery_worker` in **both** compose files — uploads were previously written outside the `/app` bind mount they already share, so files were invisible to Celery immediately (not just after a rebuild) | Verified live: uploaded a fresh file after recreating both containers, triggered a pipeline against it, run now shows a real successful sync (5 real rows, real file path) instead of the previous false "success" hiding a file-not-found |
| `uploads.py` upload_and_register name handling | `name` is now `Form(None)` so a client-supplied name actually binds (was silently ignored, always falling back to `file.filename`); a name collision (or the points-at-own-database guard) now returns a clean `409` instead of an unhandled `KeyError('id')` crash | 2 regression tests in `test_uploads.py`; verified live — uploaded with a chosen name (confirmed via `GET /sources/`), then a same-name second upload returned a clean 409 instead of crashing |
| **File connector's `sync()` `rows_processed: 0` cosmetic bug (found ahead of YC demo prep)** | `FileConnector.sync()` (`modules/ingestion/connectors/file_connector.py`, the connector actually used for every CSV/Excel/JSON source — confirmed via `ConnectorManager._get_connector()`) returned row count under a `"rows"` key while every sibling connector (`csv_connector.py`, `api_connector.py`, `gsheets_connector.py`, `mysql_connector.py`, `postgres_connector.py`) already returns `"total_rows"`, the key `_execute_run()` (`services/tasks.py`) actually reads — so `rows_processed` on every real, successfully-synced CSV/Excel/JSON pipeline run always showed `0` regardless of real row count. One-line fix: renamed the key to `total_rows`, matching every other connector's existing convention (no reader-side change needed). Found while seeding a demo tenant for the YC application video and noticing `GET /analytics/recent-runs` showed `rows_processed: 0` on runs that had genuinely synced real data. | Live-verified: re-ran the demo seed script after the fix — the same 2 CSV pipelines' successful runs now show real counts (`rows_processed: 10` for a 10-row sales CSV, `8` for an 8-row employee CSV) via `GET /analytics/recent-runs`; the failed run (broken file path) still correctly shows `0`, since it never reaches the row-count line. |
| **Empty-list tool results crashed `POST /chat/` with an unhandled 500 on Groq (found ahead of YC demo prep)** | `cap_tool_result()` (`agent/tools/_utils.py`) returned a bare empty list unchanged when a list-returning tool (`list_open_incidents`, `list_business_rules`) had zero results — LangChain passes that straight through as literal empty `ToolMessage` content, which Groq's chat completions API rejects outright (`'messages.N.content': minimum number of items is 1`), crashing the whole request with no `ChatMessage` persisted. **100% reproducible, not intermittent** — found while rehearsing the YC demo's investigation step: `triage_incident` flips an incident's status from `open` to `investigating` as a side effect (a real, distinct behavior worth knowing on its own — a second "check open incidents" in the same conversation legitimately returns empty once the first triage has run), and that empty result reliably crashed every subsequent `list_open_incidents` call. Fixed by dict-wrapping only the empty-list case (`{"items": [], "count": 0}`) in `cap_tool_result()` — every already-working shape (non-empty lists, dicts) is untouched, confirmed by re-reading the function's other callers. This closes one concrete trigger of the long-standing "malformed tool call crashes `POST /chat/`" Known-broken row; that row's general gap (no top-level exception handler on `POST /chat/` for *any other* unrecovered failure shape) is still open. | Live-verified: reproduced 3× in a row (a fresh `list_open_incidents`-only prompt against an already-triaged incident, confirmed via direct DB query that its status had flipped to `INVESTIGATING`), then confirmed the identical prompt returns a clean `200` with `{"items": [], "count": 0}` after the fix, with no other tool call behavior changed. |
| **Transform-run persistence (Phase 13)** | New `TransformRun` table (`models/all_models.py`, migration `14a3b198ab68`) — bare `tenant_id` (no FK), matching majority convention, accepted consciously. Stores `source_id`/`code`/`status`/`row_count`/`duration_ms`/`error_message`/a 20-row-capped `result_preview` for every real transform execution. Persisted only on real execution (never dry-runs/EXPLAIN or generation-only calls) via a shared `modules/transformation/transform_run_log.py` helper, wired into `POST /run/sql`/`POST /run/pandas` (`origin: "manual"`) and the chat tools `execute_sql_transform`(`dry_run=False` only)/`run_python_transform` (`origin: "chat_agent"`) — both tools gained `user_id`/`session_id` params so `agent_node`'s existing generic force-override loop covers them, same pattern already proven for `request_approval` | 8 regression tests (direct helper tests, REST endpoint tests with mocked runners, chat-tool tests via `.ainvoke()`). **Live-verified against a real CSV file** (copied into the shared upload volume): a real `run/pandas` filter (`df["widget_count"] > 15`) returned real rows and persisted a real success row (`row_count: 2`, real `result_preview`); a real Python syntax error correctly persisted a real error row with the exact error message. Full suite: 119 passed. |
| **Data Catalog aggregation endpoint (Phase 13)** | New `GET /api/v1/catalog/` (`api/v1/catalog.py`) — thin, tenant-scoped aggregation over every `DataSource.schema_snapshot`, no new table. Flattens each source's real per-table-keyed snapshot shape (`{"main": {...}}` for files, `{table1: {...}, ...}` for Postgres/MySQL) into one list of entries, normalizing each profiler's column dict (`column_name`/`data_type`/`is_nullable`) into a consistent `{name, type, nullable}` shape. Never-profiled sources appear with `profiled: false` rather than being hidden. Optional `?source_id=` filter | 5 regression tests (unprofiled source, single-table file source, multi-table Postgres-style source, tenant scoping, `source_id` filter). **Live-verified**: profiled a real CSV source via `POST /sources/{id}/profile`, then confirmed `GET /catalog/` correctly flattened and normalized its real returned schema into the expected shape. Full suite: 119 passed. |
| **`TransformGenerator._resolve_schema()` ran schema-blind for every source** | Found while designing Phase 13's Catalog endpoint (reads the same `schema_snapshot` field): the function read `snapshot.get("columns", [])`, assuming a flat `{"columns": [...]}` shape, but `SchemaProfiler` always nests columns under a table-name key, and each column dict uses `column_name`/`data_type`/`is_nullable`, not `name`/`type`/`nullable`. No crash — just always `None` from `_resolve_schema()` and `"schema_used": False` in every `generate_sql`/`generate_pandas` response, for every source regardless of real profiling data. Every NL→SQL/pandas generation to date ran schema-blind. Fixed to iterate every table using the real field names | 2 regression tests (`test_transform_generator.py`) — one asserting the resolved schema string contains real profiled columns, one asserting those columns reach the actual LLM prompt inside `generate_sql()`. **Live-verified with a real Gemini call**: seeded a source with a distinctive, unguessable column name (`zorbnak_widget_count`), and the real generated SQL correctly referenced it (`schema_used: true`) — proof the schema is genuinely grounding generation now, not just that the function returns non-`None`. |
| **`invoke_llm()` crashed on Gemini 3.5's list-shaped content** | Found live while sanity-checking the schema-grounding fix above: a real `generate/sql` call crashed with `"expected string or bytes-like object, got 'list'"` downstream in `TransformGenerator._extract_code()`'s `re.search()`. `invoke_llm()` (`services/llm_service.py`) returned `r.content` raw — annotated `-> str` but never actually guaranteed to be one. `dataops_agent.py`'s `run_agent()` already had its own normalization for this exact Gemini 3.5 structured-content-block shape (see the LangChain v1.x upgrade row), but `invoke_llm()` is a separate call path (`TransformGenerator`, `IncidentManager`, etc.) that hits the identical response shape and wasn't covered — every consumer of this path was silently at risk of the same crash on any real Gemini call whose response happened to come back as content blocks. Moved the normalization into a shared `content_as_text()` in `llm_service.py`, applied at both `invoke_llm()` return sites; `dataops_agent.py` now imports it instead of keeping its own duplicate | 1 regression test (list-shaped content case). **Live-verified**: the same real `generate/sql` call that crashed before the fix succeeded immediately after, confirmed via `llm_usage_events` that it ran on real `gemini-3.5-flash` with zero fallback. |
| **Transforms + Data Catalog screens (Phase 14)** | `GET /api/v1/transformations/runs` (Phase 13 addendum — tenant-scoped, newest-first, `limit`/`offset`/`source_id` filter; Phase 13 built `TransformRun` persistence but no read endpoint). `frontend/src/app/(app)/transforms/page.tsx` — 4 tabs (Natural Language, SQL Editor, Python Editor, History) wired to real `generate/sql`, `generate/pandas`, `run/sql`, `run/sql/dry-run`, `run/pandas`, `preview/pandas`, `explain`, and the new `runs` list; "Send to Editor" handoff from NL results; Replay (pure frontend state, no backend replay endpoint — loads a past run's code/source into the right editor and switches tabs). `frontend/src/app/(app)/catalog/page.tsx` — real `GET /catalog/` data, client-side search over table/column/tag names, Sync Metadata (sequentially calls `POST /sources/{id}/profile` per unique source with "profiling N of M" progress — Catalog otherwise had no refresh path). New `.code-block` CSS class (`components.css`) for the code editors/generated-code display — referenced in the master design prototype but never actually ported, same "prototype class never made it into our stylesheet" bug class documented elsewhere in this file; uses `--midnight-900`/`--on-dark` per the established permanently-dark-surface pattern | 5 new regression tests for the `runs` list endpoint (tenant scoping, ordering, pagination, `source_id` filter, response shape); full backend suite 124 passed. **Live-verified end-to-end via Playwright** against the real running stack: a real `employee_data.csv` source uploaded/registered/profiled; a real Gemini call generated pandas code correctly referencing real column names (`department`/`name`/`salary`) — confirms Phase 13's schema-grounding fix holds on a second, independent source; Execute ran the real code and returned 4 real filtered rows; the run persisted and appeared in History via the real endpoint; Replay correctly reloaded the code/source and switched tabs (verified against a second real tenant/run); Catalog showed the real profiled table with working search (a real "salary" search correctly matched, a bogus search correctly showed the empty state); Sync Metadata's real effect confirmed via the Last Profiled timestamp genuinely advancing and a real success toast. Both themes screenshotted, no contrast issues (the dark-mode pure-black page background is intentional — Noir is one of the 4 locked palette colors, confirmed via `tokens.css`). `npm run build` (production, Turbopack) clean, 27 routes. |
| **RBAC foundation + high-priority retrofit (Phase 15, steps 1-2)** | New `services/rbac.py`: `Role` enum locking the 5-role set (`owner`/`admin`/`data_engineer`/`data_analyst`/`viewer`) as plain lowercase strings on the existing `User.role` String(50) column — no column-type migration needed. Confirmed via direct query before writing this that the only value ever written to that column pre-Phase-15 is `"owner"` (the sole call site, `create_new_tenant_and_user()`, always sets it), so introducing the other four is a new capability, not a data migration. New `require_role(*roles)` dependency factory next to `get_current_user` in `api/v1/auth.py` (`Depends(require_role(Role.OWNER, Role.ADMIN))` — layers on top of `get_current_user`, so it enforces normal auth too, not just role). Retrofitted onto the full approved high-priority list: `POST /cicd/commits/{id}/approve`/`reject` (Owner/Admin — closes the "CI/CD deployment approve/reject has no role check" Known-broken row), `DELETE /pipelines/{id}`/`/sources/{id}`/`/quality/{rule_id}` (Owner/Admin/Data Engineer), `POST /transformations/run/sql`/`/run/pandas` (Owner/Admin/Data Engineer/Data Analyst — `run/sql/dry-run` deliberately left ungated, it's read-only EXPLAIN), `PATCH /cicd/incidents/{id}/resolve` (Owner/Admin). Medium/low-priority endpoints from `FRONTEND_BUILD_PLAN.md`'s role-check audit are **deliberately deferred, not forgotten** — tracked as "role gate expansion — revisit when real multi-user tenants exist" in Not-yet-built below, since gating them now would add real retrofitting work and surface area for zero live benefit while every tenant in this DB still has exactly 1 real member. | 9 regression tests (`test_rbac.py`) covering every retrofitted endpoint's blocked-role case, plus the allowed-role case for CI/CD approve and pipeline delete. **Live-verified against the real running server, not just pytest**, across two separate rounds: (1) CI/CD approve/reject — a real downgraded-to-`viewer` JWT got a real `403` on a real pending commit (stayed `pending_approval`), the same commit's restored `owner` token then got a real `200` ("approved and queued"); (2) pipeline delete + run/sql — a real viewer JWT got real `403`s on both a real pipeline and a fake source_id, the same user restored to `owner` then got a real `200` deleting the actual pipeline. Full suite: 133 passed (clean, isolated run — see the new Gotcha about concurrent `docker exec`/`restart` corrupting a backgrounded pytest run's results, which produced a spurious `test_catalog.py` failure and a wildly inflated duration on the first attempt before re-running in isolation). |
| **Unified permission spec — REST + AXIOM chat share one capability map (Phase 19, P0 item 1)** | Root cause fixed: the 5-role model was real only for approvals/team/settings/billing; every other mutation (sources, pipelines, quality rules, incidents, contracts, transform execution) was either fully ungated or hand-gated per-endpoint with no consistent rule, and AXIOM's tool-calling path enforced **zero** role checks of any kind — only `operation_mode` + a hardcoded risk tier, completely independent of who was asking. Fixed with one map, two consumers, per the locked spec (`FRONTEND_BUILD_PLAN.md`'s "Phase 19 plan"): `services/rbac.py` gained `PERMISSIONS: dict[str, frozenset[Role]]` (18 capabilities, e.g. `"pipelines.delete"`) and `has_permission(role, capability)`; `require_permission(capability)` (`api/v1/auth.py`, next to the now-removed `require_role`) reads it for every REST endpoint — replacing every existing `require_role(...)` call site (`pipelines.py`, `sources.py`, `quality.py`, `transformations.py`, `cicd.py`, `team.py`, `settings.py`, `billing.py`, `api_keys.py`, `approvals.py`'s inline check) and adding real gates to endpoints that had none before (`sources.py` create/sync/profile, `pipelines.py` create/trigger/pause/activate/schedule/backfill, `quality.py` create/run, `incidents.py` create + `PUT` (closes a loophole where `PUT` could reach `status: "resolved"` ungated even though `POST /resolve` was gated) + resolve, `governance.py` contract create/validate). `TOOL_CAPABILITIES: dict[tool_name, capability]` maps all 38 real AXIOM tools; a new `role_denied_tool_calls(caller_role, tool_calls)` in `agent/dataops_agent.py` — a standalone, directly-testable function, not embedded in the graph — strips any tool call the caller's role can't make *before and independently of* `approval_gate_node`'s risk-tier check, so a role denial can never later become "approved" the way a risk-blocked call can. **Fail-closed by construction**: an unmapped tool is denied for every role, enforced by an import-time `assert` in `dataops_agent.py` (crashes the process at boot, not silently at first use) plus a normal pytest assertion. `get_current_user()` (`api/v1/auth.py`) also gained exactly one additional DB query — re-reads `is_active`/`role` fresh every request (FastAPI's default per-request dependency caching keeps this to one query no matter how many permission/quota checks a request triggers) — closing the "removed/demoted member keeps their old JWT's access for the token's full TTL" gap for **every** authenticated request app-wide, not just role-gated ones. `main.py`'s unauthenticated WebSocket chat endpoint (pre-existing, separate Known-broken row at the time) now hardcodes `caller_role="viewer"` for its `run_agent()` call — narrows what that already-broken path can do via AXIOM tool calls, does **not** fix the underlying missing-auth gap itself. (Superseded: this whole endpoint was later deleted outright, not further hardened — see the "WebSocket chat auth" Known-broken row's `[RESOLVED]` marker and the new Status Table row below.) | 241 backend tests passed (up from ~213 pre-Phase-19, no regressions in any pre-existing test — required updating ~15 existing test call sites that monkeypatch `ALL_TOOLS` with test-only fake tools, since those fake tool names now also need a `TOOL_CAPABILITIES` entry or the new fail-closed gate denies them regardless of role). Three new test files: `test_permission_matrix.py` (5 roles × 18 capabilities against one representative real endpoint each, parametrized, plus a test that fails loudly if a capability is added without a matrix row or explicit exemption), `test_agent_role_gate.py` (`role_denied_tool_calls()` tested directly — unmapped-tool fail-closed, Viewer denied a builder-tier tool, Data Analyst allowed an operator-tier tool, all 12 `view`-tier tools usable by every role including Viewer, a mixed batch only strips the denied calls — no LLM anywhere in this file, per the explicit instruction to avoid burning Gemini's daily quota on routine gate checks), and the cross-consumer regression guard (`test_viewer_blocked_from_pipeline_trigger_both_consumers`) proving REST and the agent tool gate independently deny the same Viewer for the same `pipelines.operate` capability — the actual proof they share one map, not two copies that happen to agree today. **Live-verified against the real running server** (not just pytest), the headline scenario specifically: captured a real viewer JWT, confirmed it blocked (`403`) on `sources.create`; promoted the *same user* to `data_engineer` via direct DB write with **no new token issued**; reused the exact same already-issued JWT (whose own JWT `role` claim still literally read `"viewer"`) — it now passed permission and reached a real `402 quota_exceeded` (proving permission was checked and passed, distinctly from the real quota constraint that fired after it, confirming the locked permission-before-quota check order) on a create call, and cleanly returned a real `200` deleting a real source (a `sources.delete` action, no quota involved) — unambiguous proof the per-request role re-read works, not just at next login. Then deactivated the same still-cryptographically-valid, unexpired token's user via direct DB write and confirmed the identical token was immediately rejected (`401 Invalid token`) on its very next request — closing the JWT-persistence-after-removal gap with live proof, not just a test assertion. **Checkpointed in a follow-up pass, per explicit request, before continuing to further P0 work**: (1) demote vs. deactivate confirmed genuinely distinct, live — a separate fresh user's role-only change (`is_active` left `True`) left their existing, never-reissued JWT able to reach a view-tier endpoint (`200`) while a create-tier endpoint correctly downgraded from `200`→`403` (not `401`) on the same token; (2) the cross-consumer test's actual body was read, not just its pass/fail, confirming both consumers (`POST /pipelines/{id}/trigger` and a direct `role_denied_tool_calls()` call) independently deny the same Viewer and both assert against the identical `PERMISSIONS`/`TOOL_CAPABILITIES` entries; (3) a real SQL-echo capture of a live `POST /sources/` request (chains `require_permission` → `enforce_quota` → the route handler, all three depending on `get_current_user()`) showed exactly 1 `SELECT ... FROM users` query for the whole request — the per-request dependency-caching claim confirmed against real query logs, not inferred from the framework's documented behavior. |
| **File-upload UI for CSV/Excel sources (Phase 19, P0 item 2)** | `frontend/src/app/(app)/sources/page.tsx`'s "Add Source" modal now shows a real `<input type="file">` (accept scoped to `.csv` or `.xlsx,.xls` per the selected type) in place of the raw JSON-textarea connection-config path, for `csv`/`excel` source types only — every other type (Postgres, MySQL, REST API, Google Sheets) is unchanged. New `uploadAndRegisterSource()` (`lib/api.ts`) posts real multipart `FormData` to `/uploads/register` via a dedicated `fetch()` call rather than the shared `request()` helper, since that hardcodes `Content-Type: application/json`, which would strip the multipart boundary a `FormData` body needs. Selecting a file auto-fills the Name field from the filename (still editable; optional — the backend already falls back to `file.filename`). **Found and fixed a real gap in the same pass**: `POST /uploads/register` creates a real `DataSource` — the identical action `POST /sources/` gates behind `sources.create` — but `uploads.py` was never touched in the original Phase 19 permission-spec migration (it wasn't in that pass's file list), so it had *zero* role gate before this fix. Added `Depends(require_permission("sources.create"))` + `Depends(enforce_quota("data_sources"))`, same order as `sources.py`'s `create_source`. | New regression test `test_upload_register_requires_sources_create_permission` (`test_uploads.py`): a real Viewer JWT gets a real `403` on upload. **Live-verified against the real running server**: a real Owner JWT uploaded a real CSV and got back a real `source_id` plus a real 2-row/3-column ingest preview with real column names; the identical user, demoted to Viewer via direct DB write (no new token issued), got a real `403` on the identical upload request. Live-verified in a real Playwright browser too: CSV/Excel show the file input, not the JSON textarea; switching to `postgres` reverts to the JSON config path with the file input hidden; a real upload produces a real success toast and the new source appears in the live sources table. `npx tsc --noEmit` clean. Full backend suite: 242 passed. |
| **`resolve_identity()` missing `is_active` filter (Phase 19, P0 item 3(a))** | Closes a gap flagged since Phase 15's team-removal work: `resolve_password_login()` already filtered `is_active` at the query level (added in Phase 15), but `resolve_identity()` — the shared resolver `email-code` and `google` login both funnel through — had no such filter on either of its two lookup queries (`intended_tenant_id` branch and the general email-lookup branch), so a deactivated/removed team member could still log back into their old tenant via those two methods even though the password path had already been fixed. Added `User.is_active.is_(True)` to both queries in `services/auth_service.py`'s `resolve_identity()`, matching `resolve_password_login()`'s exact pattern — a deactivated user's row simply doesn't match, so `resolve_identity()` returns `{"status": "none"}` (indistinguishable from an email that was never registered — same enumeration-safety property `resolve_password_login()`'s `{"status": "invalid"}` already has, not a special "your account was deactivated" message). | 2 new regression tests: `test_deactivated_user_cannot_log_back_in_via_email_code` (`test_email_code_auth.py`) and `test_deactivated_user_cannot_log_back_in_via_google` (`test_google_oauth.py`) — both create a real user via the real signup path, deactivate via a direct DB write, then confirm the real endpoint now returns `no_account` with no `access_token`, where before the fix it would have logged them in. **Live-verified directly against the real running server** (not just pytest): created a real user via `create_new_tenant_and_user()`, confirmed `resolve_identity()` resolved to `status: "single"` before deactivation; deactivated via a direct DB write; confirmed the identical call now returns `status: "none"`. Full backend suite: 244 passed (2 unrelated pre-existing flaky failures seen once, both in the already-documented Redis rate-limiter cross-contamination Gotcha — confirmed by clearing `otp:*` keys and re-running clean twice). |
| **Invite email link pointed at a 404 (Phase 19, P0 item 4)** | `services/team_service.py`'s `_invite_link(token)` built `{base}/accept-invite?token=...`, but the real frontend route (added during Phase 17's Team screen build to close a gap the original locked spec left open) is `/invite/accept?token=...` (`frontend/src/app/invite/accept/page.tsx`, which reads the same `token` query param) — every real invite email sent since that route was built pointed the recipient at a 404. Confirmed via a repo-wide grep this is genuinely the only place in the whole backend (`services/`, `api/`, `modules/`) that constructs a frontend URL — an isolated fix, not a pattern needing a hunt elsewhere, re-confirming the same finding from this item's original planning pass. One-line fix: `/accept-invite` → `/invite/accept`. | New unit test `test_invite_link_points_at_the_real_frontend_route` (`test_team_invites.py`) — asserts the exact string `_invite_link()` produces, since every existing integration test mocks `send_invite_email()` (which calls `_invite_link()` internally) and had never actually exercised this function's output, which is exactly how the bug shipped unnoticed. **Live-verified end-to-end against the real running server with a real Resend delivery** (same technique as Phase 15/17's invite verification): sent a real invite, confirmed `last_event: "delivered"` via Resend's own API, fetched the real delivered email body and confirmed it now reads `http://localhost:3000/invite/accept?token=...` (not `/accept-invite`); then loaded that exact real link in a real Playwright browser and confirmed it resolves correctly — the accept page rendered the real tenant name and real invited role, not a 404. Full backend suite: 245 passed. |
| **Scheduled pipelines cluster (Phase 19, P0 item 5)** | Started as "fix the Celery beat argument mismatch" but investigation surfaced a materially bigger, pre-existing gap: `Scheduler.register()`/`unregister()`/`sync_all()` (`modules/orchestration/scheduler.py`) are never called from anywhere in the running app — confirmed via a repo-wide grep, zero call sites outside the module's own tests — so the arg-mismatch bug was never actually reachable to begin with; worse, even a correctly-wired call would still do nothing, since `register()`/`sync_all()` mutate `celery_app.conf.beat_schedule` in whichever OS process calls them, and the FastAPI `backend` process and the separate `celery_beat` process each hold their own independent in-memory copy of that dict (Celery's default `PersistentScheduler` is not shared across processes) — a call from `backend` was never going to reach the actual running beat process's schedule. **Fix: replaced the dynamic-injection design with a real, live static-polling mechanism**, matching the already-proven pattern of the existing `check_all_freshness()` static entry rather than inventing a new architecture. New `services/tasks.py` additions: `check_scheduled_pipelines()` (new static beat entry, `services/celery_app.py`, every 60s) polls every `ACTIVE` pipeline with a `schedule_cron` directly from the DB each tick and fires any whose cron matches the current UTC minute via `croniter.match()` (the already-installed-but-previously-unused-for-this-purpose `croniter` dependency); `_create_scheduled_run()` creates a fresh `PipelineRun` row (`triggered_by="scheduled"`) per firing, since — unlike the manual/API trigger path, which pre-creates its run inside a request/response cycle — a scheduled firing has no such window and needs a new run_id created at fire time, every time; `execute_scheduled_pipeline_run()` (2-arg task: `pipeline_id`, `tenant_id` — no `run_id`, matching what a static beat entry can actually supply) creates its run then delegates to the same `_execute_run()` core the manual path uses. `scheduler.py`'s own arg-mismatch (`args: [pipeline_id, tenant_id]` vs. the 3-arg `execute_pipeline_run`) was still fixed for internal consistency and to keep its existing test green, and its class docstring now explains why it's dead code relative to the real mechanism — kept in place rather than deleted, since its logic is otherwise correct post an earlier session's enum-case fix and has its own test coverage. **Save-time cron validation**: new `_validate_cron_or_400()` (`api/v1/pipelines.py`, using `croniter.is_valid()`) wired into all 3 write paths (`create_pipeline`, `update_pipeline`, `set_schedule`) — a malformed cron now gets a real `400` at save time instead of silently persisting and only ever surfacing (if at all) as a log line during a later poll. **Run visibility**: `DAGManager.list_pipelines()` (`modules/orchestration/dag_manager.py`) now returns a computed `next_run_at` per pipeline (via `croniter`, `None` for anything not `ACTIVE`+scheduled — showing a time for a paused pipeline would show a time that will never fire) and a real `last_run` summary (id/status/triggered_by/timestamps, fetched in one tenant-wide query rather than N+1); the Pipelines screen (`frontend/src/app/(app)/pipelines/page.tsx`) gained a new "Next / Last Run" column rendering both. | 9 new regression tests (`test_scheduled_pipelines.py`): `_create_scheduled_run()` creates a real pending run / skips a paused pipeline / skips a deleted one; `_check_scheduled_pipelines()` fires a `* * * * *` pipeline and correctly skips a never-due cron, a paused pipeline, and an unscheduled one (mocked `.delay()` to observe dispatch without a real worker); a malformed DB-level cron is skipped with a warning rather than crashing the whole poll (so one tenant's bad data can't starve every other tenant's scheduled runs); cron rejection/acceptance at all 3 REST write paths; `list_pipelines()`'s new fields reflect real state through an activate + a real scheduled-run creation. **Live-verified against the real running server — the exact gate the locked plan specified**: restarted both `celery_beat` and `celery_worker` (their code doesn't hot-reload — a Gotcha already documented elsewhere in this file for `backend`, confirmed true here too) to pick up the new task and beat entry; created and activated a real pipeline with `schedule_cron: "* * * * *"`; polled the real run-history endpoint and watched **3 real, successful, correctly-timed runs land one minute apart** (`triggered_by: "scheduled"`, real quality-check output, no duplicate/double-fired runs in any minute); confirmed `GET /pipelines/` correctly showed `next_run_at`/`last_run` reflecting this real state; paused the pipeline and confirmed both `next_run_at` immediately went `null` and no further runs fired; confirmed the same UI update live in a real Playwright browser (DOM-inspected the actual table row, not just a text-includes check, since `innerText` whitespace normalization gave a false negative on the header text first). **One pre-existing, not-newly-introduced issue found and investigated, not fixed**: worker logs show a `RuntimeError: ... got Future ... attached to a different loop` / `Exception closing connection` warning around some task boundaries — confirmed via log inspection that this **already occurs on the untouched, pre-existing `execute_pipeline_run` task** too (identical error signature, same asyncpg-connection-cleanup-across-event-loops class as the already-documented `engine.dispose()` Gotcha), so it's not a regression from this fix; all observed tasks still completed successfully with correct DB state despite the warning — see the new Gotcha below. Full backend suite: 254 passed. |
| **Dark theme palette redesign (Phase 19 dark theme, started) — token change landed, screenshot verification pass still open** | `tokens.css`'s dark-mode block redesigned: warm near-black neutral surfaces (`#101015` base, `#1B1B21` elevated) replace the original pure-black-page + Midnight-navy-card pairing, which read as murky; Ocean blue no longer appears in any surface fill, only as an accent (buttons, focus rings, highlights); text tokens shift from blue-tinted greys to desaturated neutral greys for secondary/muted tiers; primary text becomes a warm off-white (`#F1F0E1`). Also found and fixed a real pre-existing bug while checking every text pairing: `--ocean-600`/`700` (used as literal link/text color in a few spots — auth-link-btn, breadcrumb hover, notif footer link) failed AA against the *old* dark surfaces too (3.27–4.41:1), not just the new ones — fixed with a dark-mode-only override, light mode's values untouched. Light mode's `:root` block is untouched (confirmed via diff) — dark mode only. Every text pair now clears 4.5:1 with real margin (smallest is 5.39:1, on the pair that failed before). **Not yet complete**: this commit's own message flagged a required screenshot verification pass across every screen, both themes, as the next step — that pass has not happened yet. **This documentation entry itself is retroactive** — the original commit (`332d698`) changed only `tokens.css` with no CLAUDE.md/FRONTEND_BUILD_PLAN.md update, a same-commit-rule violation caught and backfilled during this session's end-of-day doc cleanup, not at the time. | Contrast ratios computed and verified per-pair (script, not visual judgment) at commit time — see the numbers above. **Visual verification (screenshots across all screens, both themes) is the open item** — this is next session's recommended starting point, see the top of this file. |
| **Dashboard Chart.js trend colors never actually resolving CSS custom properties, in either theme** | Found live while starting the dark-theme screenshot pass: both Dashboard trend charts' `borderColor`/tick-color values were raw `"var(--x)"` strings passed straight to Chart.js, which draws to Canvas 2D — and Canvas 2D silently rejects an unresolved `var()` reference (confirmed live: assigning `ctx.strokeStyle` to one is a no-op, not an error, so whatever color was already set just stays). Result: chart line/fill colors and axis-tick colors were never actually using the token system in **either** theme, not just dark — a pre-existing bug, unrelated to and not introduced by the dark-theme redesign, just surfaced by looking at the charts closely during that work. Fixed with a small `resolveToken()` helper (`components/dashboard/TrendCharts.tsx`) that reads the real computed value via `getComputedStyle` at render time. | Verified via pixel-sampling a real rendered dashboard in both themes: dark now shows real green/red/Ocean colors matching the new dark-mode tokens; light shows its own original values (now also correctly rendering for the first time). **Retroactively documented** — the original commit (`42b7095`) touched only the component file (plus 2 reference mood-board images), no doc update at the time; backfilled during this session's end-of-day cleanup. |
| **Team screen production-build failure (`react/no-unescaped-entities`)** | Found running `npm run build` during the dark-theme work — unrelated to it (the offending line was last touched in Phase 17, confirmed via `git log`). Raw apostrophes in JSX text failed ESLint's `react/no-unescaped-entities` rule, which only runs on `next build`, not `next dev` — Team's own Phase 17 build/verification loop was dev-server-only and never caught it. Fixed by escaping the apostrophes (`frontend/src/app/(app)/team/page.tsx`). | `npm run build` clean afterward. **Retroactively documented** — the original commit (`a4ef4d7`) touched only the one file, no doc update at the time; backfilled during this session's end-of-day cleanup. |
| **Team invites (Phase 15, step 3)** | New `TeamInvite` table (`models/all_models.py`, migration `a4f7c92b1d05`) — real FK to `tenants`/`users` (matching `User`'s own pattern, not the majority bare-String convention), `token_hash` (SHA-256, same TTL+single-use reasoning as `EmailLoginCode.code_hash`), `role` locked at invite time from `services.rbac.Role`. New `services/team_service.py`: `create_invite()` (rejects invites to an email already a member of that tenant; a fresh invite to the same `(tenant, email)` supersedes and revokes any still-pending one, avoiding stacked live links), `send_invite_email()` (same Resend-calling shape and "never raise on delivery failure" contract as `auth_service._send_code_email`), `accept_invite()` (tenant_id and role come from the invite record, never from the request body — per Phase 3's locked invite-join design; creates a real password-authed `User` row in the *existing* tenant). New `api/v1/team.py`, registered at `/api/v1/team`: `POST /invites` (Owner/Admin, returns the raw token once for copy/paste sharing as a fallback to email), `GET /invites` (Owner/Admin, lists all invite states), `DELETE /invites/{id}` (Owner/Admin, revoke), `GET /invites/verify?token=` (public, lets an accept UI show who's inviting/to what role/workspace before commit), `POST /invites/accept` (public, token-based). Accept is password-only for now — Google/email-code-based invite acceptance would need deeper `resolve_identity()` integration, deliberately out of this pass's scope. | 6 regression tests (`test_team_invites.py`, Resend mocked — real delivery proven live separately, see next column): full accept round trip including a real subsequent login, viewer blocked from creating invites (403), self-invite-into-own-tenant rejected ("already a member"), bogus/expired token rejected on both `verify` and `accept`, revoked invite can't be accepted, a second invite to the same email supersedes the first (stale token stops working, fresh one succeeds). **Live-verified end-to-end against the real running server with a real Resend delivery**: registered a fresh tenant, sent a real invite to the Resend account owner's own address (the only real deliverable recipient under the current sandbox restriction — see the Gotcha), confirmed `last_event: "delivered"` directly via Resend's own `GET /emails` API with the correct subject line; `GET /invites/verify` correctly showed the pending invite's role/tenant before accept; `POST /invites/accept` created a real `User` row in the *original* tenant with the locked `data_engineer` role (not owner, not a new tenant); a real subsequent `POST /auth/login` with the new password succeeded and returned the same `tenant_id`; `GET /invites` afterward correctly showed `status: "accepted"` with a real `accepted_at` timestamp. This is the exact verification gate `FRONTEND_BUILD_PLAN.md`'s Phase 15 row calls for. **Caught and fixed one self-inflicted bug before it reached a commit**: an earlier edit to `all_models.py` (adding the `TeamInvite` class) was written against a truncated `Read` that didn't show the file's actual last line (`TransformRun`'s own `created_at` column — the file had no trailing newline, and `Read` silently omitted it), causing the edit to insert `TeamInvite` *before* that line rather than after `TransformRun`'s real end — orphaning `TransformRun.created_at` (dropped `AttributeError`s in `test_transform_runs_list.py`) and silently duplicating a stray `created_at` inside `TeamInvite`. Full suite went from a clean 133 to `15 failed` on the next run; root-caused via `git diff` (not guesswork) and fixed by restoring the line to `TransformRun` and removing the duplicate. Re-ran clean: 139 passed. **General lesson, not project-specific**: a `Read` on a file with no trailing newline can silently omit the last line from view — when appending a new class/section at the end of a model file, verify the insertion point with `git diff` after editing, not just by trusting the tool call succeeded. Full suite: 139 passed. |
| **Team member management (Phase 15, step 4)** | New `api/v1/team.py` endpoints: `GET /members` (any authenticated tenant member — the roster itself isn't sensitive), `PATCH /members/{id}/role` (Owner/Admin — an Admin can't change an Owner's role or grant Owner, prevents privilege escalation via this endpoint; blocks demoting the last active Owner), `DELETE /members/{id}` (Owner/Admin — soft-removal via `is_active=False`, **not a hard delete**: `User` is referenced by real FKs with no cascade, `OnboardingProfile.user_id` and `TeamInvite.invited_by_user_id`, so a hard delete would repeat the exact `IntegrityError`-on-delete bug class already documented for pipelines; blocks self-removal and blocks Admin from removing an Owner; a redundant last-owner count check is kept as defense in depth even though it's currently unreachable given the other two guards — see the code comment). **Real bug found and fixed while building this, required for the feature to have any teeth**: `User.is_active` existed as a column but was never checked anywhere in the login path — `resolve_password_login()`'s query had no `is_active` filter, so a "removed" member could still log in with their password, silently defeating the whole feature. Fixed by adding `User.is_active.is_(True)` to that query's `WHERE` clause. **Scoped deliberately, not exhaustively**: this fix only covers password login; Google/email-code login (`resolve_identity()`) and any JWT already issued to a removed user before removal are *not* covered — the latter is the pre-existing, already-documented "no JWT revocation" Gotcha, not a new gap introduced here. | 11 regression tests (`test_team_members.py`): roster listing, admin role-change on a non-owner, admin blocked from touching an owner's role or granting owner (403 both), demoting the last owner blocked (400) vs. a second owner successfully demoted, self-removal blocked (400), admin blocked from removing an owner (403), removing a second owner when not the last one succeeds, viewer blocked from both mutations (403). **Live-verified against the real running server**: real invite→accept (reusing the just-built invite flow) produced a real second tenant member; confirmed real login succeeded before removal (200); owner removed them via a real `DELETE`; the identical login attempt immediately after returned a real `400 {"detail":"Invalid credentials"}` — proving the `is_active` login fix actually works, not just that the test suite's mocked path agrees with itself; `GET /members` afterward correctly showed `is_active: false` on the removed row while the owner's own row stayed `true`. Full suite: 150 passed (isolated run, no concurrent `docker exec`/`restart` — see the earlier Gotcha about why that matters). |
| **Plan/quota enforcement (Phase 15, step 5)** | New `services/quota_service.py` — `PLANS` dict (3 hardcoded tiers, same config-dict pattern as `personality.py`'s `RISK_ACTIONS`/`policy_engine.py`'s `TOOL_REGISTRY`; no new usage-tracking table, every resource is a live aggregate query against data that already exists): Starter 25,000 AI credits/mo, 100 pipeline runs/mo, 3 data sources, 3 team members; Growth 100,000/1,000/15/10; Scale 500,000/10,000/unlimited/25. **Credit formula**: `credits = input_tokens × 1 + output_tokens × 3` — the 3x weight approximates real provider cost skew (completion tokens cost more than prompt tokens industry-wide); `reasoning_tokens` is a documented *subset* of `output_tokens` (not additive — confirmed from `LlmUsageEvent`'s own column comment before choosing this formula), so no double-counting. Chosen over 3 other options considered (flat 1:1, a real per-model $/token table, flat per-call) as the pragmatic default until Stripe billing is genuinely live and per-model $/token pricing needs to be exact rather than approximate — full option set documented in `quota_service.py`'s own module docstring. **Tier limits were calibrated against real usage before shipping, not guessed**: the originally-proposed Starter cap (2,000 credits/mo) was checked against this project's own real dev/verification-tenant `llm_usage_events` history first and found badly miscalibrated (several actively-reused verification tenants already showed 4,000-84,000 credits/mo from ordinary live-LLM-testing sessions) — recalibrated upward (25,000/100,000/500,000) using that real data as the signal, discounting the extreme high end as accumulated-across-many-sessions test-tenant reuse rather than one real customer's month; sources/runs/members limits were left as originally proposed since the same check found those already realistic (max observed across ~2,900 dev/test tenants: 5 sources, 1 member, 6 runs/mo). New `enforce_quota(resource)` dependency factory (`api/v1/auth.py`, next to `require_role`) hard-blocks with `402 {"error": "quota_exceeded", resource, used, limit, percent, status}` once usage reaches the limit; soft-warning at 80% is deliberately *not* enforced by the dependency (a dependency has no clean way to attach a non-blocking warning to an otherwise-successful response) — instead surfaced via new `GET /api/v1/billing/usage` (any tenant member, all 4 resources) and `GET /api/v1/billing/plans` (public). Wired onto the 4 clearest cost/volume drivers: `POST /chat/` (`ai_credits` — blocks *before* any LLM call, so an over-quota tenant never spends provider tokens on a request that's going to be rejected anyway), `POST /pipelines/{id}/trigger` (`pipeline_runs`), `POST /sources/` (`data_sources`), `POST /team/invites` (`team_members`, stacked with the existing `require_role` check on the same endpoint). | 10 regression tests (`test_quota_enforcement.py`): the credit formula directly, `get_quota_status()`'s ok/exceeded transition against real seeded `DataSource` rows, all 4 hard-block endpoints returning a real `402` with the correct `resource` field, the Scale tier's `None` (unlimited) limit never blocking, `GET /billing/usage`/`GET /billing/plans` shape. **One test-seeding bug caught before it became a habit**: the first version of the AI-credits test used integer-divided `output_tokens` to hit an exact credit target, undershot the limit by a few credits due to truncation, and the request it expected to be blocked instead reached the real chat handler and made **one real, billable Gemini call** (confirmed via `llm_usage_events`, `provider: "gemini"`, timestamped exactly during that test run) — a real cost from a test bug, not a quota-logic bug. Fixed by seeding via `input_tokens` (1x weight, no division) instead of `output_tokens` (3x weight); re-ran the single test afterward and confirmed via a direct DB timestamp check that zero new real LLM calls occurred. **Live-verified against real, pre-existing historical usage data, not synthetic seeds** — this is the exact verification gate `FRONTEND_BUILD_PLAN.md`'s Phase 15 row calls for: minted a token for a real tenant from earlier in this project ("Verify35 Co," genuinely accumulated 83,930 credits across many live-LLM-verification sessions), `GET /billing/usage` correctly reported `ai_credits: {used: 83930, limit: 25000, percent: 335.7, status: "exceeded"}` (matching the raw-SQL figure computed earlier in this same session, confirming the query is correct) while its other 3 resources correctly showed real `"ok"` statuses at their real percentages; a real `POST /chat/` against that same tenant returned a real `402` with the exact quota_exceeded shape, zero LLM call made. **Soft-warn band also live-verified, separately**: seeded a fresh tenant to 85/100 pipeline runs, confirmed `GET /billing/usage` reported `status: "warning"` at `85%`, then confirmed a real `POST /pipelines/{id}/trigger` against that same tenant still succeeded (`200`, real run created) — proving the warning band is genuinely non-blocking, not just that the number renders. Full suite: 160 passed. |
| **`BillingService` interface, Stripe stubbed (Phase 15, step 6)** | New `services/billing_service.py` — the interface every payment/plan operation should go through. `get_usage()`/`check_quota()`/`get_plan()` are thin real wrappers over `quota_service.py`'s already-real, already-live-verified functions. `change_plan(new_plan)` is **real today, not stubbed** — a direct `Tenant.plan` write — because there's no real payment to gate it on yet; documented in both the module docstring and the `POST /billing/change-plan` endpoint's own docstring as a deliberate dev-mode stand-in that must become Stripe-webhook-triggered-only once real payment collection exists, flagged specifically so that transition isn't missed later. `create_checkout_session()`/`get_subscription_status()`/`cancel_subscription()`/`handle_webhook()` all raise `NotImplementedError` with a message pointing at what to use instead today. New endpoints on `api/v1/billing.py`: `GET /plan` (current plan+limits), `POST /change-plan` (Owner/Admin), `POST /checkout` and `POST /webhook` (Owner/Admin and public respectively, both honest `501`s — same "refuse rather than lie about success" pattern already established for `export_dataset`/`standardize_dataset` in the agent tools punch list, not a silent 200 that pretends to work). | 10 regression tests (`test_billing_service.py`): both service-layer and endpoint-layer coverage for `get_usage`/`get_plan`/`change_plan` (including the unknown-plan-name rejection, at both layers), a viewer blocked from changing the plan (403), and all 4 stub methods asserted to raise `NotImplementedError`. **Live-verified against the real running server**: a real fresh tenant's plan read as `"starter"`; a real `POST /change-plan` to `"growth"` returned the correct new limits and **persisted** (a subsequent `GET /plan` on the same tenant, a separate request, showed `"growth"`, not a same-request-only echo); `POST /checkout` returned a real `501` with a message pointing at the working `change-plan` alternative. Full suite: 169 passed. |
| **`get_agent()` cache re-keying + per-tenant AI model override (Phase 16, step 1)** | Closes Phase 0's approved pushback #2 (mandatory before any per-tenant model selection could ship — see the now-resolved Gotcha above for the full history). New `SUPPORTED_MODEL_OVERRIDES = ["gemini-3.5-flash", "llama-3.3-70b-versatile"]` allowlist in `services/llm_service.py` (kept in exactly one place — every validation call site imports this same list, nothing duplicated). `get_primary_llm()`/`get_llm_for_agent()` accept an optional `primary_model` override (fallback model always stays the global `settings.FALLBACK_LLM_MODEL` — fallback is reliability infrastructure, not a per-tenant preference). `build_agent()` only passes `primary_model` through when actually set, preserving the exact `get_llm_for_agent(temperature=0.0)` call shape every existing `test_agent_graph.py` fixture already monkeypatches — avoided touching ~8 unrelated test fixtures for a kwarg they never use. `get_agent()` now requires `tenant_id` (raises `ValueError` otherwise) and keys its cache as `f"{tenant_id}:{personality}:{operation}:{primary_model or 'default'}"`. New `services/settings_service.py`: `get_ai_model_override(tenant_id)` reads `Tenant.settings.ai_model_override` (re-validates against the allowlist on *read*, not just write — a value valid when set but since removed from the allowlist can't silently keep being used) and `update_tenant_settings()` validates on write. New `GET/PATCH /api/v1/settings/` (any member can view; Owner/Admin to change) — the real, user-facing way to set the override, reusing `Tenant.settings` (existing, previously-unused JSON column — zero migration needed for this sub-step). | 17 regression tests across two files. `test_get_agent_cache_isolation.py` (10): `get_agent()` rejects a missing `tenant_id`; repeated calls with identical args return the *same* cached object (proves the caching optimization itself didn't regress); two tenants with identical personality/operation/model still get *different* objects; the same tenant asking for two different model overrides gets two different objects; the full cross-tenant scenario (two tenants, two different overrides) proven both via direct `get_agent()` calls and via the real `run_agent()` entry point with a monkeypatched LLM layer that records exactly which `primary_model` string reached it per tenant (`["gemini-3.5-flash", "llama-3.3-70b-versatile"]`, never swapped or blended); the allowlist read-time re-validation. `test_settings_api.py` (7): get/patch round trip, role gating (403 for viewer), unsupported-model rejection (400), partial updates don't clobber other fields, clearing an override back to `null`. **Live-verified against the real running server with two real, different LLM providers, not synthetic seeds**: registered tenant A, set its override to `llama-3.3-70b-versatile` via a real `PATCH /settings/`, sent a real chat message — `llm_usage_events` confirmed `provider: "groq", model: "llama-3.3-70b-versatile", used_fallback: false` (a genuine primary call, not a fallback); registered tenant B with zero settings changes, sent a real chat message — confirmed `provider: "gemini", model: "gemini-3.5-flash"`; two real, back-to-back requests, two genuinely different real providers, zero cross-contamination. Full suite: 188 passed. |
| **Workspace config: name/timezone/description (Phase 16, step 2)** | Extended `services/settings_service.py`'s `get_tenant_settings()`/`update_tenant_settings()` to present a single unified view merging `Tenant.name` (a real, pre-existing column — workspace rename writes here directly) with the `Tenant.settings` JSON blob's `timezone`/`description` keys, so `GET/PATCH /api/v1/settings/` callers don't need to know which field lives on which storage. Rename validates non-empty (rejects whitespace-only names with a `400`); `slug` is deliberately left untouched on rename — treated as a permanent identifier separate from the mutable display name, avoiding any URL-breaking implication. | 4 new regression tests (`test_settings_api.py`, now 10 total in that file): a fresh tenant's settings show only its real registered name (`set(keys) == {"name"}`, confirming no phantom defaults leak in); rename persists to the real `Tenant.name` row (checked via direct DB query, not just the response echo) and survives a fresh `GET`; empty-name rejection; timezone+description alongside a rename in one request. **Live-verified against the real running server**: registered a tenant as "Original Corp Name 2", confirmed `GET /settings/` showed exactly that; a real `PATCH` renamed it to "Renamed Live Corp" and set timezone/description; a subsequent, separate `GET` request (not the PATCH response) confirmed all three values persisted. Full suite: 191 passed. |
| **Notification prefs + a real, previously-unknown `NotificationService` bug found and fixed (Phase 16, step 3)** | Extended `services/settings_service.py` with `notification_prefs` support: a proper one-level deep merge (a partial update like `{"slack_webhook_url": "..."}` no longer wipes out an already-set `notify_on`), shape validation (unknown top-level keys, non-dict `notify_on`, non-bool toggle values, unknown `notify_on` event names all rejected with a clear `400`), and `get_notification_prefs()` (fills in `DEFAULT_NOTIFY_ON = all True` for any unset toggle so callers never handle missing keys). Wired into `modules/reporting/notification_service.py`'s `send_slack()`/`send_alert()`: both now check the tenant's own configured webhook/email first, falling back to the global setting only if unset — the first real use `self.tenant_id` has ever had in this class. **A real, separate, deeper bug found and fixed along the way, required for the tenant-fix to mean anything**: every global fallback in this module (`AXIOM_SLACK_WEBHOOK_URL`, `AXIOM_SLACK_CHANNEL`, `AXIOM_ALERT_EMAIL`, `AXIOM_SMTP_HOST`/`PORT`/`USER`/`PASSWORD`/`FROM`/`USE_TLS`) referenced `Settings` fields that **never existed** — the real declared fields are `SLACK_WEBHOOK_URL`, `SLACK_DEFAULT_CHANNEL`, `SMTP_HOST`/`PORT`/`USER`/`PASSWORD` (no `AXIOM_` prefix, confirmed by reading `config.py` in full), and there was no declared equivalent of `ALERT_EMAIL`/`SMTP_FROM`/`SMTP_USE_TLS` at all. Every `getattr(settings, "AXIOM_*", default)` call was silently and permanently falling through to its Python-side default regardless of what was ever configured — this method could never have sent a single real Slack message or email, to anyone, tenant-specific or global, since it was written. Fixed by (1) adding `SMTP_FROM`, `SMTP_USE_TLS`, `ALERT_EMAIL` as real declared `Settings` fields (`config.py`) alongside the already-real `SLACK_WEBHOOK_URL`/`SLACK_DEFAULT_CHANNEL`/`SMTP_HOST`/`PORT`/`USER`/`PASSWORD`, and (2) rewriting every reference in `notification_service.py` to the real field names. | 7 regression tests (`test_notification_prefs.py`): the deep-merge behavior (two separate cases — top-level fields preserved, `notify_on` sub-keys preserved across independent partial updates), shape validation (4 rejection cases in one test), the `/settings/` endpoint accepting `notification_prefs`, and — the core proof — `send_slack()` genuinely reading a monkeypatched-real `urlopen` call and confirming the exact URL requested matches the tenant's own configured webhook over a monkeypatched global fallback, and the reverse (falls back correctly when unset). **Live-verified against the real running server with two real, live-seeded tenants and a real spied network call (not just HTTP status, which turned out to be a false signal — see below)**: tenant A (webhook set to a made-up path) — spying on the real `urllib.request.urlopen` call confirmed the exact request URL matched tenant A's own configured webhook, not the global fallback; tenant B (no override) — the same spy confirmed it used the real global `settings.SLACK_WEBHOOK_URL` value (the known dev placeholder) and got a genuine `404` from Slack's real API — the *same* failure signature already observed live in Phase 15's CI/CD testing, now reachable through this code path for the first time since the field-name fix. **A methodology note worth keeping**: the first live-verification attempt trusted `{"sent": True}` as proof of correct routing and was wrong — `urllib.request.urlopen` follows redirects by default, and Slack's real servers `302` a malformed webhook path to a generic landing page that returns `200`, so *any* URL (tenant-specific or a nonsense global one) would have reported `sent: True` under that methodology, proving nothing about which URL was actually used. Switched to spying on the real `urlopen` call directly to capture the actual requested URL — the only technique that genuinely distinguishes the two code paths. Full suite: 198 passed. |
| **Theme server-persistence (Phase 16, step 4)** | New `User.theme` column (nullable `String(20)`, `"light"\|"dark"\|"system"\|None`) — **per-user, not per-tenant**, matching the design decision made when this phase was first proposed (a personal UI preference, deliberately kept separate from `Tenant.settings`). Real `ALTER TABLE` applied directly against the dev DB (migration `b7e2f4a91c33` written for review/future real deploys, per the established "`create_all()` can't alter an existing table" Gotcha — same pattern as Phase 5's `User` migration). Deliberately **not** a JWT claim, for the identical staleness reasoning Phase 3 already applied to `personality_mode`/`operation_mode` — `GET /auth/me` now merges the JWT payload with a fresh per-request DB read of `theme` rather than baking it into the token at issue time. New `PATCH /auth/me` (self-service, no role check — a user changing their own preference needs no special permission beyond being authenticated as themselves), validates against `("light", "dark", "system")`. | 6 regression tests (`test_theme_persistence.py`): defaults to `null`, set-and-persist (checked via direct DB query, not just the response echo), invalid-value rejection, empty-body rejection, clearing back to `null`, and the per-user isolation case — two members of the *same* tenant set different themes independently, proving it's genuinely personal and not accidentally tenant-scoped. **Live-verified against the real running server**: a fresh user's `GET /auth/me` showed `theme: null`; a real `PATCH` set it to `"dark"`; a subsequent, separate `GET` request confirmed persistence. Full suite: 204 passed. |
| **Platform API keys — CRUD only, no request authentication yet (Phase 16, step 5)** | New `ApiKey` table (`models/all_models.py`, migration `c8f3a7d52e19`) and `services/api_key_service.py`: `create_api_key()` generates a real high-entropy secret (`axm_live_` + 32 url-safe random bytes), stores only its SHA-256 hash (same TeamInvite/EmailLoginCode-style reasoning — the raw key is already high-entropy, so hash-slowness protection isn't the relevant threat model) plus a short `key_prefix` for UI display, and returns the raw key **exactly once** in the create response, matching `TeamInvite`'s already-established create-once pattern. `list_api_keys()` always returns the masked shape (never `key_hash`, never the raw key). `revoke_api_key()` is idempotent (a retried revoke is a no-op, not an error). New `api/v1/api_keys.py`: `POST/GET/DELETE /api/v1/api-keys/`, all Owner/Admin-gated — unlike the team roster or usage summary, API keys are sensitive infrastructure credentials even in masked form. **Scope explicitly limited to CRUD, per the approved decision**: nothing in this app accepts one of these keys as a request credential yet — no new auth dependency, no endpoint checks for an `Authorization: Bearer axm_live_...` header. That's tracked as its own, separately-sized future item (a new auth dependency touching every protected endpoint's resolution path, plus key-scoped rate limiting) in Not-yet-built below — **Phase 17's Settings UI must not imply these keys can authenticate anything yet**; copy should describe what they're for today or label the capability as coming, not claim more than what's built. **A recurring tooling bug caught and fixed a second time**: appending `ApiKey` to `models/all_models.py` hit the exact same `Read`-near-EOF blind spot documented in Phase 15's Gotcha — worse, `git diff` after this edit revealed the *original* Phase 15 fix for this same bug had been incomplete: a stray duplicate `TeamInvite.created_at` line (with `index=True`) had been silently sitting at the true end of the file this whole time, invisible to every subsequent `Read`, and had been silently giving that column a real index in production since Phase 15 shipped (confirmed via `\d team_invites` — `ix_team_invites_created_at` genuinely exists). Not a correctness bug (an extra index is harmless, and arguably a good fit for `GET /team/invites`'s real `ORDER BY created_at` query) — fixed by declaring it intentionally (`index=True`, with a comment explaining why) rather than treating it as pure accident to be erased. | 9 regression tests (`test_api_keys.py`): raw key shown once on create and genuinely stored only as a hash (checked via direct DB query — the stored value isn't the raw key and is a real 64-char sha256 hex digest); list never exposes `raw_key`/`key_hash`; revoke persists and is idempotent (double-revoke doesn't error); revoking an unknown id 404s; empty-name rejection; viewer blocked from all 3 mutations (403 each); tenant isolation (tenant B's list never shows tenant A's key). **Live-verified against the real running server**: a real `POST` returned a real `axm_live_...` secret and its masked twin; a fresh `GET` (separate request) showed only the masked shape; a real `DELETE` revoked it; a final fresh `GET` confirmed `revoked_at` persisted. Full suite: 213 passed. |
| **Settings screen (Phase 17, step 1 of 3 — Settings → Team → Billing)** | Real `frontend/src/app/(app)/settings/page.tsx` replacing the Phase 7 stub — 5 tabs (Workspace, Notifications, AI Model, Theme, API Keys), all wired to the real, already-live-verified Phase 15/16 endpoints (`GET/PATCH /settings/`, `GET/PATCH /auth/me`, `POST/GET/DELETE /api-keys/`). No backend/schema changes — pure frontend, per the locked Phase 17 spec. Tabs 1–3 and 5 hide their mutation controls (not just disable them) for non-Owner/Admin, reading `decodeUserFromToken().role` client-side to match the backend's real `require_role`/`GET /api-keys/` gates; the API Keys tab additionally never fires its list query at all for a non-Owner/Admin (`enabled: canManage` on the React Query hook), since that endpoint is itself Owner/Admin-only server-side, unlike `GET /settings/`. New shared `frontend/src/lib/theme.ts` (`applyTheme`/`getStoredTheme`/`resolveEffectiveTheme`) backs three call sites: the Settings > Theme tab (adds `"system"`, resolved via `matchMedia` for icon/label purposes only — `data-theme="system"` is a safe no-op value under `tokens.css`'s `:root:not([data-theme="light"])` dark rule, no CSS changes needed), `ThemeToggle.tsx` (now writes through to `PATCH /auth/me`, guarded on a token existing per this session's amendment, and now shows the actually-resolved icon instead of assuming `"light"` when unset — a small pre-existing correctness gap fixed while touching this code), and the `(app)` shell's mount effect (fetches `GET /auth/me` once and reconciles server theme over the localStorage pre-paint fast-path if they differ). The raw API-key secret lives only in a plain `useState` in `ApiKeysTab` (`revealedKey`) — never written into the React Query cache, never logged — reset to `null` on modal close; live-verified this is actually true (see next column), not just asserted by code structure. | **Live-verified end-to-end via two Playwright scripts against the real running server** (not synthetic): script 1 (owner) — Workspace name/timezone round-tripped through a real refresh (not an echo); **Notifications explicit-false round trip run exactly per this session's amendment**: confirmed ON → saved → confirmed OFF → saved → refreshed → confirmed still OFF, proving the backend's `notify_on` deep-merge genuinely persists an explicit `false` (independently double-checked via raw `curl` PATCH/GET before touching the UI at all); AI Model override set to `gemini-3.5-flash` and back to plan-default (`null`), both persisted through a refresh; Theme set to Dark via the Settings tab, confirmed applied immediately (`data-theme` set synchronously) and confirmed it survives a full page reload via the `(app)` shell's server-reconciliation fetch (not just localStorage); then toggled via the topbar `ThemeToggle` instead and confirmed the Settings tab reflects that change too (proving both write-through paths converge); API Keys — created a real key, confirmed the modal shows a real `axm_live_...` secret, confirmed via `JSON.stringify({...localStorage, ...sessionStorage})` that the raw secret is **not** present in either browser storage, confirmed it never resurfaces in the DOM after a reload (masked prefix only), then revoked it and confirmed the `Revoked` badge. Script 2 (viewer) — a second user registered fresh, downgraded to `role: "viewer"` via a direct DB write, then **re-logged-in via a real `POST /auth/login`** to obtain a fresh JWT actually carrying the new role claim (JWTs are static at issuance — the DB change alone doesn't retroactively affect an already-issued token, consistent with the existing no-revocation Gotcha); confirmed Workspace/Notifications/AI Model all show the read-only notice with disabled inputs and zero Save buttons; confirmed the Theme tab stays fully self-service (no gating, by design); confirmed the API Keys tab shows the restricted message and — captured via Playwright's network listener — that `GET /api-keys/` is **never fired at all** for a viewer, not fired-then-403'd. Both themes screenshotted, no contrast issues (dark-mode Settings correctly uses the established permanently-dark page-background pattern under a themed `Card`). `npx tsc --noEmit` clean. See Known-broken below for the AI Model tab's hardcoded-allowlist tech debt, called out explicitly per this session's amendment rather than left implicit. |
| **Team screen (Phase 17, step 2 of 3 — Settings → Team → Billing)** | Real `frontend/src/app/(app)/team/page.tsx` replacing the Phase 7 stub — member roster (name/email/role/status/joined) + pending-invites table, both wired to real Phase 15 endpoints (`GET /team/members`, `PATCH /members/{id}/role`, `DELETE /members/{id}`, `GET/POST/DELETE /team/invites`). No backend/schema changes. **A real gap in the original locked Phase 17 spec, found and closed in this session**: the spec covered invite *creation* on `/team` but never speced an invite *acceptance* UI — new public route `frontend/src/app/invite/accept/page.tsx` (outside the `(app)` group, no auth guard, mirrors the existing `AuthShell`/signup form pattern) calls the already-real `GET /team/invites/verify` (shows tenant name + role before commit) and `POST /team/invites/accept`, then `saveSession()`s and redirects to `/dashboard` — closes the loop the original spec left open. The invite-creation modal also surfaces the raw `invite_link_token` as a manual-share fallback (`/invite/accept?token=...`), since Resend's sandbox can only actually deliver to the account owner's own address. Role-change/removal controls mirror the backend's real guards **client-side, computed from data already on the page** (active-owner count derived from the fetched roster) rather than reimplementing new rules — disabled+tooltip, not hidden, for: self-removal (always blocked, any owner count), last-owner demotion, last-owner removal, and (Admin only) touching an Owner's role/removal. `decodeUserFromToken()` extended with a `sub` field (previously dropped) so the client can identify "is this row me" — additive, doesn't affect any other caller. | **Live-verified end-to-end via three Playwright scripts against the real running server**: script 1 (owner, single-owner tenant) — confirmed the sole owner's own row shows a disabled Remove button and disabled role select, each with the real blocking reason as its tooltip (`"You can't remove yourself"` / `"Cannot demote the last owner"`); created a real invite to the Resend sandbox's one deliverable address, **confirmed genuine delivery via Resend's own API** (`last_event: "delivered"`, same technique established in Phase 15) independent of the app's own "sent" response; **accepted the invite through a genuinely clean, separate, unauthenticated browser context** (not the owner's session) — the accept page correctly showed the real tenant name and invited role before any credentials were entered, and after submitting, the new member appeared in the owner's roster with the exact invited role and `is_active: true`, and the invite's status flipped to `accepted` — both confirmed via the rendered UI and a direct `GET /team/members` call. **2-owner guard coverage** (the direction the single-owner test can't reach): promoted the new member to Owner, confirmed the *original* owner's role-select became enabled again (guard correctly unblocks once a second owner exists) while their Remove button stayed disabled regardless (self-removal is unconditional, independent of owner count) — then demoted and removed the second member cleanly, both correctly allowed. **JWT-replay-after-removal (this session's amendment #2)**: captured the removed member's still-cryptographically-valid JWT *before* removing them, then replayed it against `GET /team/members` afterward — **result: HTTP 200, access persists.** `is_active` is not re-checked per request; the finding matches the already-documented no-revocation Gotcha and `remove_team_member`'s own docstring, not a new bug — see the extended Gotcha below. Script 3 (viewer) — confirmed via a real re-logged-in downgraded JWT that the Invite button, all role selects, all Remove buttons, and the entire Pending Invites section are absent (not just disabled), and that `GET /team/invites` is never fired at all for a viewer (network-captured); the member roster itself remains visible (correctly ungated server-side). Both themes screenshotted, no contrast issues. `npx tsc --noEmit` clean. |
| **Billing screen (Phase 17, step 3 of 3 — Settings → Team → Billing, phase complete)** | Real `frontend/src/app/(app)/billing/page.tsx` replacing the Phase 7 stub — plan card (current plan + Owner/Admin-gated upgrade/downgrade picker calling real `POST /change-plan`), 4 real usage bars (`GET /billing/usage`) colored by the backend's own `status` field (no client-side threshold math), a plainly-labeled "Coming soon" checkout/invoices section with zero fabricated data and zero API calls. Plan picker options come from the real `GET /billing/plans` (public), not hardcoded — unlike Settings' AI Model list, a real list endpoint exists here so there's nothing to keep in sync by hand. **A real, previously-undocumented backend gap found this session, not fixed reactively per the user's explicit instruction — see the new Known-broken row**: `change_plan()` allows any plan change unconditionally, including a downgrade that immediately puts current usage over the new plan's limits (no block, no warning from the backend at all) — confirmed live, not assumed. The UI reflects this honestly rather than pretending it's blocked: a non-blocking warning (computed client-side from `usage` + `plans` data already on the page, naming the specific over-limit resource(s)) appears under the plan picker when the selected target would be exceeded, but the Confirm button is never disabled by it — matches the real backend behavior instead of inventing a stricter client-side rule the backend doesn't enforce. | **Live-verified against the real running server, including the two amendments this session required**: (1) **Downgrade path** — seeded a fresh tenant on Growth with 4 real `DataSource` rows (via real `POST /sources/`, no mocks) and 1 real triggered pipeline run, then upgraded/downgraded through the real API: `POST /change-plan` to Starter (limit 3, usage 4) returned a real `200` — **the backend allows it unconditionally**, confirmed by direct `curl` before touching the UI; the resulting `GET /billing/usage` correctly showed `data_sources: {used: 4, limit: 3, percent: 133.3, status: "exceeded"}`; a subsequent real `POST /sources/` on the same tenant correctly `402`'d with `quota_exceeded` (existing usage grandfathered, new usage blocked). The UI's warning banner was confirmed to render with the exact affected resource name before the confirm click, and the Confirm click itself was exercised through the real UI (not just curl) and correctly persisted through a fresh reload. (2) **Viewer-token `POST /change-plan`** — a real re-logged-in downgraded-to-viewer JWT got a real `403 {"detail":"Requires role: owner or admin"}`, confirming no backend gap here (unlike the downgrade-path finding above); the UI also correctly renders no plan-picker/Confirm-button at all for that same viewer session. (3) **Real, non-zero usage bars** — verified against the tenant seeded in this same session (4 real sources, 1 real pipeline run), not an all-zero fresh tenant; after the real downgrade, the Data Sources bar was independently confirmed via raw DOM inspection to render `class="progress-fill danger"` (a Playwright locator bug in one intermediate check briefly suggested otherwise — resolved by dumping the actual rendered HTML directly, which showed the component was correct all along). (4) Stripe/no-payment-gating gap extended into Known-broken per explicit instruction, not left as only a Status Table mention. **Final tenant state, recorded per instruction rather than reverted**: the QA tenant used for this verification (`Billing QA Co ...`) was left on `starter` with `data_sources: 4/3 (exceeded)` — a real, intentionally-left artifact of the downgrade test, not cleaned up (matches this project's established practice of leaving verification dev-tenant data in place, e.g. "Verify35 Co" from Phase 15, "Quota Corp" tenants from earlier quota testing). Both themes screenshotted, no contrast issues (the exceeded/danger red bar reads clearly on the dark background). `npx tsc --noEmit` clean. **Phase 17 (Team, Billing, Settings UI) is now fully complete — all three screens built and live-verified.** |
| **AI Employees screen (Phase 18, step 1 of 2 — AI Employees → Landing page)** | Real `frontend/src/app/(app)/ai-employees/page.tsx` replacing the Phase 7 stub — 6 tiles (AXIOM, real/active, links to `/chat`; LEDGER/DEPLOY/INSIGHT/SENTINEL/PULSE, honestly locked) copied verbatim from the master design's `renderAIEmployees()` for name/role/description/capability tags. Zero API calls — nothing to call, this screen is pure static content, matching the phase table's "Backend prerequisite: None." **Three deliberate deviations from the literal design, decided in-session (not unilaterally)**: dropped "Join Waitlist" (wired to nothing real, no backend for it this phase — no `mailto:` fallback either, per instruction); dropped "Browse Marketplace" (implies a marketplace that doesn't exist); omitted per-tile pricing (`$59/mo` etc. were Phase 0 mockup placeholders, not committed pricing — showing a firm price for an unpurchasable product creates a commitment not actually made; **AI-employee pricing display is deferred until real pricing is set alongside the Stripe integration work**, see `FRONTEND_BUILD_PLAN.md`'s "Phase 18 note"). "1 Active" header badge kept — honestly true, AXIOM is real. **One real, small, isolated bug found and fixed in the same commit**: the locked-tile "Coming Soon" overlay's scrim was far too subtle (`rgba(0,0,0,0.04)`, essentially invisible) — underlying card text stayed fully legible and colorful, defeating the whole "locked" visual metaphor. Fixed with a new theme-aware `.locked-overlay` CSS class (`components.css`, ~72% opacity per theme, light/dark values chosen explicitly rather than one translucent color reused across both) rather than the ad-hoc inline `var(--overlay-scrim, ...)` fallback the first pass used. | **Live-verified via Playwright against the real running server, including the explicit rendered-text audit this session required as the standard for this page**: captured the full `body.innerText` and asserted none of `/mo`, `$`, "Join Waitlist", "Browse Marketplace", "waitlist", "marketplace", "customers", "users worldwide", "companies" appear anywhere — all passed, full captured text logged for review, not just spot-checked. Confirmed zero page-specific backend requests fire (only the pre-existing, unrelated `(app)` shell's own `GET /auth/me` theme-reconciliation call, already covered by Phase 17's own verification); confirmed all 6 tiles render with exactly 5 "Coming Soon" badges; confirmed "Open AXIOM →" navigates to the real `/chat` route. Both themes screenshotted before *and* after the scrim fix — the before-shot visually confirmed the bug (locked-tile text fully readable through the overlay), the after-shot confirmed the fix (clearly dimmed, badge legible, matches the "locked" metaphor in both themes). `npx tsc --noEmit` clean. **A real, pre-existing, app-wide finding surfaced by this session's stricter zero-console-errors verification standard, not introduced by this page**: a React hydration-mismatch warning fires on `data-theme="dark"` reload — confirmed also present on `/dashboard` and `/sources` (both long-shipped, previously-verified pages), so this is a characteristic of the root layout's theme pre-paint script, not an AI-Employees-specific bug. Not fixed in this session (cross-cutting, affects every page, not isolated to this build — see the new Gotcha below); logged rather than silently let through since prior phases' verification apparently never checked console output this strictly. |
| **Landing page (Phase 18, step 2 of 2 — phase complete)** | Real public landing page at `frontend/src/app/page.tsx`, replacing the Phase 2 component-showcase scaffold that had silently occupied root `/` since Phase 2 (the same stale page a user ran into mid-project — see Gotchas). No design mockup existed for this anywhere in the repo (confirmed by reading `design-proposal.html` in full — it's the Phase 0 palette/typography sign-off doc, not a landing page; `wunomo_architecture.html` is explicitly marked non-spec) — built fresh against the locked design system and real copy, proposed as complete plain-text copy and signed off before any code was written, per this session's explicit process. Positioning: platform-first ("hire AI employees," not AXIOM-first), hero sells the workforce vision, roster section proves it's real (AXIOM Active, 5 honestly Coming Soon — identical copy to `/ai-employees`, not restated, see the new shared `lib/employees.ts`/`components/shared/EmployeeCard.tsx` extraction below), AXIOM features section is the concrete proof point (4 blurbs, all grounded in real shipped Phases 1-17: pipelines, quality/incidents, chat tool-calling, roles/approvals). Social-proof section omitted entirely per explicit instruction (no fabricated testimonials/logos/user counts) — features flow straight into the final CTA. No screenshots this version (a QA tenant's exceeded-usage bars are "not hero material" — real screenshots deferred to deploy time against a production-shaped tenant). **Refactor done alongside this build, not just a copy-paste**: `EMPLOYEES` data and the tile-rendering JSX were extracted out of `/ai-employees` into shared `lib/employees.ts` + `components/shared/EmployeeCard.tsx` specifically so this page and `/ai-employees` structurally cannot drift apart on roster copy — `/ai-employees` itself was updated to consume the same shared module, not left duplicating the literal data. **Auth-aware CTAs** (nav: "Log in"/"Get Started" vs. "Go to Dashboard"; AXIOM roster tile: "Get Started with AXIOM" → `/signup` vs. "Open AXIOM →" → `/chat`) read the token client-side via a `useEffect`-only check (`useIsAuthed()`) — deliberately never touches the DOM synchronously pre-hydration, unlike the theme pre-paint script, specifically to avoid introducing a *second* hydration-mismatch bug on top of the one this page inherits from the root layout regardless (see Gotchas). **A real routing mistake caught by this session's own verification before it ever ran**: every CTA was first written pointing at `/register`, a route that doesn't exist in this codebase — the real route is `/signup`. Caught by checking the actual `app/` directory listing before running any live test, not by the test failing; fixed across all 5 occurrences before first execution. | **Live-verified via Playwright against the real running server, covering every item this session's verification standard named explicitly**: both themes (light/dark) × both desktop (1440px) and mobile (375px) viewports, all 4 screenshotted; a genuinely clean context (no `axiom_token` in `localStorage` at all, confirmed by direct read, not assumed) for the logged-out pass; every `<a>` link on the page enumerated and fetched directly (`page.request.get`), confirming both real hrefs (`/login`, `/signup`) return `<400`; the full rendered-text audit (same standard as AI Employees) asserted absence of pricing/waitlist/marketplace language *and* the explicitly forbidden positioning claims (`99.9%`, "SOC 2 certified", "GDPR compliant", "replace your employees", "save $", "% savings", "ROI", "guarantee") — all passed, full text logged for review; mobile viewport confirmed zero horizontal overflow; the auth-aware CTA was verified in both directions with a real registered user (logged out → "Get Started with AXIOM"/`/signup`; logged in → real "Open AXIOM →" that actually navigates to `/chat`). **Hydration-warning check** (explicitly required this session): confirmed the same already-documented app-wide root-layout warning reproduces here on a dark-theme reload — expected, not new, matches the Gotcha found during the AI Employees build; the auth-check hook's own `useEffect`-only design was specifically verified to add zero *additional* hydration warnings beyond that one inherited one. `npx tsc --noEmit` clean. **Deployment is explicitly out of scope for this session** — "done" here means built and verified locally only; real production deployment is a separate, tracked, user-side-gated step (domain purchase) — see Outstanding items and the new Not-yet-built entry for Privacy Policy/Terms of Service, both required before any real production signup traffic, not attempted this session. **Phase 18 (AI Employees + Landing page) is now fully complete.** |
| **Landing page rebuild — full marketing page, real logo, real screenshots, real About/Contact (Phase 19 polish)** | Supersedes the Phase 18 landing page's own scope (copy-only, no screenshots, no logo, no About/Contact sections — that row is kept above for history, not deleted). Real Wunomo logo files (user-supplied `wunomo logo.png` wordmark + `wunomo W logo.jpeg` mark, both navy-on-Pearl-Perfect) copied into `public/wunomo-wordmark.png`/`public/wunomo-mark.jpeg` — the mark appears in nav + footer (small, bordered/rounded "chip" treatment since the source images have an opaque cream background baked in, which reads intentionally as a badge in both themes rather than a broken-transparency rectangle), the wordmark gets one larger brand moment in the hero. Nav rebuilt with real section anchors (`#features`/`#about`/`#contact`, hidden under 860px — no hamburger, sections stay reachable via the footer's own copies of the same links), a light/dark `ThemeToggle` (reused as-is from the app shell — already guards its server write-through on a token existing, so it degrades gracefully logged-out), sticky+blurred backdrop. New `components/shared/ScreenshotCarousel.tsx` — real, dot-navigable, arrow-navigable, CSS-transform carousel (420ms, disabled entirely under `prefers-reduced-motion: reduce`) showing 4 real screenshots from a freshly-seeded demo tenant, never fabricated: Dashboard (real 100% health/2 active pipelines/100 quality score/4 seven-day runs/2 sources/0 open incidents), AXIOM chat (a real 2-message exchange with a real `list_data_sources` tool-call trace), Pipelines (2 real pipelines, both `active` with real `success` last-runs), Governance/Lineage (a real 6-node/4-edge graph auto-populated from the 2 sources+pipelines). New About (`#about`) and Contact (`#contact`) sections — About is the signed-off copy naming Alpha Parallel; Contact is two plain `mailto:` links (no fake-submitting form). Corrected the approved copy's own CTA target from `/register` (doesn't exist in this codebase) to the real `/signup` route, caught before writing any code by checking the real `app/` directory first — the identical class of mistake Phase 18's own build already documented and fixed once. Hero load-in animation (`landing-fade-up`, 420ms, staggered) is real but respects `prefers-reduced-motion` by being fully disabled (not just shortened) under that media query. | **Seeding is real, not synthetic**: a fresh "Wunomo Demo Co" tenant registered via the real `/auth/register`, 2 sources uploaded via the real `POST /uploads/register` (repo-root `employee_data.csv`/`sales_data.csv`), both profiled, 2 pipelines created/activated/triggered to real `success` runs, 1 real quality rule (`not_null` on `emp_id`) created and run to a real pass (`"0 null values in 'emp_id'"` — found and fixed a real seeding mistake along the way: `POST /quality/`'s column field is a top-level `column_name`, not nested under `rule_config.column` as first tried, which silently produced a no-op "No column specified — skipping" rule instead of an error), 1 incident logged and resolved, 1 real chat message that fell back to Groq (Gemini's per-minute free-tier cap, a tighter dimension than the already-documented 20/day cap, was actively 429ing during this session) and produced a real persisted tool-calling exchange, confirmed via `GET /chat/sessions`. Screenshots captured via Playwright against this real tenant, with Next.js's dev-mode `<nextjs-portal>` overlay (a closed-shadow-DOM custom element a plain injected stylesheet cannot reach into) hidden via direct DOM removal, not CSS — see the new Gotcha. **Full live verification, both themes + both viewports**: a dark-OS/no-`localStorage` visitor gets `data-theme="light"` (Part 1's fix re-confirmed still holding after this full rebuild, not just at the time it was first fixed); clicking the toggle applies dark immediately; `/login` remains unaffected (still falls through to OS preference); zero horizontal overflow at 390px; `prefers-reduced-motion: reduce` measured via `getComputedStyle` to have `animationDuration: "0s"`; every internal `<a href>` enumerated and fetched, all real hrefs return `200`; a full rendered-text honesty audit (pricing/waitlist/marketplace/social-proof/unverifiable-claim terms) found zero matches; zero `pageerror` console events. `npx tsc --noEmit` clean. Deployment remains out of scope, same as Phase 18. **Confirmed, per explicit follow-up question**: all 4 screenshots were captured with `localStorage.setItem("axiom_theme", "light")` set explicitly before navigating to each screen (not left to fall through to OS preference) — light-mode app UI on the light-default landing page, not a mismatched dark screenshot. **Maintenance note, not a defect**: these 4 PNGs (`public/landing/shot-*.png`) are a dated snapshot of the "Wunomo Demo Co" tenant's UI as of 2026-07-22/23 — they will silently go stale the moment any materially visible UI change lands on Dashboard, Chat, Pipelines, or Governance/Lineage (a redesigned card, a new KPI, a changed color) without anyone re-running the capture. Nothing re-generates them automatically. **If a future session materially changes the visual appearance of any of those 4 screens, re-capture the landing carousel screenshots in the same session** (same seeded-tenant-then-screenshot method, `public/landing/shot-*.png`, `nextjs-portal` hidden via the technique in the Gotcha below) rather than letting the landing page silently show an outdated version of the product. |
| **Migration-drift verification + fix (Phase 19, dedicated session)** | Verified `alembic upgrade head` against a genuinely empty database for the first time in this project's history — an isolated scratch Postgres 16 Docker container/network (`scratch_migration_pg`, `scratch_migration_net`), never the dev DB, torn down at the end of the session. Found and fixed 2 real gaps in the migration files: (1) `pipeline_commits`/`pipeline_deployments` (`models/cicd.py`) had no migration creating them at all — new `609092d6978f_add_cicd_pipeline_tables.py`, inserted between `7e3773979374` and `db3efa7d4d82` (whose `down_revision` now points at it), creates both tables plus the `cicdstatus`/`gatedecision`/`deploymentstatus` enum types via plain `sa.Enum(...)` (which auto-creates the type, unlike the ORM-level `create_type=False` declarations in `models/cicd.py` that assume it already exists). Column shapes match `models/cicd.py`'s current live state exactly, including `pipeline_deployments.pipeline_id` already `NOT NULL` from creation — making `db3efa7d4d82`'s own later `alter_column(..., nullable=False)` a harmless no-op rather than a second source of truth. (2) `team_invites` was missing its `ix_team_invites_created_at` index in `a4f7c92b1d05`, even though `models/all_models.py`'s `TeamInvite.created_at` has declared `index=True` since Phase 15 — found via an order-independent `information_schema` diff against a `create_all()`-built baseline (a raw `pg_dump` text diff wouldn't reliably catch this kind of gap since column/index ordering differences are expected noise). Also corrected (for clarity, not because it was live drift — see below) `db3efa7d4d82`'s misleading lowercase 3-value `incidentstatus` `CREATE TYPE` value list to match the model's real uppercase 4-value shape. **A related, deeper finding, not originally in scope but found while building the "known-good" comparison baseline**: `Base.metadata.create_all()` — this project's actual real running source of truth (see the Alembic Gotcha) — **also** cannot bootstrap a genuinely empty database, for the identical `create_type=False` reason a fresh Alembic run couldn't before this fix. No `create_enums.sql` or equivalent exists anywhere in the repo; whatever manual step long ago created `cicdstatus`/`gatedecision`/`deploymentstatus` in the real dev DB was never committed or documented anywhere. | **Definitive, order-independent proof, not a raw text diff**: built the alembic-upgraded schema on one empty scratch DB and a `create_all()`-built "known-good" schema (with the 3 enum types pre-created by hand, mimicking whatever undocumented manual step the real dev DB once had) on a second empty scratch DB, then compared `information_schema.tables`, `information_schema.columns` (name/type/nullable/length), `pg_indexes`, foreign keys (via `information_schema.table_constraints`/`key_column_usage`/`constraint_column_usage`/`referential_constraints`), primary keys, and `pg_enum` labels — every one of the 6 comparisons came back byte-identical across all 24 real tables (excluding Alembic's own `alembic_version` bookkeeping table, which correctly exists only on the alembic-built side). The only initial `pg_dump`-level differences (before the order-independent check) were `alembic_version` itself (expected) and two tables' column *ordering* (`llm_usage_events`, `users` — columns added later via `ALTER TABLE ADD COLUMN` land at the physical end in a migration-built table but appear in class-declaration order in a fresh `create_all()`-built one; same columns, same types, same nullability, zero functional difference, confirmed explicitly rather than assumed). This dev DB itself was never touched, restarted, or migrated — confirmed via `docker ps` before and after showing identical uptimes for every dev-stack container. **Same-day follow-up (user-requested): idempotency confirmed for the real deploy case.** A fresh scratch DB (`scratch_idem_pg`, its own isolated network, also never the dev DB) was run through `alembic upgrade head` twice in a row — the second run produced zero `"Running upgrade"` lines and exit code 0, a clean no-op, because Alembic's `alembic_version` tracking skips every already-applied revision without re-executing its SQL. Also tested (not the primary deploy scenario, but a real question worth answering) what happens if version tracking itself is lost against an already-fully-migrated schema (`DELETE FROM alembic_version`, then re-run): this does fail, with `DuplicateObject: type "approvalstatus" already exists` — but at the very first, oldest, pre-existing migration (`7e3773979374`), not the new CI/CD migration this session added. Every migration in the chain except `db3efa7d4d82`'s two `DO $$ ... EXCEPTION WHEN duplicate_object` -wrapped statements shares this same non-defensive property against replay, so the new migration introduces no new fragility relative to the rest of the chain — this is a pre-existing, chain-wide characteristic, not a regression. See the new Gotcha entries (idempotency + a sized, not-yet-built drift-detection CI proposal) for full detail. |
| **Public-launch risk cluster opened (2026-08-02) — sizing/blast-radius/order proposed and approved, execution started** | Five pre-launch risk items triaged in a docs-only planning pass, no code written until approved: (a) free LLM tiers can't serve real users, (b) `change_plan()`'s free-upgrade hole, (c) the unauthenticated WebSocket chat endpoint, (d) local-disk file uploads → Cloudflare R2, (e) nightly `pg_dump` + a tested restore. **Approved order: (c) → (b) → provision `wunomo.in`/`api.wunomo.in` infra → (e) → (a) → (d)** — (d) confirmed not launch-blocking since the target host is a plain VPS with a persistent Docker volume (would have reordered earlier if the target were a platform that doesn't persist local disk across deploys/restarts). **Real numbers pulled from actual `llm_usage_events` history** (read-only query against the real dev DB, no LLM calls made) to ground (a)'s eventual cost math: across 235 genuine `agent_chat` turns (excluding synthetic test-fixture rows, which skew wildly higher), average **~3,419 total tokens/turn** combined (Gemini: ~2,588 avg over 140 samples; Groq: ~4,643 avg over 95 samples — Groq runs notably higher input-token counts for the same task, worth knowing before assuming the two providers cost the same per turn). **Still owed, not yet done**: a live pull of current Gemini/Groq paid-tier pricing and the resulting monthly cost at 20 users × 5 turns/day using this real per-turn figure; and a deployment-checklist note to set a hard provider-console spend cap when paid keys go live, as an independent backstop alongside (b)'s app-layer fix. (c) is done — see its own new Status Table row. | Sizing/blast-radius facts for all 5 items gathered via direct code reads (`config.py`, `services/llm_service.py`, `services/billing_service.py`, `services/quota_service.py`, `api/v1/billing.py`, `main.py`'s WS route, `api/v1/uploads.py`, `modules/ingestion/connectors/file_connector.py`, both `docker-compose.yml` files) plus the live `llm_usage_events` query above — not guessed. |
| **(c) WebSocket chat auth — resolved by deletion** | See the now-`[RESOLVED]` "WebSocket chat auth" Known-broken row above for the finding and fix; this row is the verification record. `main.py`'s `/ws/chat/{tenant_id}/{session_id}` route (handler + the `WebSocket`/`WebSocketDisconnect` imports it alone needed) removed entirely, per explicit instruction to delete rather than harden once confirmed dead. | Grepped `frontend/src/**` for any WebSocket usage before writing any code — zero matches; grepped the whole backend — the only reference was the route's own definition, no test touched it either; independently corroborated by an already-on-record `USER_MANUAL.md` finding from an earlier audit session reaching the identical conclusion. **Live-verified against the real running server**: `docker compose restart backend` completed clean (`Application startup complete`, no startup errors); `GET /health` still returned `200`; `GET /ws/chat/faketenant/fakesession` now returns a plain `404 {"detail":"Not Found"}`, confirmed byte-identical to a deliberately nonexistent control path (not a different error shape that would suggest something else broke). Full backend suite re-run as a regression check: **255 passed**, zero failures — expected, since nothing real ever called this route. |
| **(b) change-plan free-upgrade hole — closed by disabling self-serve** | `POST /billing/change-plan` (`api/v1/billing.py`) now always returns `501` for any caller who passes `require_permission("billing.manage")` — Option 1 from the two proposed (disable, not build `payment_verified` architecture before Stripe is real), matching the existing `/checkout`/`/webhook` honest-501 pattern exactly. `BillingService.change_plan()` (`services/billing_service.py`) is untouched, still a real, working `Tenant.plan` write — its docstring now documents it as the sanctioned manual-provisioning path (call it directly, e.g. via `docker exec dataops_backend python -c "..."`, or write `Tenant.plan` via SQL) for onboarding a real paying customer before Stripe checkout/webhooks exist. Frontend (`frontend/src/app/(app)/billing/page.tsx`): the plan-select/confirm control and its `wouldExceed`-limit warning logic are removed entirely (not hidden-but-present), replaced with a plain "handled manually... contact us" message shown to every role, since nobody can self-serve a change now regardless of role; `changePlan()`/`getPlans()` removed from `lib/api.ts` as dead code (confirmed via grep — nothing else referenced either). | **Live-verified against the real running server, the exact bypass scenario the fix targets**: registered a fresh tenant ("Risk B Co," a real Owner), called `POST /change-plan` directly via `curl` (not through any UI) — real `501` with the honest message, and confirmed via both `GET /plan` and a direct `SELECT plan FROM tenants` that the plan genuinely never changed (still `starter`) — a true no-op, not a silent partial success. Demoted the same user to Viewer via direct DB write, logged in fresh, confirmed the identical call still correctly `403`s (the `require_permission` role gate fires before the disabled body, so a non-Owner/Admin's failure mode is unchanged from before this fix). **Administrative path confirmed working**: called `BillingService('<tenant_id>').change_plan('growth')` directly inside the running container, bypassing the disabled HTTP endpoint entirely — real `200`-equivalent result, confirmed persisted via both a fresh `GET /plan` and a direct DB query (`plan: "growth"`) — proving a real customer can still be manually provisioned onto a paid tier before Stripe exists. Backend regression tests rewritten (`test_billing_service.py`): the old "owner can change plan via endpoint" test replaced with one asserting the endpoint 501s even for an Owner and the tenant's plan is unchanged afterward; the old "endpoint rejects unknown plan with 400" test replaced with one confirming even a garbage plan name still just 501s (validation logic no longer runs at all); `test_viewer_cannot_change_plan` and `test_permission_matrix`'s `billing.manage` row both left unchanged and still pass, since 501 ≠ 403 doesn't disturb either's allow/deny assertions. Full backend suite: 255 passed. `npx tsc --noEmit` clean. |
| **`custom_sql` quality-rule arbitrary-SQL security fix (product-audit follow-up)** | Closed the Critical-severity finding from `docs/PRODUCT_AUDIT.md`: `QualityRuleEngine._run_rule_logic()`'s `custom_sql` branch (`modules/quality/rule_engine.py`) now routes through `SqlRunner.run_on_source()` instead of calling the raw, unsandboxed `connector.execute_sql()` directly. Only Postgres/MySQL sources are affected (the only two connectors that ever exposed `execute_sql()`); every other source type keeps its prior harmless "skipped — not a SQL source" behavior. | 3 new regression tests (`test_custom_sql_rule_safety.py`), run against the real live Postgres this test suite already talks to: a `DROP TABLE tenants` payload is rejected by `SqlRunner`'s keyword blocklist before any connection is even attempted, and the `tenants` table is confirmed to still exist afterward via a direct DB query — the actual proof, not just an assertion on the returned error message; a legitimate SELECT-based violation-counting query still executes for real and returns a real row count; a CSV source still skips harmlessly, preserving pre-fix behavior for non-SQL sources. Full backend suite: 258 passed. |
| **Agency fix 3/4: approval outcomes now reflected in the chat thread** | Closes `FRONTEND_BUILD_PLAN.md`'s P1 item 13. `GET /chat/sessions/{id}/history` used to return a blocked tool call's status exactly as written at turn time, forever — even after the real `ApprovalRequest` was later approved/rejected/executed on the Approvals screen, so a reopened conversation showed a permanently frozen "Needs approval" no matter what actually happened. New `_resolve_blocked_call_statuses()` (`api/v1/chat.py`) resolves each blocked call's *current* status at read time via a real (if coarse) join against `ApprovalRequest`, correlated by `(tenant_id, session_id, action_name)` in chronological order since `ApprovalRequest` doesn't store the LangChain `tool_call_id` — correct in the common case (no more than one concurrently-blocked call per action name per session); a precise join would need a new column, out of scope for this cheap, approved version. Maps the real `ApprovalStatus` enum to 4 new trace-status strings (`approval_approved`/`approval_rejected`/`approval_executed`/`approval_failed`), each carrying the real `approval_id` and, once available, the real `execution_result`. Frontend (`ToolCallBlock.tsx`, `lib/api.ts`'s `ChatToolCall` type): each new status renders its own distinct badge — critically, a rejected or failed action no longer falls through to the generic "Completed" badge, which would have said the opposite of what actually happened. | 3 new regression tests (`test_chat_approval_status_resolution.py`): a real chat message seeded with a blocked call plus a real `ApprovalRequest` later marked `EXECUTED` — history now shows `approval_executed` with the real `approval_id` and `execution_result`, not the frozen original; same for `REJECTED` → `approval_rejected`; a genuinely still-`PENDING` approval correctly still shows `blocked_pending_approval` (the fix doesn't invent an outcome that hasn't happened). All existing `test_chat_sessions.py` tests still pass unchanged. `npx tsc --noEmit` clean. Full backend suite: 268 passed. |
| **Agency fix 4/4: wired NotificationService's 3 dead presets into real trigger points** | Found via `docs/PRODUCT_AUDIT.md`'s code audit: `notify_incident()`/`notify_stale_sources()`/`notify_pipeline_failure()` all existed, worked, and were completely dead code — grepped, called from nowhere except their own definitions. The only live caller of `NotificationService` anywhere was the chat tool `send_alert`, meaning nothing automatic ever proactively told anyone about anything; AXIOM only ever notified when explicitly asked mid-conversation. Wired 3 real, one-shot trigger points: (1) `api/v1/incidents.py`'s `POST /incidents/` now calls `notify_incident()` after a real incident is logged (genuinely one-shot, no spam risk). (2) `services/tasks.py`'s `_execute_run()` failure branch now calls `notify_pipeline_failure()` (also one-shot per failed run). (3) `services/tasks.py`'s `_check_freshness()` now calls `notify_stale_sources()` — **but only for sources newly going stale**, not on every 15-minute tick: the already-known, accepted "freshness checker creates duplicate open incidents" gap (see that Known-broken row) means an ongoing-stale source gets a fresh `Incident` row every tick regardless; wiring the notification naively on top of that would have turned a low-severity, accepted DB-clutter issue into active Slack/email spam every 15 minutes. A tenant-scoped check for an already-open staleness incident for the same source gates the notification (not the incident creation, which is deliberately left untouched) so only the first tick a source goes stale actually notifies anyone. All 3 wire-ups are defensively wrapped — `NotificationService`'s own contract is "never raise," but a notification bug must never be able to break incident creation or fail a pipeline run's own error handling. | 4 new regression tests (`test_notification_wiring.py`), using the same real `urlopen`-spy technique already established in `test_notification_prefs.py` (trusting HTTP status alone is a known false signal): a real `POST /incidents/` genuinely calls the tenant's configured webhook; incident creation still succeeds even if the notification call itself raises; a real pipeline failure (via a direct `_execute_run()` call against a source that can't sync) genuinely notifies; the freshness anti-spam guard is proven directly — a source's first stale tick notifies once, a second tick for the same still-stale source notifies zero times, and the pre-existing duplicate-`Incident`-row behavior is confirmed still happening underneath (found live during test-writing: this test run shares its DB with the real, live `celery_beat`/`celery_worker` containers, whose own independent 15-minute freshness check can race in and create additional real duplicates for the same stale source — the test asserts `>= 2` incidents, not an exact count, to be robust to that real environmental overlap rather than asserting something that could be flaky for reasons that have nothing to do with the fix). Full backend suite: 272 passed. |
| Test suite (auth + smoke only) | `backend/tests/test_api.py`, 9 tests | health check, docs availability, 6 "requires auth → 401" checks, register/login round trip, one validation-rejection test |

### 🔴 Known-broken
| Area | What's broken | Evidence |
|---|---|---|
| **Settings UI's AI Model allowlist is hardcoded, not fetched — will silently drift from the backend allowlist** | `frontend/src/app/(app)/settings/page.tsx`'s `MODEL_OPTIONS` const hardcodes the same 2 model strings as `services/llm_service.py`'s `SUPPORTED_MODEL_OVERRIDES`, with a code comment pointing at that file as the source of truth — but there's no list endpoint, so nothing enforces the two stay in sync. If the backend allowlist ever gains or loses a model without a matching frontend edit, the UI will either offer a model the backend immediately 400s on save, or silently omit one the backend actually accepts. Flagged as a known, accepted gap per the locked Phase 17 spec's own explicit adjustment (hardcode now, no endpoint exists to fetch this from) — not a bug to fix reactively, but a real maintenance trap for whoever next edits `SUPPORTED_MODEL_OVERRIDES` without knowing this UI mirrors it by hand. | `frontend/src/app/(app)/settings/page.tsx`'s `MODEL_OPTIONS` vs `backend/services/llm_service.py`'s `SUPPORTED_MODEL_OVERRIDES` — two independent literals, no shared source, no test cross-checking them |
| **Gemini 3 tool-calling non-functional** — **[RESOLVED: LangChain v1.x upgrade session]** | Was blocked on `thought_signature` support, missing from `langchain-google-genai==2.1.4`. Upgraded the full ecosystem (`langchain` 1.3.14, `langchain-core` 1.4.9, `langgraph` 1.2.9, `langchain-google-genai` 3.2.0, `langchain-groq` 1.1.3, `langchain-community` 0.4.2) and live-verified a real multi-turn, multi-tool-call conversation on Gemini as primary with zero fallback to Groq. Two further real bugs surfaced and fixed in the same session — see the "LangChain v1.x upgrade" Status Table row and the new Gotchas: (1) `AIMessage.content` can now be a list of structured content blocks instead of a plain string, which crashed **all** Gemini chat (not just tool calls) on save to the VARCHAR `chat_messages.content` column; (2) dict/list-typed tool arguments arrive from Gemini via `langchain-google-genai` as JSON-encoded strings rather than native objects — root-caused to the library's own parsing layer (confirmed via a raw Gemini API call bypassing LangChain entirely, which returns a real nested object), not our schemas or Gemini's actual model output. Also switched primary model `gemini-3-flash-preview` → `gemini-3.5-flash` after hitting a hard 20/day free-tier cap on the preview model — see the new Gotcha. | See the "LangChain v1.x upgrade" Status Table row for the full live-verification transcript (3-turn, 5-tool-call conversation, zero Groq fallback). |
| **`incident_manager.py`'s `triage_model` field doesn't reflect which model actually served the request** | `IncidentManager.triage_incident()` hardcodes `"triage_model": "gemini-3-flash-preview"` in its result dict, regardless of whether the call actually succeeded on Gemini primary or fell back to Groq — `LLMService.complete()` (which this code calls) discards the model/provider info `invoke_llm()` already knows and logs via `log_llm_usage()`, returning only the raw string content. Found while updating this literal for the `gemini-2.0-flash`→`gemini-3-flash-preview` rename; pre-existing, not introduced by that change. Low blast radius (only caller of `LLMService.complete()` besides its own test) — fixing properly means changing `complete()`'s return shape, deferred rather than done reactively alongside an unrelated model-name fix. | `modules/observability/incident_manager.py` — the literal is static; `services/llm_service.py`'s `LLMService.complete()` returns `r.content` only, discarding the `model`/`provider`/`used_fallback` info `invoke_llm()` computes internally |
| **A malformed tool call — or any unhandled primary+fallback LLM failure — crashes `POST /chat/` with a raw 500, not a graceful message** | Found live during Phase 8 verification: Groq's `llama-3.3-70b-versatile` called `get_pipeline_run_history` without its required `pipeline_id` argument, and Groq's own tool-schema validation rejected it with a 400 `tool_use_failed` — this propagated as an unhandled exception all the way up through `run_agent()`/LangGraph to `POST /chat/`, which has no top-level exception handler for this case, returning FastAPI's generic `{"detail": "An unexpected error occurred..."}` with no `ChatMessage` persisted at all (clean failure, no corrupted state, but no graceful degradation either — a real user just sees a dead end). Distinct from the LLM-fallback mechanism (`_TimeoutFallbackChatModel`), which only handles the *primary* failing over to the *fallback* — this is the *fallback itself* producing a malformed request that neither provider nor our own code recovers from. **Still not fixed on the backend** — but Phase 10's chat UI now mitigates the symptom on the frontend: `chat/page.tsx`'s `send()` catches the thrown error and renders a clear inline message with the composer re-enabled, instead of a silent hang, live-verified against a second real trigger of this same failure shape (both LLM providers' daily quotas simultaneously exhausted, so primary *and* fallback both raised) — see the Phase 10 Status Table row. **This is a frontend mitigation, not a backend fix** — `POST /chat/` still has no top-level exception handler and still returns an unhandled 500 with no `ChatMessage` persisted. | Real chat request (`"How many pipelines do I have?"` and `"List my data sources."`, both against a fresh tenant with zero pipelines/sources) triggered this live; `docker compose logs backend` showed the raw `groq.BadRequestError`/`groq.RateLimitError` traceback, not a handled path. Re-triggered a second way in the Phase 10 session via a genuine Groq `RateLimitError` (429, TPD quota exhausted) with Gemini primary also exhausted (20/day) — identical unhandled-500 shape, confirming this isn't specific to malformed tool args, it's any unrecovered primary+fallback failure. **A third concrete trigger found and fixed ahead of YC demo prep** (this one *was* fixed, unlike the general gap this row tracks — see the new Status Table row): any tool returning a bare empty list (`list_open_incidents` with zero open incidents, `list_business_rules` with none configured) produced literal empty `ToolMessage` content, which Groq's API rejects outright — 100% reproducible, not intermittent. This is now closed for the empty-list case specifically; the general "no top-level exception handler on `POST /chat/`" gap remains open for any *other* unrecovered failure shape. |
| **`PolicyEngine.execute_approved_action()`'s `TOOL_REGISTRY` dispatch is broken for every single registered action — approving a blocked AXIOM action does not execute it** — **[RESOLVED: agency-work follow-up session, 2026-08-04]** | Was: every `TOOL_REGISTRY` entry (11 real entries, not 8 as originally counted — `profile_schema`, `sync_source`, `rerun_pipeline`, `backfill_pipeline`, `pause_pipeline`, `run_quality_check`, `modify_business_rule`, `resolve_incident`, `triage_incident`, `update_contract`, `generate_report`) mapped to a `(module_path, function_name)` tuple, resolved via a **module-level** `getattr(importlib.import_module(module_path), function_name)` — but every real target was an **instance method**, so `getattr()` returned `None` for all of them, `FAILED` every time. Two entries had a second, independent bug on top of that: `pause_pipeline`'s registered method name (`"pause"`) doesn't exist even as an instance method (the real one is `pause_pipeline`); `modify_business_rule` targeted `BusinessRules.update_rule`, which doesn't exist under any name at all — `BusinessRules` has no update/modify capability whatsoever, only `create_rule`/`list_rules`/`run_all`. `update_contract`'s target (`_noop`) does exist and is callable, but is a plain synchronous `def` while `execute_approved_action()` unconditionally `await`s its result — this raised `TypeError`, caught by the same generic handler, `FAILED` with a different message (an earlier draft of `docs/PRODUCT_AUDIT.md` incorrectly claimed this one silently reported `EXECUTED` — corrected in a follow-up commit once caught, see that file's own correction note). **Fixed**: `TOOL_REGISTRY` now carries a small dict per action (`module`, `class`, `method`, `constructor_args`) — `execute_approved_action()` instantiates the class with `tenant_id` (plus anything named in `constructor_args`, pulled from `action_args` — needed since `SchemaProfiler.__init__` requires `source_id` up front) before calling the real bound method with whatever of `action_args` remains. `pause_pipeline`'s method name corrected. `modify_business_rule` removed entirely (no real capability to dispatch to — a request for it now correctly surfaces "not registered" instead of pointing at a method that was never going to exist). `_noop` made genuinely `async def`. | **3 new end-to-end regression tests** (`test_approval_execution.py`), each creating real state, requesting approval via the real `PolicyEngine.create_request()`, approving it, then re-reading the DB directly (not just checking the API response) to confirm the real side effect happened: approving `pause_pipeline` genuinely flips a real `Pipeline.status` to `PAUSED`; approving `resolve_incident` genuinely flips a real `Incident.status` to `RESOLVED` with the real resolution notes persisted; a full HTTP round trip through the real `POST /approvals/{id}/approve` endpoint (not just a direct `PolicyEngine` call) genuinely pauses a real pipeline too. All 3 pass. This is the exact test class whose absence let this bug ship undetected for months — see `docs/PRODUCT_AUDIT.md`'s test-coverage section. |
| **WebSocket chat auth** — **[RESOLVED: public-launch risk cluster session, 2026-08-02 — deleted, not hardened]** | Was: `WS /ws/chat/{tenant_id}/{session_id}` (`main.py:141-181`) took `tenant_id` straight from the URL path with **no JWT verification** — anyone could connect as any tenant. Was the most severe issue in the codebase; Phase 19's unified permission spec had only partially mitigated it (hardcoded `caller_role="viewer"` on the endpoint's `run_agent()` call, narrowing what could be done via the hole without closing it). **Fixed by deletion, per explicit instruction, after confirming the route was genuinely dead**: grepped the entire frontend for any WebSocket usage and found none — independently corroborated by an already-on-record `USER_MANUAL.md` finding from an earlier session's audit (`grepped the entire frontend for any WebSocket usage and found none; this screen (and the whole app) only ever uses the REST POST /chat/ endpoint`). No test referenced it either. Removed the route handler and the now-unused `WebSocket`/`WebSocketDisconnect` imports from `main.py` entirely — smaller and safer than adding auth to a path nothing legitimate depends on. | Live-verified against the real running server: backend restarted clean (`Application startup complete`, no errors); `GET /health` still `200`; `GET /ws/chat/{anything}/{anything}` now returns a plain `404 {"detail":"Not Found"}`, confirmed byte-identical to a deliberately nonexistent control path, not just erroring differently. Full backend suite: 255 passed, zero regressions. |
| **Removed/demoted team members keep full access for up to the JWT TTL (60 minutes) after removal — `is_active` and `role` are never re-checked per-request** | **Severity: Medium** (not the WebSocket-auth-severity "anyone can be anyone," but a real access-control gap with a bounded, known, non-trivial window). `get_current_user()` (`api/v1/auth.py:24-29`) is a bare `jwt.decode()` — no DB lookup at all. `JWT_EXPIRE_MINUTES = 60` (`config.py:23`, confirmed live via `services/auth_service.py:72`'s `timedelta(minutes=settings.JWT_EXPIRE_MINUTES)`) means a member removed via `DELETE /team/members/{id}` (`is_active → False`) or demoted via `PATCH /members/{id}/role` keeps their **original** access — every endpoint, every role-gated action — for up to a full hour after an Owner/Admin believed the removal/demotion was immediate. **Live-confirmed, not theoretical**: a real JWT captured before a real soft-removal was replayed against `GET /team/members` afterward and returned `200`, not `401`/`403` (Phase 17 Team build). Only the *password* login path (`resolve_password_login()`) checks `is_active` at all, and only at the moment of a fresh login — Google/email-code login and every already-issued token are unaffected (see the separate, already-documented Gotcha for the login-path gap). **Fix directions** (not yet decided, both real options): (1) add a per-request `is_active`/`role` re-check against the DB in `get_current_user()` — correct and immediate, but turns every authed request into a DB round trip, a real cost across this whole API surface; (2) shorten `JWT_EXPIRE_MINUTES` significantly and/or add real refresh-token rotation — bounds the window without a DB hit on every request, but 60→something short changes UX (more frequent re-logins) and still isn't instant. A hybrid (short-lived access token + refresh, refresh checks `is_active`) is the more standard answer but is a real design change, not a one-line fix. **Candidate for Phase 19** (Polish) — flagging now rather than letting it hide as "expected behavior" in a Gotcha. | `api/v1/auth.py:24-29` (`get_current_user()`); `config.py:23` (`JWT_EXPIRE_MINUTES`); `services/team_service.py`'s `remove_team_member`/`change_member_role` (the DB writes that don't propagate); the extended "JWT tenant_id is trusted" Gotcha below (mechanism explanation); live reproduction in Phase 17's Team screen Status Table row |
| **`request_approval` tool is broken** — **[RESOLVED: LangChain v1.x upgrade session, 22-site tool punch list]** | Was: `governance_tools.request_approval` called `PolicyEngine(tenant_id).request_approval(...)`, but `PolicyEngine` has no such method — the real method is `create_request(...)`. **Fixed and live-verified** in the same session as the rest of the `governance_tools.py` punch list (see that Status Table row): redirected to `create_request()`, with `user_id`/`session_id` added to `agent_node`'s force-override loop since `create_request()` requires them. A real chat-triggered call created a real `ApprovalRequest` row, `user_id` cross-checked against the DB to match the real JWT `sub` claim. **Found stale during the Phase 19 manual-writing session** — the fix shipped sessions ago but this row was never updated; a pure documentation-bookkeeping correction, this is the third such stale row found (see the other two, above), which prompted adding Workflow Rule 9 below. | `agent/tools/governance_tools.py` vs `modules/governance/policy_engine.py`; the "22-site tool punch list — `governance_tools.py`" Status Table row for the live-verification detail |
| **CI/CD high-risk commits double-book their approval into two never-linked tables** — **[RESOLVED: Phase 15]** | Was: `services/cicd_tasks.py`'s `run_ci_pipeline` set the commit's `gate_decision = "pending_approval"` **and** separately created a `PolicyEngine` `ApprovalRequest` (`action_name: "cicd_pipeline_deployment"`) for the same event — the latter was never registered in `PolicyEngine.TOOL_REGISTRY`, so approving it via the general `/approvals/{id}/approve` path always failed execution, orphaned forever. **Fixed per the approved direction** (option A of the two originally proposed: stop creating the duplicate, not register-and-reconcile) — removed the `create_approval_request()` call and its now-dead function entirely from `cicd_tasks.py`; `PipelineCommit.gate_decision` (already the real, role-gated — see the RBAC retrofit row — tested, deploy-wired path) is now the only place this state lives. The read-side dedupe filter in `GET /approvals/merged` (added when this was first found, Phase 12) is left in place defensively — harmless no-op now that no new orphaned rows can be created, still correctly hides any pre-existing orphaned rows from before this fix. | 2 regression tests (`test_cicd_no_duplicate_approval.py`): a forced high-risk commit correctly reaches `gate_decision: "pending_approval"` with zero `ApprovalRequest` rows written for it; `GET /approvals/merged` shows the commit exactly once, under `cicd_deployment`, never `policy_engine`. **Live-verified against the real running server with a real, non-monkeypatched risk calculation** (not just the pytest suite): seeded a real commit with real risk-triggering content (`DROP TABLE` + 3 real SQL-complexity keywords + a "new" file, genuinely scoring 65/100 through the actual `calculate_risk_score()` logic — confirmed via the real `check_results` returned in the API response), ran the actual Celery task function directly against the live server, confirmed `gate_decision: "pending_approval"` was set; `GET /approvals/merged` returned exactly 1 entry (`source: "cicd_deployment"`, real risk_score 65); a direct `psql` query for `approval_requests` on that tenant returned **zero rows** — the double-write is genuinely gone, not just filtered on read. Full suite: 171 passed. |
| **In-graph approval gate doesn't persist to DB** — **[RESOLVED: it does, via a different layer than originally suspected — live-verified in the Phase 10 follow-up session]** | Originally: `approval_gate_node` in the LangGraph agent only mutates in-memory `pending_approvals` state — it never calls `PolicyEngine.create_request()`, so approvals surfaced mid-chat never appear in `GET /api/v1/approvals`. That's still true of `approval_gate_node` itself, but it's not the whole path: `api/v1/chat.py`'s `POST /chat/` handler reads `result["pending_approvals"]` after `run_agent()` returns and directly constructs a real `ApprovalRequest` row per blocked call (`chat.py:74-87`). **Live-verified, not just code-read**: sent a real chat message in `advisory` operation mode (blocks every tool call, the easiest reliable trigger) via a real Playwright-driven browser, and confirmed a real `ApprovalRequest` row appeared via both `GET /approvals` and `GET /approvals/merged` — correct `user_id`/`session_id`/`action_name`, `status: "pending"`. **This same verification pass found the row's `risk_level` was always hardcoded `"high"`** regardless of the action's real tier (`approval_gate_node` never attached a real `risk_level`/`reason` to the blocked call dict, so `chat.py`'s `pa.get("risk_level", "high")` always hit that default) — fixed in the same session (now threads `personality.get_risk_level()` through), re-verified live: the same call now correctly reports `"low"`. | `agent/dataops_agent.py`'s `approval_gate_node` (still no DB write of its own, but now attaches real `risk_level`/`reason` to each blocked call) vs `api/v1/chat.py:74-87` (the API layer's own `ApprovalRequest` creation — confirmed live, not just read) vs `models/all_models.py` (`ApprovalRequest.status` default) and `modules/governance/policy_engine.py` (`list_pending()`'s filter) |
| **`approval_executor.py` is dead/orphaned** | Defines `execute_tool_call()` but is never imported or called anywhere in the backend | Repo-wide grep, zero call sites outside its own definition |
| **Scheduled pipeline runs are argument-mismatched** — **[RESOLVED: Phase 19, P0 item 5 — and the arg mismatch turned out to be the smaller half of the real bug]** | Was: Celery beat fires `services.tasks.execute_pipeline_run` with `[pipeline_id, tenant_id]` (2 args) but the task signature requires `(run_id, pipeline_id, tenant_id)` (3 args). **Investigating this surfaced the real, bigger issue**: the dynamic per-pipeline scheduling mechanism this bug lived in (`Scheduler.register()`/`sync_all()`) was never called from anywhere in the running app, and even if it were, it mutates a `beat_schedule` dict that isn't shared across the `backend`/`celery_beat` process boundary — so scheduled pipelines never ran *at all*, arg-mismatch or not. Fixed by replacing the whole mechanism with a real, live static-polling beat task — see the new "Scheduled pipelines cluster" Status Table row for the full fix and live-verification detail (a real `* * * * *` schedule firing 3 real, correctly-timed runs one minute apart). | See the "Scheduled pipelines cluster (Phase 19, P0 item 5)" Status Table row. |
| **CI/CD deployment approve/reject has no role check** — **[RESOLVED: Phase 15]** | Was: `POST /cicd/commits/{id}/approve` and `/reject` enforced tenant scoping but, unlike `approvals.py`, did **not** check role — any authenticated tenant member could approve/reject production deployments. Fixed by retrofitting the new shared `require_role()` dependency (`Depends(require_role(Role.OWNER, Role.ADMIN))`), matching `approvals.py`'s existing precedent exactly. See the new "RBAC foundation" Status Table row. | `api/v1/cicd.py` — 2 new regression tests (`test_rbac.py`) plus live-verified against the real running server: a real downgraded-to-`viewer` JWT got a real 403 (`{"detail":"Requires role: owner or admin"}`) on a real pending commit, and the same commit's original `owner` token still approved it successfully (real deploy queued) immediately after — proving both the block and the no-regression case on the live server, not just the test suite. |
| **CI/CD webhook tenant spoofable if unconfigured** | `POST /cicd/webhook` is gated only by an optional HMAC signature (`GITHUB_WEBHOOK_SECRET`); if that env var is unset, the endpoint trusts a client-supplied `X-Tenant-ID` header/query param verbatim | `api/v1/cicd.py:130-132` |
| **`test_runner.py` (`QualityTestRunner`) is a no-op stub** | Every `_check_*` method unconditionally returns `"passed": True` without inspecting data. Not wired into the real quality-check path (`rule_engine.py`/`business_rules.py` are what's actually used) — but if anything ever calls `QualityTestRunner` expecting real checks, it will silently lie | `modules/quality/test_runner.py:37-53` |
| **`POST /billing/change-plan` executes real, unconditional plan changes with zero payment gating and zero usage-based blocking** — **[RESOLVED: public-launch risk cluster session, 2026-08-02 — closed by disabling self-serve entirely, Option 1, not by adding the usage-check/force-flag fix originally proposed]** | Was: two distinct gaps, both live-confirmed during Phase 17's Billing build — (1) no payment gate (any Owner/Admin could self-upgrade for free, real risk the moment paid LLM keys go live — see item (a) in the public-launch risk cluster); (2) no usage-based downgrade block (a downgrade could silently strand a tenant over-limit). **Both closed by the same fix, per explicit instruction to disable rather than build a `payment_verified`/usage-check architecture before Stripe exists**: `POST /change-plan` (`api/v1/billing.py`) now unconditionally returns `501` for every caller who passes the existing `require_permission("billing.manage")` role gate — matching the honest-stub pattern already established for `/checkout`/`/webhook`. This closes gap (1) directly (no plan change, free or otherwise, is reachable via the API at all) and moots gap (2) as a side effect (no self-serve downgrade path remains to strand anyone). `BillingService.change_plan()` itself is deliberately untouched and stays real — see the Status Table row below for the sanctioned manual-provisioning path this preserves. Frontend: the Billing screen's plan-select/confirm control is removed entirely (not just disabled) and replaced with honest copy ("handled manually... contact us"); `changePlan()`/`getPlans()` removed from `lib/api.ts` as now-dead code. | See the new "(b) change-plan free-upgrade hole — closed by disabling self-serve" Status Table row for the full live-verification detail (direct-curl bypass attempt as a real Owner, viewer-403 regression, and the administrative path proof). |
| **`update_contract` approval is a no-op** | `TOOL_REGISTRY["update_contract"]` maps to `_noop()`, which just echoes kwargs back — approving this action type does nothing real. **Updated, 2026-08-04**: as of the dispatch-mechanism fix (see the resolved `execute_approved_action()` row above), this now genuinely reaches `EXECUTED` status instead of crashing to `FAILED` (`_noop` was made properly `async def`) — but it's still a deliberate stub with no real "update a contract" capability behind it, so approving it still does nothing real. No contract-update capability exists anywhere in this codebase to wire it to; this remains a real, deliberate gap, not a bug. | `modules/governance/policy_engine.py` (`_noop`, `TOOL_REGISTRY["update_contract"]`) |
| **Migration/model drift on incident status enum** — **[RESOLVED: Phase 19 migration-drift session, 2026-08-02 — turned out to be dead code, not live drift]** | Was thought to be real drift (`db3efa7d4d82` creating `incidentstatus` as lowercase 3-value vs. the model's uppercase 4-value). **Live-verified against a genuinely empty scratch Postgres 16 database that this never actually manifests**: `7e3773979374` runs first in the chain and creates the correct, uppercase 4-value `incidentstatus` type via the `incidents` table's plain `sa.Enum(...)` column (which auto-creates the Postgres type); `db3efa7d4d82`'s own lowercase 3-value `CREATE TYPE` attempt then hits Postgres's `duplicate_object` exception (caught by its own `DO $$ ... EXCEPTION WHEN duplicate_object THEN null; END $$;` wrapper) and is silently discarded — the real type is never touched. Confirmed by inspecting `pg_enum` directly on the scratch DB after a full `alembic upgrade head` run: `incidentstatus` holds exactly `OPEN, INVESTIGATING, RESOLVED, SUPPRESSED`, matching the model. Fixed anyway for clarity, in the same commit as the real fixes below — the lowercase 3-value list was misleading to read even though inert, so it's now corrected to match reality with a comment explaining why the statement is a defensive no-op. | `migrations/versions/db3efa7d4d82_add_cicd_incidents.py` (both the fix and the explanatory comment); live-verified via `SELECT enumlabel FROM pg_enum ... WHERE typname='incidentstatus'` against a scratch DB built by a full `alembic upgrade head` run from empty |
| **Missing migrations for 2 of 3 CI/CD tables** — **[RESOLVED: Phase 19 migration-drift session, 2026-08-02]** | Was: `pipeline_commits` and `pipeline_deployments` (`models/cicd.py:76,103`) were never `CREATE TABLE`'d by any Alembic migration — only `cicd_incidents` was added by `db3efa7d4d82`, which assumes `pipeline_deployments` already exists. **Confirmed live, not just by inspection**: `alembic upgrade head` against a genuinely empty scratch Postgres 16 container died at `db3efa7d4d82` with `psycopg2.errors.UndefinedTable: relation "pipeline_deployments" does not exist`. Root cause went one level deeper than "missing migration," though: `models/cicd.py`'s own enum columns (`cicdstatus`/`gatedecision`/`deploymentstatus`) are declared with SQLAlchemy's `create_type=False` — meaning **no mechanism in this repo, including `Base.metadata.create_all()` itself**, can create those 3 Postgres enum types from nothing; confirmed by also running `create_all()` directly against a second empty scratch DB and hitting the identical class of failure (`UndefinedObjectError: type "cicdstatus" does not exist"`) until the 3 types were created by hand first. No `create_enums.sql` (or any other SQL file) exists anywhere in this repo — the real dev DB must have had this done as a real, one-off, never-committed manual step at some point in the past. **Fixed**: new migration `609092d6978f_add_cicd_pipeline_tables.py`, inserted between `7e3773979374` and `db3efa7d4d82` (which now depends on it instead of `7e3773979374` directly), creates all 3 enum types (via plain `sa.Enum(...)`, which *does* auto-create the type, unlike the `create_type=False` ORM-level declarations) plus both tables, matching `models/cicd.py`'s current live column shapes exactly. | Live-verified end-to-end: a full `alembic upgrade head` run from a genuinely empty scratch Postgres 16 container (isolated Docker network/container, never the dev DB) now completes cleanly through all 11 migrations with zero errors; the resulting schema was then diffed against a `create_all()`-built "known-good" baseline (built on a second empty scratch DB, with the 3 enum types pre-created by hand to simulate whatever manual step the real dev DB once had) via `information_schema` across tables/columns/indexes/foreign keys/primary keys/enum labels — byte-identical across all 24 tables, zero remaining diff. See the new "Migration-drift verification" Status Table row for the full methodology. |
| **`ENABLE_AUTONOMOUS_MODE` / `ENABLE_DESTRUCTIVE_ACTIONS` are dead config** | Defined in `config.py`/`.env.example`, never read anywhere else in the backend. Actual autonomy gating is entirely `personality.requires_approval()`'s risk table, independent of these flags | Repo-wide grep, zero call sites beyond definition |
| **`SYNC_DATABASE_URL` is dead config** | Declared in `config.py`/`.env.example`; only Alembic uses a sync engine, and it builds its own URL by string-replacing `DATABASE_URL`, not from this setting | `migrations/env.py:21-26` |
| **Phantom high-risk tools** — **[RESOLVED: agency-work follow-up session, 2026-08-04]** | Was: `personality.RISK_ACTIONS["high"]` listed `delete_records`, `drop_table`, `modify_schema`, `revoke_access` as tool names — none registered in `ALL_TOOLS`. The `"medium"`/`"low"` tiers had the same problem (`rerun_pipeline`, `modify_business_rule`, `update_contract`, `publish_output`, `run_quality_check`, `list_sources`, `generate_report` all matched nothing real either) — meaning `requires_approval()` silently never blocked most real tools in any operation mode, undermining the whole risk-gating premise the agency work depends on. **Fixed**: every name in `RISK_ACTIONS` now references a real, currently-registered tool. `"high"` is now genuinely empty — confirmed by reading every file in `agent/tools/*.py` that no delete/drop/revoke-capable AXIOM tool exists at all today; leaving it empty is an honest reflection of that, not an oversight. Every one of the 19 real tools that causes a state change is now explicitly listed in `"medium"` (previously several silently defaulted to `"low"` via the fallback, meaning Assisted mode's approval gate was skipping real mutations it was supposed to catch). | 4 new regression tests (`test_personality_risk_actions.py`): every `RISK_ACTIONS` name is a real tool; no tool appears in two tiers; every real tool is classified somewhere (none silently defaults); `"high"` is confirmed empty. `test_agent_graph.py`'s existing risk-level test updated to use the real tool name `run_pipeline` instead of the now-removed phantom `rerun_pipeline`. Full backend suite: 260 passed, 2 failed (`test_email_code_auth.py`'s rate-limit-cooldown tests) — confirmed **pre-existing and unrelated**, not a regression from this fix: both fail identically across two separate full-suite runs but pass cleanly 11/11 when `test_email_code_auth.py` is run in isolation after clearing Redis's `otp:*` keys, matching the already-documented "test suite hits whatever DB/Redis state carries over between runs" Gotcha exactly. |
| **Duplicate/drifted docker-compose files** | `dataops-agent/docker-compose.yml` and `dataops-agent/backend/docker-compose.yml` both define the full stack with different container names (`dataops_backend` vs `dataops-backend`) and different health-check strategies (root version has no Postgres/Redis healthchecks, backend version does) | Both files read in full |
| **Freshness checker creates duplicate open incidents** | `_check_freshness()` in `services/tasks.py` (runs every 15 min via Celery beat) creates a brand-new `Incident` row every time a source is still stale — it never checks for an already-open incident about the same condition first. A source stuck stale for hours accumulates one near-duplicate "Stale data: X (N.Nh overdue)" incident per 15-minute tick. Observed live: 7 near-duplicate open incidents for one source. Not fixed — flagging only | `services/tasks.py` `_check_freshness()`, unconditional `db.add(incident)` inside the `if hours_since > sla_hours:` branch, no dedup query against existing open incidents for the same `affected_assets` |
| **CSV connector's `sync()` return key doesn't match what `_execute_run` reads** — **[RESOLVED: ahead of YC demo prep]** | Was: the file connector's `sync()` returned row count under a `"rows"` key, but `_execute_run()` in `services/tasks.py` reads `sync_result.get("total_rows", 0)` — so `rows_processed` on a real, successfully-synced CSV/Excel/JSON pipeline run always showed `0` even though the sync genuinely worked. Fixed by renaming the key to `total_rows` in `file_connector.py`, matching every sibling connector's already-existing convention. See the new Status Table row for the live-verification detail. | `modules/ingestion/connectors/file_connector.py`'s `sync()`; live-verified via the demo seed script showing real non-zero `rows_processed` after the fix |
| **Celery beat has dead commented-out config** | A second `cicd-post-deploy-monitor` beat entry (300s interval) is commented out with a stray inconsistent comment | `services/celery_app.py:46-49` |
| **`login()` picks an arbitrary tenant-account on email collision** — **[RESOLVED: Phase 3/6 — row was stale, fix shipped and was live-verified, just never marked here]** | Was: `POST /auth/login` had no tenant scoping on its email lookup, silently authenticating into an arbitrary tenant-account on collision. **This was fixed and live-verified during Phase 3/6** — `resolve_password_login()` (`services/auth_service.py`) checks the password against every matching row across tenants; exactly one match logs in as before, more than one returns a real workspace-disambiguation list with no token yet, matching the fix direction this row originally specified. Live-verified via Phase 6's UI click-through (two real API-created tenants sharing an email → UI login → workspace picker renders both → picking one completes login) and covered by regression tests. **Found stale during Phase 19's triage** — the fix shipped sessions ago but this row was never updated; a pure documentation-bookkeeping correction, not a new code change. | `services/auth_service.py`'s `resolve_password_login()`; Phase 6 Status Table row for the live UI verification |
| **`register()` never checks for an existing email before creating a new tenant** — **[RESOLVED: Phase 3/6 — row was stale, fix shipped and was live-verified, just never marked here]** | Was: `POST /auth/register` unconditionally created a new tenant on every call, no warning on email reuse. **This was fixed and live-verified during Phase 3/6** — `register()` now runs the non-blocking existing-workspace nudge (a real confirm step requiring a click to proceed, not an auto-redirect), matching the fix direction this row originally specified. Live-verified via Phase 6's UI click-through (a real duplicate-email signup showing the nudge banner, including its own "Continue" click). **Found stale during Phase 19's triage** — same situation as the row above, doc bookkeeping only. | `api/v1/auth.py`'s `register()`; Phase 6 Status Table row for the live UI verification |
| **22 `agent/tools/*.py` call sites referenced nonexistent classes/methods** — **[RESOLVED: LangChain v1.x upgrade session]** | All 6 files fixed, file-by-file, each in its own commit with its own live verification — see the 6 "22-site tool punch list — `<file>.py`" Status Table rows above for exactly what changed in each and how it was verified. Not every fix was a pure rename: `governance_tools.py`'s contract tools needed a new shared `contract_service.py` (logic that never existed on `LineageTracker`, extracted from inline REST-endpoint code); `quality_tools.py`'s `validate_business_rule` and `transformation_tools.py`'s `generate_sql_transform`/`run_python_transform` needed real signature redesigns, not just renames, since their original contracts didn't match anything the target classes actually support; `reporting_tools.py`'s `export_dataset` and `transformation_tools.py`'s `standardize_dataset` have no real equivalent anywhere in the codebase and now return honest not-implemented errors instead of crashing or lying. Two bugs found one layer deeper than the tool call sites themselves, both flagged separately (not fixed in this pass): `BusinessRules.create_rule()` can't create any of its own 8 supported rule types (validates against the wrong type list); `services/cicd_tasks.py` still double-books CI/CD approvals into two tables (pre-existing, unrelated to this sweep). | See the 6 per-file Status Table rows for the individual verification methodology and evidence — every site was either live-verified through a real chat tool call or through direct `.ainvoke()` invocation against real seeded tenant data (the latter used when both LLM providers' daily quotas were exhausted mid-session — see Gotchas), never against a mock. Full regression suite: 102 passed after the final file. |
| **`BusinessRules.list_rules()` can't iterate its own engine's return shape** — **[RESOLVED: LangChain v1.x upgrade session, 22-site punch list]** | Fixed by unwrapping `QualityRuleEngine.list_rules()`'s real `{"rules": [...], "count": N}` shape before iterating, instead of iterating the dict directly (which iterated its *keys*). See the "22-site tool punch list — `quality_tools.py`" Status Table row. **A second, deeper, separate bug was found while writing the regression test for this one and is NOT fixed**: `BusinessRules.create_rule()` delegates to `QualityRuleEngine.create_rule()`, which validates `rule_type` against its own base 8 types (`not_null`/`unique`/.../`custom_sql`) — never against `BUSINESS_RULE_TYPES` (`reconciliation`/`kpi_sanity`/`cross_source_count`/`freshness_sla`/`column_sum_match`/`kpi_trend`/`pipeline_success_rate`/`data_volume_growth`). Every real business-rule type is unconditionally rejected as "Unknown rule_type" — `BusinessRules.create_rule()` cannot successfully create a single business rule of any of its own 8 supported types today. Out of scope for this pass (a `modules/quality/` bug, not an `agent/tools/*.py` call-site bug); the regression test for the `list_rules()` fix creates its test rule directly via `QualityRuleEngine` with a valid base type, manually tagged `_business_rule: True`, to isolate it from this separate bug. | 3 regression tests in `test_business_rules.py`. Live-verified via direct tool invocation: `validate_business_rule`/`list_business_rules` both run cleanly against real (empty) data without crashing — the crash this row originally documented is gone. The `create_rule` type-validation bug reproduced live while writing the test (`{'error': "Unknown rule_type 'pipeline_success_rate'. Valid: ['not_null', 'unique', ...]"}`), confirming it's real and current. |
| **`custom_sql` quality rules executed arbitrary, unsandboxed SQL against a tenant's real connected database** — **[RESOLVED: product-audit follow-up session, 2026-08-04]** — **Severity: Critical (security).** | Found via `docs/PRODUCT_AUDIT.md`'s code audit, not previously documented anywhere. `QualityRuleEngine._run_rule_logic()`'s `custom_sql` branch called `connector.execute_sql(sql)` directly — the raw Postgres/MySQL connector method with **zero** keyword blocking, zero SELECT-only restriction, using the tenant's real stored sync credentials. Reachable by any role except Viewer (`quality.manage`) via REST or the `create_quality_rule` chat tool, and — critically — **re-executed automatically on every real pipeline run** via `_execute_run()`'s quality-check step and the 60s scheduled-pipeline poller, with zero further human interaction after the rule was created once. A rule like `rule_config: {"sql": "DROP TABLE users; --"}` (plus any single-character `column_name`, a structural quirk of the code's own early-exit ordering, not a real barrier) would genuinely drop the table the next time that pipeline ran. **Fixed**: routed through `SqlRunner.run_on_source()` instead — the same keyword-blocklisted, SELECT/WITH-only, row-capped, audit-logged safety layer `execute_sql_transform` already uses. `custom_sql` rules are meant to return a count of violation rows, which is exactly a SELECT, so no legitimate capability is lost. Non-Postgres/MySQL sources still harmlessly skip, matching prior behavior. | 3 new regression tests (`test_custom_sql_rule_safety.py`): a real `DROP TABLE tenants` payload is rejected before ever reaching the connection, with the `tenants` table confirmed to still exist afterward via a direct DB query (the real proof, not just an assertion on the returned message); a legitimate SELECT-based violation-counting query still executes and returns a real result; a non-SQL (CSV) source still skips harmlessly. Full backend suite: 258 passed. |
| **`services/cicd_tasks.py:run_schema_check()` has always silently no-op'd** — **Severity: Medium.** | Found via `docs/PRODUCT_AUDIT.md`'s code audit. Imports `from models.schema_snapshot import SchemaSnapshot` — **no `models/schema_snapshot.py` file exists anywhere in this repo**, confirmed via a repo-wide search. Every call raises `ImportError`, caught by the function's own `except ImportError: return {"status": "skipped", ..., "drift_detected": False}`. Every CI/CD risk score ever computed has excluded schema-drift detection entirely, silently, with no error surfaced anywhere. Not fixed this session — flagging only. | `services/cicd_tasks.py` (`run_schema_check()`); confirmed via `find`/grep that `models/schema_snapshot.py` does not exist |
| **CI/CD's post-deploy auto-rollback monitor has no live trigger** — **Severity: Medium-High** (the safety net a real bad deploy would depend on doesn't exist in practice). | Found via `docs/PRODUCT_AUDIT.md`'s code audit. `services/cicd_monitor.py`'s rollback decision depends on `PipelineDeployment.post_deploy_run_count`/`post_deploy_failure_count`, which are only ever incremented by `POST /cicd/deployments/{id}/record-run` (`api/v1/cicd.py:340`) — whose own docstring says it's "called by the pipeline executor after each run post-deployment." The real pipeline executor (`services/tasks.py:_execute_run()`) **never calls this endpoint or an equivalent internal function**. Every real deployment sits at `run_count=0, failure_count=0` forever; the monitoring-window check never fires; `monitoring_active` stays `True` indefinitely with no data ever arriving. The auto-rollback logic itself (`_trigger_rollback`) is only ever exercised by tests calling the endpoint directly, never by a real pipeline run. Not fixed this session — flagging only. | `services/cicd_monitor.py`, `api/v1/cicd.py:340`, `services/tasks.py:_execute_run()` (no call site found via grep) |
| **`contract_service.py:validate_contract()` reads `schema_snapshot` as a flat shape that never occurs in practice** — **Severity: Medium** (a real, live-shipping bug in a governance feature, currently untested against the case that triggers it). | Found via `docs/PRODUCT_AUDIT.md`'s code audit. Reads `live_schema.get("columns", [])` and `live_schema.get("row_count")` directly off `DataSource.schema_snapshot`, assuming a flat `{"columns": [...], "row_count": N}` shape — but `SchemaProfiler` always nests the schema per table name (`{"main": {...}}` for files, `{table_name: {...}}` for DB sources), confirmed by reading `schema_profiler.py`. **The identical bug class already found and fixed in `TransformGenerator._resolve_schema()`** (see that Status Table row) — the fix there iterates `for table_name, table_info in snapshot.items()`; `contract_service.py` reads from the same field and was never given the equivalent fix. Practical effect: validating a contract against any genuinely profiled source will report every expected column as missing (`schema.column_missing`) and never fire the `min_row_count` check. The one existing validation test deliberately uses an unprofiled source, so it only exercises the `schema.no_snapshot` branch and never reaches this bug. Not fixed this session — flagging only. | `modules/governance/contract_service.py:87` (`validate_contract()`) vs `modules/ingestion/schema_profiler.py`'s real nested shape; `tests/test_contract_service.py` (no test exercises a profiled source) |
| **`BusinessRules.create_rule()` cannot create any of its 8 supported business-rule types — given its own dedicated row, not just a mention inside the `list_rules()` fix above** — **Severity: Medium** (an entire advertised feature — business rules — is uncreatable through its own intended API). | See the fuller technical detail in the `list_rules()` Known-broken row directly above — this row exists so the bug has its own discoverable entry with an explicit severity, per the product-audit follow-up session. `create_rule()` delegates to `QualityRuleEngine.create_rule()`, which validates against the wrong 8-item type list. Not fixed this session. | `modules/quality/business_rules.py:64`; `tests/test_business_rules.py`'s `_create_business_rule_directly()` helper, whose own docstring documents this as a known, unfixed workaround |
| **No CI pipeline exists — every test in this project runs only when a person remembers to run it manually** — **Severity: High** (structural — this is the root cause behind how many of the bugs on this page went undiscovered for months). | Found via `docs/PRODUCT_AUDIT.md`'s code audit. Searched the entire repo for `.github/workflows` or any equivalent — none exists (the only `.github` directories found are inside `frontend/node_modules/**`, third-party vendor files). No automated test runner, no lint gate, no build check triggered by a push or PR, for either the 258-test backend suite or the frontend. Not fixed this session — flagging only, this is a process/infrastructure gap, not a one-line code fix. | Repo-wide search for `.github/workflows`, `.gitlab-ci.yml`, or equivalent — none found outside vendored `node_modules` |
| **Zero automated frontend tests exist — every "Playwright-verified" claim in this file describes a one-off script, run once, never committed** — **Severity: High** (same root-cause class as the missing CI pipeline above — nothing re-verifies any historical frontend claim today). | Found via `docs/PRODUCT_AUDIT.md`'s code audit. `frontend/package.json` has no test framework in `dependencies`/`devDependencies` (no jest/vitest/`@playwright/test`/cypress/testing-library) and no `test` script. A repo-wide search for `*.test.ts(x)`, `*.spec.ts(x)`, `__tests__/`, `e2e/`, `playwright.config.*` under `frontend/` returned zero results. Concretely already caused stale documentation: this audit pass independently found the Command Palette's keyboard navigation doesn't exist (despite its own "↵" hint implying it does) and that Billing's plan-picker UI was removed without CLAUDE.md's Phase 17 narrative ever being updated — both are exactly the kind of regression a real test suite would have caught immediately. Not fixed this session — flagging only, this is a process/infrastructure gap. | `frontend/package.json` (no test framework, no `test` script); repo-wide search for test file patterns — zero results |

| **Sources UI's CSV/Excel connection-config placeholder used the wrong key — `"path"` instead of the real `"file_path"`, silently breaking any source created by typing the UI's own example** — **[RESOLVED: Phase 19 pre-walkthrough]** | Found live while seeding a Phase 19 walkthrough tenant: registered a CSV source with `connection_config: {"path": "employee_data.csv"}` (copying the exact shape `frontend/src/app/(app)/sources/page.tsx`'s own `SOURCE_TYPES` placeholder suggests), then called `POST /sources/{id}/profile` and got `[Errno 2] No such file or directory: ''`. Root-caused by reading the real connector, not guessing: `FileConnector.__init__` (`modules/ingestion/connectors/file_connector.py`) reads `config.get("file_path", "")` — its own docstring literally documents the required shape as `{file_path, sheet_name?, orient?}` — so a `path` key silently misses, defaults to empty string, and every subsequent operation (preview/sync/profile) fails with a path-resolution error. This is not a backend bug (the connector is correct and consistent with its own docs) — it's a real, user-facing frontend bug: **both** the `csv` and `excel` entries in `SOURCE_TYPES`'s placeholder text used the wrong key, so any real user typing the UI's own suggested example verbatim would register a source that can never load. Fixed: both placeholders corrected to `{"file_path": "..."}`. | `frontend/src/app/(app)/sources/page.tsx`'s `SOURCE_TYPES` (`csv`/`excel` placeholders) vs `modules/ingestion/connectors/file_connector.py`'s own docstring and `__init__`. **Live-verified**: re-seeded the walkthrough tenant's 2 sources via the real, correct path (`POST /uploads/register`, which already used `file_path` correctly) instead of the broken direct-path flow — both sources profiled successfully with real schemas (7/8 and 7/10 columns/rows), a real pipeline run against the corrected source produced a genuine quality-check result (`"0 null values in 'name'"`, not the earlier `"No data to check"`/`"No source attached"` failures), and the Data Catalog correctly shows both sources with real profiled schemas. |

| **"New Chat" button race condition (Phase 19 polish)** | `frontend/src/app/(app)/chat/page.tsx`'s `newChat()` cleared UI state (messages, activeSessionId, draft) but never reset `sending`, and `send()` had no guard against a stale response. Two real, reproducible failure modes: (1) clicking "+ New Chat" while a previous message was still in flight left the composer disabled (still gated on the never-reset `sending` flag) — indistinguishable from the button doing nothing, which is exactly what was reported; (2) if that stale request later resolved successfully, its handler (`if (!activeSessionId) setActiveSessionId(res.session_id)`) would fire against the *new*, activeSessionId-null thread, silently re-attaching the old session_id — so a message the user believed they were sending in a fresh conversation could land in the old one. Fixed with a `chatGenerationRef` counter bumped by both `newChat()` and `selectSession()` (session-switching had the identical hazard); `send()` captures the generation at call time and checks it before applying its response/error/`finally` state, so a stale result is silently dropped instead of clobbering whatever thread is now on screen. `newChat()` also now explicitly resets `sending`/`provider` and pushes a real toast ("Started a new chat." / "Already a new chat." for the already-empty case), closing the original missing-feedback finding too. | Live-reproduced the original bug via Playwright before fixing: screenshotted the composer showing "AXIOM is still thinking… (15s)" immediately after a New Chat click, and captured the actual `POST /chat/` request bodies proving a second message sent after New Chat inherited the first (stale) request's `session_id`. **Live-verified after the fix**, 3 scripts: composer is enabled and the empty-state renders immediately after New Chat even while racing an in-flight send (0ms gap between Send and New Chat); the stale request's response never lands in the new thread (0 bubbles 4s later); a message sent immediately after New Chat gets a genuinely fresh `session_id` (`null` in the request, same as the first message of a new session) rather than the old one. Also fixed the identical hazard in `selectSession()` (switching threads mid-send) as part of the same commit, since it shares the same root cause. |

### ⚪ Not yet built
- Real anomaly-detection and daily-report Celery tasks (`services/tasks.py` — both explicitly commented `"""Placeholder — full implementation in Phase 3."""`).
- Any test coverage for: `agent/` (LangGraph graph, tools, personality gating), `modules/*` (all 6 module packages), `services/` (Celery tasks, LLM service, CI/CD services). Current suite (9 tests) only covers auth plumbing and a couple of smoke checks.
- **Role gate expansion** — `require_role()` (Phase 15) is only retrofitted onto the high-priority endpoint list approved for this phase (CI/CD approve/reject — done; `DELETE pipelines/sources/quality-rules`, `run/sql`/`run/pandas`, `cicd incidents/resolve` — in progress). The medium/low-priority items in `FRONTEND_BUILD_PLAN.md`'s role-check audit (pipeline trigger/pause/schedule, source sync/profile, incident create/resolve, governance contract/lineage writes, quality rule create/run, uploads, analytics KPI writes) remain open to any authenticated tenant member regardless of role — deliberately deferred, not an oversight, since every tenant in this DB currently has exactly 1 real member and gating them now has no live benefit yet real retrofitting cost. **Revisit once real multi-user tenants exist** (i.e. once Phase 15's invite flow actually gets used to add a second real member to a real tenant) — that's the point at which an under-gated medium-priority endpoint stops being theoretical.
- Any tenant-scoping enforcement mechanism beyond "the developer remembered to add the filter."
- Token revocation/refresh for JWTs (no `iat`/`nbf`, no blocklist).
- **API keys as real, request-authenticating credentials** — Phase 16 built the full CRUD lifecycle (`ApiKey` table, generate/hash/store, masked list, idempotent revoke, all live-verified), but nothing in this app currently accepts one as a credential. This is a genuinely separate, phase-sized piece of future work, not a footnote to the CRUD build: it needs a new auth dependency (`get_current_user_or_api_key()` or similar) wired into every protected endpoint's resolution path, a decision on key-scoped permissions (does a key inherit its creator's full role, or get its own narrower scope?), key-scoped rate limiting, and `last_used_at` actually being written somewhere (the column exists, nothing populates it yet). **When Phase 17 builds the Settings UI for these keys, the copy must not imply they can authenticate anything today** — describe what they're actually for right now, or explicitly label the capability as "coming soon," rather than shipping UI that oversells a CRUD-only feature.
- **Privacy Policy and Terms of Service pages** — the landing page (Phase 18) links to real `/login`/`/signup`, but nothing in this app has a Privacy Policy or Terms of Service page, and neither auth flow requires accepting one before creating a real account. **Grouped with the domain-purchase/production-launch cluster** (see Outstanding items) — required before any real production signup traffic, not before this session's local build-and-verify work. Explicitly in scope for Phase 19's launch-readiness partition (see `FRONTEND_BUILD_PLAN.md`'s "Phase 19 note": every open item like this must be triaged into fix-before-launch vs. consciously-accepted, not silently carried forward).

---

## 3. Known Gotchas

*This section only grows. Append; never delete. If a gotcha turns out to be fixed, mark it `[RESOLVED: <how/when>]` rather than removing it.*

- **Two docker-compose.yml files, not one.** `dataops-agent/docker-compose.yml` (root, referenced by the README quick-start) and `dataops-agent/backend/docker-compose.yml` both stand up the full stack independently, with different container/network names and different Postgres/Redis healthcheck behavior. Running both simultaneously will port-collide on 8000/5432/6379. Always confirm which one a command/doc is referring to before running `docker compose` commands.
- **The frontend service is commented out in the root compose file.** `docker compose up` never starts a frontend even after the missing files are restored — you must uncomment it in `dataops-agent/docker-compose.yml` first.
- **`.gitignore` in this repo only covers Python.** `[RESOLVED: initial commit]` — a Node/Next.js block (`node_modules/`, `.next/`, `out/`, `.vercel`, `.turbo/`, etc.) was added before the first commit, and the ~11.5k already-staged `node_modules`/`.next` files were unstaged. Still double-check `git status` after any broad `git add -A`/`git add .` inside `dataops-agent/frontend/` in case new build output reappears.
- **JWT tenant_id is trusted, never re-validated against the DB — and this applies to every claim in the token, not just tenant_id, including `role`.** `get_current_user()` (`api/v1/auth.py`) is a bare `jwt.decode()` with no DB lookup at all — no `is_active` check, no fresh `role` read, nothing. If you ever add a "deactivate tenant," "remove user from tenant," or "change a user's role" feature, remember that a previously-issued JWT for that user stays valid, **with its original claims**, until it naturally expires — there's no revocation and no per-request re-check. Concretely: `PATCH /team/members/{id}/role` updates the DB row, but the affected user's *existing* session keeps operating under their *old* role (both for `require_role()` gates and for whatever the frontend decodes client-side via `decodeUserFromToken()`) until they get a fresh token — either by naturally re-logging-in, or by the token expiring and them being forced to. Same for `DELETE /team/members/{id}`'s soft-removal (`is_active = False`): the removed member's still-valid JWT continues to be accepted by every authed endpoint, unchanged, until expiry — **confirmed live during Phase 17's Team build** (a JWT captured immediately before removal was replayed against `GET /team/members` afterward and returned a real `200`). The only path that's actually real-time is a *fresh* login attempt, since `resolve_password_login()` filters `is_active` at that specific moment (see the separate, already-documented Gotcha that this still isn't true for Google/email-code login). **When building any UI around role/removal, don't imply the effect is immediate for an already-open session** — it's immediate for the DB and for anyone who logs in fresh afterward, not for a session already in flight. **This is now tracked as a real fix item, not just documented behavior — see the new "Removed/demoted members keep access for up to the JWT TTL" Known-broken row below** (severity, exact TTL, and fix directions).
- **Tenant scoping is a per-query convention, not a framework guarantee.** Every new query against a tenant-owned table must manually add `.where(Model.tenant_id == tenant_id)`. There is no base repository/query class to inherit this from. When reviewing or writing any new DB query in this codebase, explicitly check for the tenant filter — it is the single easiest way to introduce a cross-tenant data leak here.
- **Two different LLM fallback mechanisms coexist.** `services/llm_service.py` has both `get_llm_for_agent()` (LangChain `.with_fallbacks()`, used by the actual agent graph) and `invoke_llm()` (manual tenacity-retry + manual fallback, apparently unused by the graph). Don't assume changing one changes the other's behavior.
- **Ingestion connectors expose a raw `execute_sql()` that bypasses `SqlRunner`'s safety layer.** `SqlRunner` (the safe, keyword-blocked, parameterized path) is one specific caller; `postgres_connector.py` and `mysql_connector.py` both independently expose `execute_sql()` methods that run arbitrary SQL with no keyword filtering. If you add a new caller, go through `SqlRunner`, not the connector directly, unless you've deliberately re-derived the same protections.
- **`python_runner.py`'s in-process `exec()` sandbox is intentionally not airtight.** It AST-allowlists imports/names and uses a restricted builtins namespace + thread timeout, but the code's own docstring says this is "sufficient" only for already-LLM-validated code, not untrusted input, and recommends a real microVM (gVisor/Firecracker) for anything stronger. Don't expose this path to raw user-submitted Python without adding real process isolation first.
- **Debug/patch scripts in `backend/` root are leftover hotfixes, not part of the app.** `diagnose.py`, `fix_analytics.py`, `fix_analytics2.py`, `patch_analytics.py` were run manually inside the container (they hardcode the `/app/...` container path) to patch a `NoneType has no attribute 'value'` crash in `/api/v1/analytics/pipelines` caused by NULL pipeline-status rows in a groupby. The underlying fix should already be reflected in `api/v1/analytics.py` itself — if you're debugging analytics and considering writing a new one-off patch script, first check whether `analytics.py` already has the fix these scripts describe, and prefer fixing it in place over adding another patch script to the pile.
- **Test suite hits whatever DB `APP_ENV=test` resolves to — there's no isolated test DB or per-test reset — and the same is now true of Redis for anything rate-limited.** `conftest.py` builds an `AsyncClient` against the real `main.app` with no fixture-level DB setup/teardown. Running tests repeatedly can accumulate rows; use a unique/random email per test (most tests already do — `test_register_and_login` didn't, and a hardcoded email accumulating duplicate tenants across repeated runs is exactly what made login()'s disambiguation fix start (correctly) failing that test — fixed by giving it a unique email like everything else). The email-code auth system's Redis-backed rate limiters (`services/auth_service.py`) have the identical problem in a new form: the per-IP counter (`otp:ip:{ip}`) is shared across every test run in a session, real curl testing, and the pytest ASGI test client alike, since none of them get a distinct IP and Redis state isn't reset between invocations. Heavy manual live-testing against `/auth/email-code/request` right before running `pytest` can leave the per-IP counter primed near/over its cap, causing an unrelated later test run to fail with `rate_limited` for reasons that have nothing to do with that run's own test logic. If an email-code test fails this way, check `redis-cli KEYS "otp:*"` before assuming the code is broken — `redis-cli --scan --pattern "otp:*" | xargs redis-cli DEL` clears it (safe, it's all short-TTL test/dev state). Don't assume test runs are hermetic, for either store.
- **`RISK_ACTIONS["high"]` names tools that don't exist** (`delete_records`, `drop_table`, `modify_schema`, `revoke_access`). If you're asked to "make destructive actions require approval," check whether the actual mutating tool (e.g. `execute_sql_transform`, `resolve_incident`) is even in that risk table before assuming the gate already covers it — right now, most real mutating tools default to whatever risk tier `get_risk_level()` falls back to, not `"high"`.
- **Approvals created mid-chat (via the LangGraph `approval_gate_node`) do not show up in the `/api/v1/approvals` queue.** Only approvals created through `PolicyEngine.create_request()` (e.g. via `seed_approval.py` or a correctly-written tool) persist to the DB. If a user reports "the agent said it needs approval but I don't see it in the approvals list," this is why — check whether the code path that created it actually called `create_request()`.
- **Celery beat's scheduled pipeline-run task call looks arg-mismatched** — **[RESOLVED: Phase 19, P0 item 5]** — was a symptom of a bigger, now-fixed issue (the whole dynamic per-pipeline scheduling design was unreachable, not just arg-mismatched). Replaced with a real, live, static-polling mechanism (`check_scheduled_pipelines()`), live-verified against a real `* * * * *` schedule. See the Status Table row.
- **Mutating `celery_app.conf.beat_schedule` from the `backend` (FastAPI) process has zero effect on the `celery_beat` process's actual running schedule — they don't share memory, and Celery's default `PersistentScheduler` isn't a shared/cross-process store.** Found while fixing the scheduled-pipelines cluster: `modules/orchestration/scheduler.py`'s `Scheduler.register()`/`sync_all()` were designed to inject dynamic per-pipeline entries into exactly this dict at runtime, from whichever process calls them — but a call from an API request handler (running in `backend`) can only ever mutate `backend`'s own private copy, never the separate `celery_beat` container's in-memory schedule. **If a future feature needs truly dynamic (not just DB-polled) per-item Celery beat scheduling, it needs a genuinely shared store — RedBeat (Redis-backed) or `django-celery-beat` (DB-backed) — not a bare mutation of `conf.beat_schedule` from a different process than the one running beat.** The pattern that *does* work across this process boundary, used for the actual fix: a static beat entry (defined once in `celery_app.py`, present in every process from boot) that polls the database directly on each tick — exactly how `check_all_freshness()` and the new `check_scheduled_pipelines()` both work.
- **`celery_worker`/`celery_beat` don't hot-reload code changes, same as `backend` (see the existing `docker compose restart backend` Gotcha) — restart both explicitly after editing anything in `services/tasks.py`, `services/celery_app.py`, or any module they import.** Cost real time while verifying the scheduled-pipelines fix: both containers had been running for hours before the new task/beat-entry code was written, and `docker compose restart celery_beat celery_worker` was required before the new `check-scheduled-pipelines-1min` entry or `check_scheduled_pipelines()` task were even registered — a live poll against the running server showed nothing happening until both were restarted.
- **A `RuntimeError: ... got Future ... attached to a different loop` / `Exception closing connection <AdaptedConnection ...>` warning in Celery worker logs around some task boundaries is a pre-existing, already-present quirk of this codebase's `asyncio.run()`-per-task + pooled-asyncpg-connections pattern — confirmed benign, not a new regression, before assuming otherwise.** Found while live-verifying the scheduled-pipelines fix: both the new `check_scheduled_pipelines()`/`execute_scheduled_pipeline_run()` tasks *and* the untouched, pre-existing `execute_pipeline_run()` task show the identical error signature in the logs (confirmed by grepping worker logs for both task names) — so it predates this session's changes and isn't something the new code introduced. Every task observed still completed successfully with correct DB state (`Task ... succeeded`, and the real `PipelineRun` rows this warning surrounds were confirmed correct via a live API poll) despite the warning appearing. Root cause is the same class of issue `engine.dispose()` (called at the top of every task, already present before this session) exists to mitigate — a stale asyncpg connection object from a previous `asyncio.run()` call's now-closed event loop getting garbage-collected during a *later* call's loop, which logs a cleanup error but doesn't propagate a real failure. **Not fixed — out of scope for this session** since it predates the changes made here and doesn't affect correctness; flagging so a future session doesn't mistake this cosmetic noise for a new bug when touching Celery task code.
- **`RunTracker` is a pass-through, not a separate system.** It exists only "for agent tool compatibility" (per its own comment) and just delegates to `DAGManager`. Don't look for run-tracking logic there — it's in `dag_manager.py`.
- **There is no real DAG.** Despite the name `DAGManager` and the module path `modules/orchestration/`, pipeline execution is a fixed linear sequence (sync → quality check → done), not a graph with dependency resolution. If a task asks you to add a new pipeline step, you're inserting into a linear chain in `tasks.py`'s `_execute_run`, not registering a DAG node.
- **Any LangGraph node you add to `agent/dataops_agent.py`'s `StateGraph` must return only the new/delta value for reducer-annotated channels, never the full accumulated state.** `AgentState.messages` uses `Annotated[Sequence[BaseMessage], operator.add]`, so LangGraph automatically appends whatever a node returns onto the existing value. Returning `state["messages"] + [new]` (instead of just `[new]`) — which every custom node in this file did until the fix — makes the reducer concatenate old+new onto the already-accumulated state, doubling history on every node hop (a 3-message conversation reached 63 messages in 3 turns). `ToolNode` (the one prebuilt node here) has always done this correctly; match its pattern for any new custom node.
- **Any new tool exposed to the agent must never accept `tenant_id` (or `user_id`/`session_id`) as an LLM-suppliable argument that isn't also force-corrected server-side.** Tool schemas require these as function-call arguments the LLM itself generates, but the LLM has no reliable way to know the real value — it will hallucinate a placeholder unless told, and can still get it wrong even when told. `agent_node` in `dataops_agent.py` now force-overwrites `tenant_id` on every outgoing tool call to the real, server-derived value; if you add a tool that takes `tenant_id` (or add `user_id`/`session_id` as tool args for the first time), extend that same override loop rather than trusting the LLM's argument. All tools in `ALL_TOOLS` now take `tenant_id` this way — `get_cicd_status` was the last holdout and is fixed (see Status Table).
- **`FALLBACK_LLM_MODEL` must be a model capable of reliable tool-calling.** `llama-3.1-8b-instant` on Groq fails real tool-calling requests outright with a `tool_use_failed` 400 error (malformed function-call generation) — it only ever looked "fine" in testing that didn't exercise tool calls. Keep this set to `llama-3.3-70b-versatile` (or an equally capable model) — `config.py`'s own default already documents this; don't let `.env` drift back to a cheaper/faster model without re-testing an actual tool-invoking chat message live.
- **No agent tool should ever call back into this app's own REST API over HTTP.** Tools should call the same service-layer functions the REST endpoints call, in-process — that's the pattern every tool follows (`modules/*` classes, or plain functions in `services/*.py` like `cicd_service.py`). An HTTP round-trip back into your own API (`get_cicd_status` did this until fixed — see Archive) is a sign a tool was stubbed out before the shared service layer existed, and it silently loses auth/tenant context in the process: `httpx` doesn't raise on a 4xx by default, so a failed/unauthenticated self-call can return empty defaults (`{}`, `0`, `"unknown"`) instead of an error. Treat any tool whose failure mode is unusually generic zero/empty defaults as a signal to check whether it's swallowing a failed call this way.
- **"Verified live" only proves the exact code path that specific test exercised — write regression tests against the real model/schema, not assumptions about column names.** `get_pipeline_for_repo()` referenced `Pipeline.is_active`/`Pipeline.source_config`, neither of which exist (real columns: `status`, `pipeline_config`) — it would have raised `AttributeError` on its very first real call, forever, since the function was written. There was no mock or fixture to blame (no automated test existed for it at all); the original fix was verified manually/live, and that manual pass most likely exercised a different code path (setting `pipeline_config` at pipeline-creation time, which has always used the correct field name) than the one that was actually broken (the function itself, plus the update path). When you add a regression test for a DB-touching fix, prefer hitting the real model via a real session (see `test_cicd_service.py`, `test_pipeline_run_status.py`) over a hand-rolled mock — a mock can only be wrong about the schema in the same way the code under test is already wrong, silently agreeing with itself.
- **The primary→fallback LLM handoff has a hard 15-second ceiling (`PRIMARY_LLM_TIMEOUT_SECONDS` in `services/llm_service.py`) — don't remove it or "simplify" back to bare `.with_fallbacks()`.** A failing Gemini call doesn't fail fast on its own: google-api-core's gRPC retry layer honors the server's suggested `retry_delay` (observed 40s+), and `langchain_google_genai`'s own hardcoded tenacity retry (`max_retries=2`, backoff up to 60s) sits on top of that — neither is configurable from outside, and `ChatGoogleGenerativeAI`'s `timeout` constructor field is dead code in the installed version (never wired to the actual request — verified by grepping the library source for `self.timeout`, zero hits). Measured live: 2m22s before this fix, 14.7s after. If you're tempted to raise/remove the 15s ceiling because "it might cut off a slow-but-working Gemini call," Groq's own responses have consistently landed in the 2-12s range in testing, so 15s already has margin — don't just trust a config field on the primary model to bound this instead, confirm it actually works with a live forced-failure test first (it didn't, here).
- **`get_agent()`'s module-level `_cache` dict in `agent/dataops_agent.py` is keyed only by `"{personality}:{operation}"` — this will break once per-tenant AI model selection ships (`FRONTEND_BUILD_PLAN.md` Phase 16).** — **[RESOLVED: Phase 16]** The cached value is a fully-built agent object with a specific primary/fallback LLM already bound in. If tenants can choose their own model, the SAME cache key today would serve tenant A's cached agent (built with tenant A's model choice) to tenant B on a matching personality/operation combo — silently leaking one tenant's model preference into another tenant's requests. **Fixed exactly as planned here**: `get_agent()` now requires `tenant_id` (raises `ValueError` if omitted — can't silently regress back to the old key by a caller forgetting to pass it) and the cache key is `f"{tenant_id}:{personality}:{operation}:{primary_model or 'default'}"`. `run_agent()` resolves the tenant's real, DB-stored, allowlist-validated model override (`services/settings_service.get_ai_model_override()`) before calling `get_agent()`. **Live-verified exactly per this Gotcha's own instruction**: two real tenants, one with `ai_model_override: "llama-3.3-70b-versatile"` and one with no override, made real back-to-back chat requests — `llm_usage_events` confirmed tenant A got `provider: "groq", model: "llama-3.3-70b-versatile", used_fallback: false` (a genuine primary call on its own override, not an accidental fallback) and tenant B got `provider: "gemini", model: "gemini-3.5-flash"` — no cross-tenant leak. See the "get_agent() cache re-keying + AI model override" Status Table row for full detail.
- **All Python enums in `models/all_models.py` use UPPERCASE member names with lowercase string values** (e.g. `RunStatus.SUCCESS = "success"`, `PipelineStatus.ACTIVE = "active"`, `IncidentStatus.OPEN = "open"`) — the opposite convention from `models/cicd.py`'s enums (`CICDStatus`, `GateDecision`, `DeploymentStatus`, and cicd.py's own separate, unused `IncidentStatus`/`IncidentSeverity`), which use lowercase members. This mismatch was independently miswritten as lowercase-member access (`RunStatus.success`, `PipelineStatus.active`, `IncidentStatus.open/investigating/resolved`) in **6 different files** — `incident_manager.py`, `monitor.py`, `anomaly_detector.py`, `scheduler.py`, `business_rules.py`, `report_generator.py` — all fixed and live-verified this session (see Status Table), but the fact that it recurred independently 6 times strongly suggests it's an easy mistake to make here specifically (lowercase values, easy to assume lowercase members too). **When writing or reviewing any new comparison against `RunStatus`/`PipelineStatus`/`IncidentStatus`/`IncidentSeverity` from `all_models.py`, double check the member name is uppercase** — and if you're touching `models/cicd.py`'s enums instead, remember those really are lowercase, so don't "fix" them to uppercase by pattern-matching this gotcha.
- **A Status Table "How verified" cell that only restates what the code claims to do, instead of citing a specific live call or regression test, means it probably wasn't actually verified.** The entire enum-case bug cluster above hid behind exactly this pattern — Observability's and Reporting's rows both read like proof ("real DB-aggregate freshness/health metrics", "real LLM-powered incident triage", "read in full, no stubs") but named no live test, because there wasn't one; every code path they described raised `AttributeError` on its very first real call, silently swallowed into `{"error": ...}`. Phase 4's customer-simulation pass gave real, live, end-to-end coverage to the CI/CD incident system (webhook → risk-check → approval → deploy) but never touched the general DataOps `Incident`/`Monitor`/`AnomalyDetector`/`ReportGenerator` code paths — two different "incident" systems exist in this codebase (`models.all_models.Incident` vs `models.cicd.CICDIncident`), and only one got exercised. When reading this Status Table (or writing a new row into it), treat "How verified" text that doesn't name a specific test file, live request, or reproducible command as a claim to re-check, not a fact to build on.
- **`agent/tools/*.py` was written against an imagined API surface for `modules/*` that was never cross-checked against the real implementation — treat any tool you haven't personally live-verified as unverified, regardless of what the Status Table says elsewhere.** Evidence this is one process-level root cause, not scattered noise: the wrong class name `RuleEngine` (real: `QualityRuleEngine`) was independently guessed the same way in two unrelated files (`business_rules.py` and `quality_tools.py`); the wrong method names across the board are consistently plausible-sounding-but-never-real (`.query()`, `.detect()`, `.send()`, `.standardize()`, `.status_report()`) — the signature of code written from a tool's docstring/intent rather than from reading the target class. It manifests in at least 4 distinct mechanical shapes with no single fix: wrong class name, wrong method name, wrong enum-member case (the cluster fixed this session), and return-shape mismatches (e.g. `business_rules.py`'s `list_rules()` bug) — plus at least one outright missing feature (`LineageTracker` has no contract methods despite `governance_tools.py` calling `.create_contract()`/`.validate_contract()`). See the new Known-broken row ("22 more `agent/tools/*.py` call sites...") for the full punch list; two static sweeps (module-import check, call-site-existence check) are exhaustive for the wrong-name flavors, so that list doesn't need re-discovering — but isn't exhaustive for return-shape or missing-feature bugs, which need per-site review or live testing to catch.
- **This dev DB's schema was never actually created by Alembic — `alembic_version` doesn't even exist.** `main.py` calls `Base.metadata.create_all()` on backend startup (line ~28), and that's what has been creating every table this whole project, migrations included — Alembic's own migration chain (`7e3773979374` → `db3efa7d4d82` → `fe6e4b941fce`) has never actually been applied to this database. Running `alembic upgrade head` from this state fails immediately (`DuplicateObject: type "approvalstatus" already exists`) because it tries to replay the full migration history against objects `create_all()` already created. **Don't run `alembic upgrade head` against this dev DB without first checking `SELECT * FROM alembic_version` — if that table doesn't exist, the fix for a brand-new table is to restart the backend container (triggers `create_all()`, which only creates missing tables/indexes — no migration run needed).** This also explains and generalizes the already-documented "Missing migrations for 2 of 3 CI/CD tables" Known-broken entry — it's not that 2 tables specifically are missing migrations, it's that Alembic isn't the real source of truth for schema in this environment at all. Still write the migration file for every new model (it's correct, reviewable, and will matter for a real production deploy where `create_all()` presumably isn't what runs) — just don't expect `alembic upgrade head` to be how it actually lands here. **For a migration that alters an *existing* table (new column, nullable change, etc.), `create_all()` can't help — it only creates missing tables, never alters ones that already exist.** Phase 5's `User` migration (nullable `hashed_password`, new `google_id`/`email_verified`) needed this for real, and the fix is straightforward: verify the DB's actual current schema matches the migration chain's expected state at the previous head (spot-check a table or two), then `alembic stamp <previous_head_revision>` (marks that revision current without running anything — safe, since `create_all()` already produced an equivalent schema) followed by a normal `alembic upgrade head`, which now only has to apply the one genuinely new migration. Worked cleanly here; this is the real, durable fix for the underlying drift, not a one-off workaround — repeat it for any future migration that alters existing tables rather than just adding new ones. **Update, Phase 19 migration-drift session (2026-08-02) — the migration *files* are now verified to build a correct, complete schema from empty; this dev DB's own state is unchanged and still not what this paragraph describes fixing.** `alembic upgrade head` now runs cleanly start-to-finish against a genuinely empty database — verified on an isolated scratch Postgres 16 container, never this dev DB, which was not touched, restarted, or migrated by that session. Two real gaps were found and fixed in the migration files themselves: `pipeline_commits`/`pipeline_deployments` had no migration creating them at all (new `609092d6978f_add_cicd_pipeline_tables.py`), and `team_invites` was missing its `ix_team_invites_created_at` index (fixed in `a4f7c92b1d05`) — see both now-resolved Known-broken rows and the new "Migration-drift verification" Status Table row for the full methodology and proof (an `information_schema` diff against a `create_all()`-built baseline, byte-identical across all 24 tables). **The significant finding, stated plainly: neither of this project's two schema-creation mechanisms — not Alembic, not `create_all()` — could bootstrap a genuinely empty database, for the same root cause.** Both assumed the 3 CI/CD enum types (`cicdstatus`/`gatedecision`/`deploymentstatus`, `models/cicd.py`, all declared `create_type=False`) already existed rather than creating them. `create_all()` never creates a `create_type=False` type itself (that's what the flag means); the pre-existing Alembic chain never created them either (see the now-resolved "Missing migrations for 2 of 3 CI/CD tables" Known-broken row). Confirmed live: running the exact `create_all()` call `main.py`'s lifespan hook makes, against a genuinely empty scratch DB, failed with `UndefinedObjectError: type "cicdstatus" does not exist` until those 3 types were created by hand first — the identical failure class as the fresh `alembic upgrade head` run. No `create_enums.sql` or equivalent exists anywhere in this repo, in any commit, ever. **The practical consequence: this project was effectively unbuildable from a truly empty database by any means this repo could reproduce, and nothing before this session would have told you that** — the real dev DB has been running this whole time only because someone, at some unrecorded point, ran 3 `CREATE TYPE` statements by hand and never wrote them down anywhere; every fresh clone, new environment, or disaster-recovery restore was one `docker volume rm` away from being unable to start the app at all, silently, until whoever hit it spent real time debugging an `UndefinedObjectError` with no lead as to why a supposedly-working `create_all()` call would ever produce one. Fixed now (the new migration creates these 3 types for real, via plain `sa.Enum(...)` which does self-create) — but the underlying lesson generalizes: a `create_type=False` declaration is a claim that something else guarantees the type exists, and this repo had nothing that did. **This dev DB itself still has no `alembic_version` table and is still exactly as this paragraph originally described** — reconciling it (via `alembic stamp` at the right revision, following this same paragraph's own worked example for the `User` migration) is a separate, not-yet-done action; this session only verified and fixed the migration *files*, per explicit instruction not to touch the dev DB.
- **`alembic upgrade head` is idempotent for the real, every-deploy case (already-at-head, no version-tracking loss) — but is NOT defensively idempotent against a lost/reset `alembic_version` table, and that's true of the whole pre-existing chain, not something the migration-drift session's fix introduced.** Verified both scenarios live on a scratch Postgres 16 container (never the dev DB) after the migration-drift fixes above landed: (1) **the scenario that actually happens on every real deploy** — running `alembic upgrade head` a second time against a DB already at head produced zero `"Running upgrade"` lines and exit code 0, because Alembic checks `alembic_version` first and skips every already-applied revision entirely; this holds regardless of what any individual migration's SQL does, since the SQL simply never re-executes. (2) **A narrower, rarer recovery scenario** — manually deleting the `alembic_version` row (simulating lost version tracking against an already-fully-migrated schema) and re-running `alembic upgrade head` fails hard, with `psycopg2.errors.DuplicateObject: type "approvalstatus" already exists` — but critically, **this fails at the very first, oldest, pre-existing migration (`7e3773979374`)**, not at the new CI/CD migration this session added. Every migration in this chain except `db3efa7d4d82`'s two defensively-wrapped `CREATE TYPE ... DO $$ ... EXCEPTION WHEN duplicate_object THEN null; END $$;` statements uses plain `sa.Enum(...)`/`op.create_table(...)`, none of which tolerate being replayed against an already-migrated schema. The new migration (`609092d6978f`) matches this same pre-existing convention exactly — it is neither more nor less fragile than the rest of the chain under version-tracking loss. **Don't treat this as something to fix reactively on the new migration alone** — if defensive idempotency against lost tracking is ever wanted, it needs to be a chain-wide convention (e.g. wrapping every `CREATE TYPE` the way `db3efa7d4d82` already does, or a documented recovery runbook using `alembic stamp`), not a one-off patch on the newest file.
- **Drift-detection test — built (2026-08-02), `backend/tests/test_schema_drift.py`.** Was proposed and sized earlier the same day (originally as a "not built yet" entry, superseded by this one) as a schema-vs-models CI check that would have caught this whole session's migration-drift problem months earlier. Uses Alembic's own `compare_metadata(migration_context, target_metadata)`: creates a throwaway, real sibling Postgres database on the same server the rest of the test suite already talks to (`CREATE DATABASE schema_drift_check_<random>`, always dropped in teardown — never the shared dev/test `dataops` database itself), runs `alembic upgrade head` against it from empty, then diffs the result against `Base.metadata` (both `models.all_models` and `models.cicd` imported, same as `migrations/env.py`). Empty diff list passes; any diff fails with the exact `(action, table, column)` tuples printed, not just "something differs." One real, non-obvious bug hit while building it: `migrations/env.py` reads `DATABASE_URL` directly from `os.environ` (not from the `Config` object `command.upgrade()` is given), so the test has to actually mutate and restore the process's real env var around the migration call — passing the throwaway DB's URL via the `Config` object alone would silently do nothing and the migration would target whatever `DATABASE_URL` normally points at. A second, separate bug: constructing the throwaway/admin connection URLs via `str(sqlalchemy_url_object)` produced a literal `"...://user:***@host/db"` string — SQLAlchemy's `URL.__str__` masks the password by default — which psycopg2 then dutifully tried to authenticate with verbatim; fixed by using `url.render_as_string(hide_password=False)` instead. **Verified both directions the way a drift test must be, per explicit instruction ("a drift test that can't go red is worse than none")**: passed clean against the current, already-reconciled migration chain; then a deliberate, uncommitted one-line drift (`Tenant.drift_test_deliberate_column`, no matching migration) made it fail with exactly `('add_column', None, 'tenants', Column('drift_test_deliberate_column', ...))` — the precise, actionable diff, not a vague error; reverted, confirmed green again, confirmed via `git diff` the model file was byte-identical to before. Caveats carried over from the original proposal, still true: `compare_metadata()`'s default config doesn't diff `server_default` values or enum *value* changes as aggressively as table/column/index existence; this check only catches migrations-vs-models drift, not migrations-vs-any-specific-already-deployed-database drift (a separate, harder problem). **Not yet wired into actual CI** (no CI pipeline exists in this repo to wire it into) — it's a real, runnable pytest test today (`docker exec dataops_backend python -m pytest tests/test_schema_drift.py`), ready to be added to whatever CI gets built later.
- **Frontend: don't use `--surface` as a text color, even though it "looks white" in light mode — it's a background-purpose token that flips to a dark navy in dark mode.** Found live via Playwright screenshots, not code review: `.btn-primary` and `.toast` both used `color: var(--surface)` for their light text-on-Midnight-background styling, which worked by coincidence in light mode (`--surface` = near-white `#FFFEF5`) but broke in dark mode once `--surface` was correctly redefined to a dark card-background color (`#0F2440`) — primary buttons rendered as unreadable dark-text-on-dark. Fixed with a dedicated `--on-dark` token (tokens.css) that stays a constant near-white in both themes, for text sitting on permanently-dark surfaces (Midnight buttons/toasts/tooltips, the sidebar once Phase 7 builds it). The same class of bug hit `tr.selected`'s `--ocean-50` (a light-mode-only near-white tint with no dark override, rendering as a jarring white bar in dark tables) — now redefined as a translucent overlay in both dark-mode blocks. **When adding any new component with a permanently-dark background (not one that flips with theme), use `--on-dark` for its text, not `--surface` or a bare light hex — and check every color token actually has a sane dark-mode value, not just the ones that happen to already be redefined.**
- **Frontend: CSS class combos don't compose the way you'd expect when both classes set the same property at equal specificity — later-in-file wins, not "more specific intent."** `<Input error="...">` renders `className="input-hint text-danger"`, expecting the error's red to show — but `.input-hint` (defined later in `components.css`, in the Input section) has the same specificity as `.text-danger` (defined earlier, in the Typography section) and so silently overrides it back to muted gray, making error text invisible-as-an-error. Caught via screenshot, not code review — it doesn't error, it just silently shows the wrong (still legible) color. Fixed with a compound selector (`.input-hint.text-danger { color: var(--danger); }`) that wins regardless of file order. **Any time two utility-ish classes are combined on one element to override a property, use a compound selector for the override, not two same-specificity single classes and hope for the right source order.**
- **Once implemented, `EmailLoginCode.request_ip` (Phase 3's email-code auth design, `FRONTEND_BUILD_PLAN.md`) must not trust the raw client socket address once this app sits behind any reverse proxy or load balancer in production.** A naive `request.client.host` reads the proxy's own IP, not the real caller's — silently collapsing the per-IP rate limit (20 requests/hour) into a single shared bucket for every user behind that proxy, or worse, making it trivially bypassable if a client can spoof `X-Forwarded-For` and the app trusts it blindly without validating which hop it came from. Confirm the real deployment topology (which proxy, how many hops, whether it sets `X-Forwarded-For`/`X-Real-IP` and can be trusted to do so honestly) before wiring this up — don't ship the per-IP limiter against `request.client.host` unexamined the day this gets implemented.
- **Never create a `redis.asyncio` client as a module-level global at import time — it binds to whatever event loop happens to be running (or none) at that moment, and breaks with `RuntimeError: Event loop is closed` the first time it's used from a different loop.** This is invisible in the real app (uvicorn runs one loop for the whole process, so it never matters) but bites hard under pytest-asyncio, where a module-level client created during test collection/import can end up bound to a loop that a later test's loop machinery has since closed. `services/auth_service.py` hit this exactly (`_redis = aioredis.from_url(...)` at module scope) — fixed with a lazy singleton that checks `asyncio.get_running_loop()` against the loop it was created for and transparently recreates the client if they differ (`_redis()` function, not a bare module variable). Apply the same pattern to any future `redis.asyncio`/similar async-client usage added directly to application code (as opposed to Celery's own internal Redis usage, which manages this itself).
- **`dataops-agent/.gitignore`'s Python-template `lib/` pattern (line 17, under "Distribution / packaging") silently swallows any directory literally named `lib` at any depth — including legitimate frontend source.** `frontend/src/lib/api.ts` (Phase 6) was untracked from the moment it was created; `git add` on it appeared to succeed with no error, and it only surfaced when `git status` after a broad add showed the file missing from the diff entirely. Fixed with an explicit re-include (`!frontend/src/lib/` + `!frontend/src/lib/**`) after the Node/Next.js block. **If you ever create a new directory literally named `lib` anywhere under `dataops-agent/frontend/`, verify it's actually tracked** (`git status` or `git check-ignore -v <path>`) rather than trusting a clean `git add` exit code — the same Python-template origin as the historical node_modules/.next incident (see Archive), just a narrower blast radius this time.
- **Never run `npm run build` (production) while `npm run dev` is running in the same frontend directory — both write into the same `.next/`, and the resulting collision corrupts the dev server's build manifest.** Hit live during Phase 6: ran `npm run build` to verify a clean production build while the dev server (started earlier for live UI testing) was still up in the background; the next few page loads in the browser came back as raw "Internal Server Error" pages, and the dev server's log filled with `ENOENT: ... _buildManifest.js.tmp.<random>` errors — Turbopack's dev server writes/renames temp manifest files, and the concurrent production build racing on the same directory broke that. Fixed by killing the dev server, deleting `.next/` entirely, and restarting `npm run dev` fresh. If you need both a build check and a live dev server in the same session, either run them sequentially (build, confirm clean, *then* start dev) or point one of them at a different directory/copy — don't run them concurrently against the same `.next/`.
- **Run `git status` before ending any session — a dirty tree means verified fixes may be sitting uncommitted, and that risk compounds silently across sessions.** This happened for real: 9 files' worth of real, already-verified fixes from this project's very first testing round (Dockerfile PYTHONPATH, connector_manager.py's internal-DB guard, schema_profiler.py's username fix, rule_engine.py's table name fix, python_runner.py's FileConnector/MySQLConnector fixes, and three cicd_*.py files) sat uncommitted for the rest of the session because the repo had zero git history at the time they were made, and once that context got summarized away, nothing prompted a `git status` check to notice they were still pending. Worse: when `services/tasks.py` was later touched again for an unrelated fix (the pipeline false-success bug), its own still-uncommitted `engine.dispose()` fix from that same earlier round rode along into the new commit's diff *without being mentioned in that commit's message* — a real fix, correctly committed, just silently uncredited and only discovered by explicitly diffing that commit against what its message claimed to change. Don't trust memory/the todo list alone to track what's pending commit across a long or compacted session — `git status` is the actual source of truth, and it's cheap to check.
- **A provider model deprecation can look identical to a billing/quota problem — `limit: 0` doesn't mean what it sounds like.** This project spent multiple sessions treating Gemini's `limit: 0` free-tier error as an account/billing/key issue (tried key rotation, a fresh Google Cloud project, etc. — see Archive) before finding the real cause: `gemini-2.0-flash` had been deprecated by Google and its free tier removed, so *every* key in *every* project was always going to show `limit: 0` against that specific dead model, regardless of billing state. The fix was a model name change (`gemini-2.0-flash` → `gemini-3-flash-preview`), not anything account-side. **If a fresh key in a fresh project still shows `limit: 0`, suspect the MODEL first, not the account** — check the provider's deprecation/model-status page before spending another session on billing troubleshooting.
- **Gemini 3's real measured latency is 13-26s even for trivial prompts, driven by an internal "thinking" pass — don't assume a new model's speed matches the one it replaced.** `PRIMARY_LLM_TIMEOUT_SECONDS` was tuned to 15s against Gemini 2's assumed profile; live-measuring `gemini-3-flash-preview` after the model swap found even "what is 2+2?" took 13-26s across 4 samples and burned 94-99 reasoning/"thinking" tokens doing it — meaning the model would lose the 15s race to the Groq fallback almost every time, silently defeating the point of fixing it as primary. Raised to `PRIMARY_LLM_TIMEOUT_SECONDS=30` and added `GEMINI_THINKING_BUDGET=0` (a real `ChatGoogleGenerativeAI` constructor field in the installed library version) to cap reasoning-token spend — confirmed via `thinking_budget=0` live tests that it reliably eliminates reasoning tokens, but it is **not proven to reliably reduce wall-clock latency** (one measured sample was slower with it capped than without), so keep it for the token-cost reduction, not as a latency fix. **Any time a model is swapped (deprecation, provider migration, a newer version), re-measure real latency live before trusting an existing timeout constant tuned for the old model** — this class of regression is invisible in code review, only live timing samples catch it.
- **`iteration_count` (an agent's own graceful-stop check) and LangGraph's `recursion_limit` almost never count the same thing — a "safety cap" that isn't calibrated against the real supersteps-per-iteration multiplier isn't a safety cap.** `agent_node`'s `iteration_count` incremented once per agent_node visit, but each real tool-calling round costs 3 LangGraph supersteps (agent → approval_gate → tools) — so LangGraph's own default `recursion_limit=25` was reached after only ~8 real iterations, well before the old `iteration_count > 20` check could ever fire. The result: any runaway tool-calling conversation always crashed with an unhandled `GraphRecursionError` first, never hit the graceful message the code appeared to guarantee. Found live only because Gemini 3's tool-calling failure mode (see the Known-broken row above) happened to make a real conversation loop far enough to hit it — this bug likely predates that by a long time, just never got exercised deeply enough before. Fixed with explicit, mutually-derived constants (`MAX_AGENT_ITERATIONS`, `AGENT_RECURSION_LIMIT` in `agent/dataops_agent.py`) and a regression test that asserts the real numbers stay apart, not just that the happy path works. **Any future change to either constant must re-derive the other** — the relationship is `supersteps ≈ 1 + 3*(MAX_AGENT_ITERATIONS+1) + 2`, and `AGENT_RECURSION_LIMIT` should sit comfortably above that, not just barely above it.
- **Reading a prototype's CSS for reference doesn't mean it made it into this project's stylesheet — check, don't assume.** Phase 9's KPI cards used `.stats-grid`/`.metric-card`/`.metric-value`/`.metric-change`, all read directly from the master design prototype during planning — but Phase 2 only ported a specific named "10-component core library" (Button, Card, Badge, Input/Select, Table, Modal, Toast, Tabs, Progress, Skeleton) into `components.css`, and these page-layout utility classes were never part of that list. The result: real components, real data, correct React — rendered as unstyled stacked rows instead of a grid, because the CSS simply didn't exist. Code review wouldn't have caught this (the classNames looked entirely reasonable); only the Playwright screenshot did. **Before using a class name pulled from `wunomo-ai MASTER DESIGN.html`, grep `components.css` for it first** — don't assume "I saw it in the prototype" means "it's in our stylesheet."
- **`.grid` and `.grid-N` (e.g. `.grid-2`, `.grid-3`) are two separate classes that must both be applied — `.grid-2` alone sets `grid-template-columns` but not `display: grid`.** Same root cause as the `.stats-grid` gotcha above (a plausible-looking className that silently does less than expected): Phase 9's two-chart row used `className="grid-2"` alone and rendered as stacked full-width blocks instead of two columns, while the same phase's three-card bottom row (built with an inline `style` instead) rendered correctly by accident. Fixed by using `className="grid grid-2"` — check for this exact pattern (base `.grid` + a modifier) any time a multi-column CSS-grid layout looks like it collapsed to one column.
- **A failed `fetch` (expired JWT, network error, 401/500) in a React Query hook renders as an all-zeros empty state, not an error — and nothing in this codebase currently distinguishes the two.** Found live during Phase 9 verification: a JWT expired mid-session (60 min lifetime) while debugging, and the dashboard silently rendered every KPI as `0`/`—`/empty-state text, indistinguishable from "this is a genuinely brand-new tenant with no data yet" — confirmed via `curl` that the API was actually returning `401 Invalid token`, not real zeros. This isn't Dashboard-specific — every page built so far uses the same `token ?? fallback` pattern with no shared 401 handling, so any page will have this same failure mode once its JWT expires mid-session. **[RESOLVED: Phase 12 pre-work]** — see the "Centralized 401 handling" Status Table row below.
- **A browser console "blocked by CORS policy" error can mean the real response was a 500, not a CORS misconfiguration — check the actual HTTP status via a non-browser client before touching `CORSMiddleware`.** Found live verifying Phase 12's Pipelines screen: deleting a pipeline with run history failed in Chrome with `No 'Access-Control-Allow-Origin' header is present`, which reads exactly like a CORS config bug — but `allow_methods=["*"]`/`allow_origins=settings.cors_origins` were already correct, and a direct `node`/`curl` `DELETE` against the same endpoint (bypassing the browser's CORS enforcement entirely) revealed the real cause: a `500` from an unhandled `IntegrityError` (see the `DELETE /pipelines/{id}` Status Table row). FastAPI's error response path for an unhandled exception doesn't carry the CORS headers the success path does, and Chrome reports that missing-header response as a CORS violation rather than surfacing the real 500 — a genuinely confusing failure mode since nothing about the message points at the backend exception. **Any time a DELETE/PUT/PATCH from the browser shows a CORS error but GET/POST to the same API work fine, reproduce the exact request with `node -e 'fetch(...)'` or `curl` first** — a real CORS bug would fail the same way outside the browser's enforcement (it wouldn't, since curl doesn't enforce CORS) or show consistent behavior across methods; a masked 500 will show its real status immediately once the browser's CORS layer is out of the picture.
- **The `backend` container runs plain `uvicorn main:app` with no `--reload` flag — editing a mounted source file does NOT take effect until the container is restarted, even though the live volume mount makes it look like it should.** Cost real time during Phase 12's lineage-sync work: edited `connector_manager.py`/`dag_manager.py`/`governance.py`, ran a live `curl`/`node fetch` verification against the running `dataops_backend` container, and got an empty graph with zero related log lines — not an error, just silently running the pre-edit code from whatever was last imported when the process started (7+ hours earlier). `docker compose exec backend python -m pytest` was unaffected (a fresh `python` process re-imports from disk every time), which is why the regression tests passed while the live curl check against the long-running server didn't — a confusing split-brain if you don't know to expect it. **Any time a live/curl/Playwright verification against the running stack doesn't reflect a just-made backend edit, restart the backend container (`docker compose restart backend`) before concluding the code is wrong** — check this before spending time debugging the change itself.
- **`docker compose restart` does NOT reload `.env` file changes — it restarts the existing container with whatever environment was captured at its last `create`/`recreate`, silently ignoring anything edited since.** Found live while switching `PRIMARY_LLM_MODEL`: updated all three `.env` files, ran `docker compose restart backend`, and every subsequent chat request kept using the *old* model — not visible from the API response alone (`provider: "gemini"` looked correct), only caught by cross-checking `llm_usage_events.model` in the DB and noticing it still said the pre-switch model name. `docker compose restart` reuses the container as-is; only `docker compose up -d <service>` (recreates if the compose config or referenced `.env` changed) or `docker compose up -d --force-recreate <service>` (unconditional) actually re-reads `env_file:`. **Any time you change a `.env` value, use `up -d --force-recreate <service>`, not `restart`** — and if you're not sure whether a previous `restart` masked a stale value, verify directly (e.g. `docker compose exec backend python -c "from config import settings; print(settings.WHATEVER)"`) rather than trusting that a request "worked."
- **`AIMessage.content` is no longer guaranteed to be a plain string post-LangChain-v1-upgrade — it can be a list of structured content blocks, and this affects plain chat, not just tool calls.** `langchain-google-genai` 3.2.0's Gemini 3.5 responses come back as `[{"type": "text", "text": "...", "extras": {"signature": "..."}}]` (the new home for what used to be a bare `thought_signature` field) instead of a bare string — every prior version of every provider integration in this stack always returned a plain string, so `agent/dataops_agent.py`'s `last_ai.content` and `chat_messages.content` (a VARCHAR column) both assumed it unconditionally. This crashed on save with `asyncpg.exceptions.DataError: invalid input for query argument $6 ... expected str, got list` — and critically, it crashed on **"What is the capital of France?"**, a plain non-tool-calling prompt, not something exercised by tool-calling-specific tests only. Fixed with `_content_as_text()` in `dataops_agent.py`, extracting `.text` from any `type: "text"` blocks. **Any time a message-content-touching code path is added or a provider/library is upgraded, test a plain non-tool prompt explicitly** — tool-calling-focused testing alone would not have caught this, since the crash's trigger condition (list-shaped content) is independent of whether a tool was involved.
- **This project's Gemini API key/project is capped at a hard 20 requests/day on the free tier — and, corrected from an earlier wrong theory in this same file, that cap is NOT specific to `-preview`-suffixed models.** Originally found on `gemini-3-flash-preview` (`limit: 20, model: gemini-3-flash`, real 429 error body) and assumed to be a preview-model-only restriction, so the primary model was switched to `gemini-3.5-flash` (GA, no `-preview` suffix) to escape it. **That assumption was wrong**: later the same session, `gemini-3.5-flash` hit the identical `limit: 20, model: gemini-3.5-flash` error too — direct counter-evidence, not a guess. The real explanation is unresolved: most likely this specific key/project sits on a flat 20 req/day free-tier ceiling applying to every Gemini 3.x model tried so far, independent of preview/GA status — not the 1,500 RPD figure that was assumed to apply. Whether a genuinely different (higher) tier exists for this project and how to reach it is unknown without checking Google AI Studio's own quota/billing dashboard directly (no programmatic way to query it was found). The model switch to `gemini-3.5-flash` was still real and worth keeping — dramatically faster (1.1-2.8s measured vs. the preview model's 13-26s) — just don't expect it to have solved the quota ceiling. **Budget Gemini calls assuming ~20/day total until this is actually confirmed with the user's Google AI Studio dashboard** — don't repeat the mistake of assuming a model-name pattern (preview vs. GA) explains a quota error without testing the specific model actually seeing the error.
- **`langchain-google-genai` (confirmed on 3.2.0) returns dict/list-typed tool arguments as JSON-encoded strings, not native Python objects — a bug in its own function-call parsing layer, not in Gemini, not in our tool schemas.** Proven by process of elimination: the tool's own LangChain-generated schema correctly declares the param `"type": "object"`; a raw Gemini API call (bypassing LangChain entirely, direct `httpx` POST) for the identical request returns a genuine nested JSON object; only once the response passes through `langchain_google_genai`'s parsing does it arrive as a string — reproduced deterministically across multiple prompts, including one with an explicit, unambiguous target value, so it isn't prompt-ambiguity either. Affects any tool with a `dict`- or `List[...]`-typed parameter (7 tools across 5 files in this codebase: `register_data_source`, `create_data_contract` ×2, `request_approval`, `create_pipeline`, `create_quality_rule`, `get_kpi_summary`, `generate_sql_transform`, `standardize_dataset`). Fixed centrally in `agent_node`'s existing tool-call loop (`_coerce_stringified_object_args()`, `dataops_agent.py`) rather than touching each tool — it looks up each tool's own declared schema (`tool.args`) to know which params are object/array-typed, and `json.loads()`s any that arrived as a string. **If Groq is primary, this doesn't manifest** (only reproduced via Gemini) — don't assume a dict-arg tool "works" just because it was tested under Groq fallback.
- **`config` is a reserved parameter name in LangChain's `Runnable`/`Tool` interface — a tool function parameter literally named `config` gets silently dropped before the underlying function is ever called, regardless of what value the LLM sent.** `StructuredTool._arun(self, *args, config: RunnableConfig, run_manager=None, **kwargs)` treats any `config` keyword as the framework's own `RunnableConfig`, not as a regular tool argument, so it's consumed separately and excluded from `**kwargs`. Found while writing a regression test for the dict-arg-stringification bug above: a fake tool with a `config: dict` parameter failed with `TypeError: missing 1 required positional argument: 'config'` even though the value was correctly present and correctly typed right up until the actual function call — renaming the test's parameter to `payload` fixed it immediately, isolating the cause. **`orchestration_tools.py`'s real `create_pipeline(tenant_id, name, source_id, config: dict)` has this exact collision** — its `config` argument has never once reached `DAGManager.create_pipeline()` correctly, independent of anything else wrong with that call site; renamed to `pipeline_config` (matching the real `DAGManager` method's own parameter name) as part of the 22-site tool punch-list fix. **Never name a `@tool`-decorated function's parameter `config`** — pick anything else (`pipeline_config`, `settings`, `options`, etc.).
- **Groq's daily TPD (tokens-per-day) quota is a rolling window, not a fixed daily reset — a successful cheap probe after a 429 does NOT mean the quota is meaningfully free again, and a bigger request right after can re-exhaust it instantly.** Found live in the Phase 10 session: after a `429` reporting "try again in ~14m," a tiny no-tool probe (`"Say OK."`) succeeded a few minutes later — read as "the quota freed up." It hadn't, in any durable sense: the very next request (a real tool-calling chat message, much larger token cost from the full system prompt + tool schemas) immediately 429'd again, this time reporting `Used 99968/100000` and **"try again in 1h5m."** The window is sliding/rolling (old usage ages out continuously, freeing a few tokens at a time), not a hard reset at a fixed daily boundary — a small cheap request can slip through a brief trickle while the budget for anything larger is still effectively zero. **Don't treat one successful cheap request as proof the quota is usable again** — if the next real request 429s with a large "try again" figure, the trickle is gone and the wait is real; budget for the worst case (the large figure), not the optimistic one (the probe that happened to succeed). Also confirms Groq's TPD budget is shared at the **organization** level (`org_01km8sb853e2tvhp21df5zga6f` in every error seen across every session so far) — every tenant/test/session in this project draws from the same pool, so cumulative usage across unrelated work compounds within a day.
- **A long `sleep` backgrounded with `run_in_background: true` (or a manually-`disown`ed shell sleep) is not reliable for waits longer than a couple minutes in this environment — it can report "completed" almost immediately with an empty log, without the sleep actually running to completion.** Found live waiting out a Groq quota cooldown: a `(sleep 720 && node combined-verify.js) &` backgrounded command reported `completed, exit 0` after roughly 2 real minutes, with a 0-byte log — the sleep never ran to term. Switched to a `until <curl probe succeeds>; do sleep 30; done` polling loop (also `run_in_background: true`, no manual `disown`) instead, which behaved correctly and fired one clean notification when the real condition (a successful chat response) was met. **For any real external-system wait in this project (LLM quota resets, webhook delays, etc.), prefer a polling `until`-loop over a bare long `sleep`**, even when backgrounded — a bare sleep's completion signal cannot be trusted to mean the duration actually elapsed.
- **`User.is_active` is only enforced on the password login path (`resolve_password_login()`, fixed in Phase 15) — Google login and email-code login (`resolve_identity()`) still don't check it at all.** — **[RESOLVED: Phase 19, P0 item 3(a)]** `resolve_identity()`'s two lookup queries now both filter `User.is_active.is_(True)`, matching `resolve_password_login()`'s exact pattern — see the Status Table's "`resolve_identity()` missing `is_active` filter" row for the fix, both regression tests, and the live-server verification. All 3 login methods now consistently block a deactivated/removed member. This still doesn't touch any JWT already issued before removal — that's the separate, still-open "no JWT revocation" gap below.
- **A file with no trailing newline can cause `Read` to silently omit its last line from view, and an `Edit` insertion right at that boundary can then orphan that line into whatever gets inserted after it.** Found live while adding `TeamInvite` to `models/all_models.py` (Phase 15): `all_models.py` had no trailing newline, so a `Read` of the tail of `TransformRun`'s class body didn't show its actual final field (`created_at`) — an `Edit` appending the new `TeamInvite` class right after what looked like the last line inserted it *before* that real final line instead, orphaning `TransformRun.created_at` (dropped it entirely, causing real `AttributeError`s at query time — `test_transform_runs_list.py` went from passing to 5 failures) and silently duplicating a stray second `created_at` inside `TeamInvite`. Root-caused via `git diff`, not guesswork — the diff made the exact misplacement obvious immediately. **When appending a new class/section at the end of a model (or any) file, confirm the insertion landed where intended with `git diff` right after the edit** — don't just trust that the tool call succeeded, especially for files that might lack a trailing newline (Python files at the end of an editing session are a common case).
- **Running `docker compose restart backend` (or any DB-mutating `docker exec`) while a `docker exec dataops_backend python -m pytest` is still executing in the background corrupts that test run — spurious failures and a wildly inflated reported duration, not a real regression.** Found live during Phase 15: backgrounded a full-suite run, then (not knowing it was still in flight) restarted the backend container and ran several manual `docker exec` DB-mutation scripts for live RBAC verification. The backgrounded pytest eventually reported `1 failed, 132 passed in 12034.25s` (**3.3 hours**, vs. the normal ~118s) — `test_catalog.py::test_catalog_source_id_filter` failing with `KeyError: 'count'`. Re-running that single test in isolation immediately after: 5/5 passed in 12s. Re-running the full suite in isolation: 133 passed in 117.81s — completely clean. The concurrent restart/DB-mutation activity was corrupting the exec'd pytest process's DB state/connections mid-run, not exposing a real bug. **Never run `docker compose restart <service>` or other DB-mutating commands against a container while a test suite is still running inside it in the background** — either wait for the backgrounded suite to finish first, or don't background it if you know you'll need the container for something else in the meantime.
- **This project's Resend account is still in sandbox mode — `onboarding@resend.dev` can only deliver to the account owner's own verified address (`pratyakshsoni2005@gmail.com`), not arbitrary recipients.** Confirmed live at the start of Phase 15: a real send to `ps6664@srmist.edu.in` (a different real address) got a real `403` directly from Resend's API — `"You can only send testing emails to your own email address (pratyakshsoni2005@gmail.com). To send emails to other recipients, please verify a domain at resend.com/domains..."` — and the identical call against the owner's own address succeeded with `last_event: "delivered"`. This is invisible from inside the app: `_send_code_email()` (`services/auth_service.py`) deliberately never surfaces delivery failures to the caller (enumeration-safety by design, see Phase 3's rate-limiting design) — the request endpoint always returns `{"status":"sent"}` regardless, so a 403 only shows up in `docker compose logs backend` (`email_code_send_failed`), never in the API response. **Any invite-flow or email-code testing against a real third-party address (not the account owner's own) will silently fail delivery while the app reports success** — until a domain is verified at resend.com/domains and `EMAIL_FROM` switches off `onboarding@resend.dev`, real live-verification of email delivery is only possible against `pratyakshsoni2005@gmail.com` itself. Check `docker compose logs backend | grep resend` after any "was this actually delivered?" question, don't trust the API's `{"status":"sent"}` alone.
- **The same-commit documentation rule (Workflow Rule 6/7) can silently slip during a run of fast, related fixes — check `git log --stat` for code-only commits, not just memory, before calling a session's docs current.** Found during this session's own end-of-day cleanup: 3 consecutive commits from earlier the same day (`332d698` dark theme token redesign, `42b7095` Chart.js `resolveToken()` fix, `a4ef4d7` Team screen apostrophe fix) each changed real, verified code with zero CLAUDE.md/FRONTEND_BUILD_PLAN.md update — all landed back-to-back while chasing one thread (start the dark-theme pass → hit two unrelated bugs along the way → fix both → never circle back to the docs before moving on). Every other commit that same session, both before and after, *did* follow the rule correctly, which is exactly what makes this the dangerous failure mode: it's not "forgot the rule exists," it's "got pulled into a chain of real fixes and the doc-update step quietly dropped off the end of each one." All 3 were retroactively backfilled once caught (see their Status Table rows, explicitly marked retroactive). **A cheap, mechanical check for a future session to run before ending**: `git log --stat --since="<session start>" | grep -B5 "files\? changed"` and scan for commits whose stat block doesn't include `CLAUDE.md` or `FRONTEND_BUILD_PLAN.md` — don't rely on remembering to update docs "as you go" being sufficient on its own.
- **A React hydration-mismatch console warning fires on every page, app-wide, whenever `localStorage`'s theme is `"dark"` and the page does a full reload — a characteristic of the root layout's theme pre-paint script, not a per-page bug.** Found live during Phase 18's AI Employees build, only because that session's verification standard explicitly checked for zero console errors (including after a dark-mode reload) — no prior phase's verification appears to have checked this specifically. Root cause: `app/layout.tsx`'s inline pre-paint script sets `document.documentElement.dataset.theme = 'dark'` synchronously before React hydrates (the whole point — avoids a flash of the wrong theme), but the server-rendered HTML has no `data-theme` attribute at all, so React's hydration diff sees a real client/server mismatch on the `<html>` tag and logs the warning React itself describes as "won't be patched up." **Confirmed app-wide, not AI-Employees-specific**: reproduced identically on `/dashboard` and `/sources`, both long-shipped, previously-verified pages — this has presumably been happening since Phase 7 introduced the theme system, just never caught by a verification pass that checked console output this strictly. Cosmetic only (the page still renders correctly post-hydration; nothing visibly breaks), but it is a real, reproducible defect, not nothing. **Not fixed as of Phase 18** — a real fix would mean touching the root layout's pre-paint mechanism, which is cross-cutting (affects every single page, not isolated to whatever page happens to surface it), so per this project's own "fix now only if small/isolated" rule it wasn't fixed reactively inside an unrelated page's build. If a future session touches theme/hydration logic, or if Phase 19's Polish pass gets to console-error cleanliness, this is the entry to close — likely fix shapes: suppress hydration warning specifically on the `<html>` element (`suppressHydrationWarning`, a real Next.js-supported escape hatch for exactly this documented pattern), or restructure the pre-paint script to avoid a genuine attribute mismatch in the first place.
- **Any test that monkeypatches `agent.dataops_agent.ALL_TOOLS` with a test-only fake tool must also monkeypatch `TOOL_CAPABILITIES`, or the Phase 19 role-permission gate denies it for every role, including in tests that have nothing to do with permissions.** `role_denied_tool_calls()` (`agent/dataops_agent.py`) fails closed — a tool name with no entry in `services/rbac.py`'s `TOOL_CAPABILITIES` is denied for every role — which is exactly the intended production behavior (see the "Unified permission spec" Status Table row), but it means any test using a fake tool name (`fake_echo_tool`, `fake_identity_tool`, etc., the established pattern in `test_agent_graph.py`) needs a matching `monkeypatch.setattr(dataops_agent, "TOOL_CAPABILITIES", {"fake_tool_name": "view"})` alongside the existing `ALL_TOOLS` monkeypatch, and `caller_role="owner"` (or any role — `"view"` grants every role) passed to `run_agent()`. Forgetting this doesn't produce a confusing partial failure — the test's tool call is silently stripped before it ever reaches `ToolNode`, so the symptom looks like "the tool never ran" with no exception pointing at the real cause. This bit every pre-existing test in `test_agent_graph.py`, `test_get_agent_cache_isolation.py`, and `test_chat_sessions.py` when the gate first landed (~15 call sites needed this exact one-line fix) — check for this pattern first if a new or existing agent-graph test's tool call mysteriously stops executing.
- **`POST /api/v1/quality/`'s column target is a top-level `column_name` field, not nested inside `rule_config`.** Tried `rule_config: {"column": "emp_id"}` first (a reasonable-looking guess, since `rule_config` is where every other rule-type-specific setting lives) — the rule was created successfully with no error, but running it produced `"message": "No column specified — skipping"` and a hollow `passed: true` instead of a real check. `api/v1/quality.py`'s `RuleCreate` schema declares `column_name: Optional[str] = None` as its own top-level request field, separate from `rule_config`. Silent, not a crash — the exact "plausible-looking field name that isn't real" failure class already documented elsewhere in this file for `agent/tools/*.py`, just found here in a live API-seeding session instead of a tool call site. **When creating a `not_null`/`unique`/`range`/etc. quality rule via the REST API, pass `column_name` at the top level of the request body, not inside `rule_config`.** **Confirmed, per explicit follow-up question, that this is a raw-API/manual-caller trap only, not a user-facing bug**: both real consumers already get this right — the Quality screen's create-rule form (`frontend/src/app/(app)/quality/page.tsx`) sends `column_name` as its own top-level field, and the AXIOM tool `create_quality_rule` (`agent/tools/quality_tools.py`) declares `column_name` as its own top-level parameter too (so it's part of the schema the LLM sees and fills in correctly, not something buried in a nested `rule_config` object it might guess wrong). Nobody using the actual product can hit this — only someone constructing a raw request body by hand (as this session did, live-seeding data) or guessing the shape from `rule_config`'s general role elsewhere in the same API.
- **Next.js's dev-mode build-status overlay (`<nextjs-portal>`) cannot be hidden with an injected stylesheet — it renders inside a closed shadow root.** Found while capturing real product screenshots for the landing page carousel: a plain `page.addStyleTag({ content: "[data-next-badge-root] { display: none }" })` had no effect even though that selector is exactly what the overlay's own internal CSS uses — because the whole thing lives inside a custom element (`<nextjs-portal>`) with an encapsulated shadow DOM that a page-level `<style>` tag cannot pierce. The fix is to hide the **host element** itself via `page.evaluate(() => document.querySelectorAll("nextjs-portal").forEach(el => el.style.display = "none"))`, which works because the host node is a normal, selectable DOM element even though its internals aren't — re-run after every `page.goto()`, since it's a fresh element on every full navigation. Purely a dev-server/screenshot-tooling concern — this overlay never renders in a production build, so it's not something end users or `npm run build` verification ever see.
- **[RESOLVED: Phase 19 polish session] The landing page (`/`) silently defaulted to dark mode for any visitor whose OS/browser `prefers-color-scheme` is dark, even though light is the signed-off "polished public mode."** Root cause: the root layout's pre-paint script (`app/layout.tsx`) only sets `data-theme` when something is already in `localStorage` — a genuinely first-time visitor gets no attribute at all, and `tokens.css`'s `@media (prefers-color-scheme: dark) { :root:not([data-theme="light"]) { ... } }` rule then treats "no attribute" identically to an explicit `"system"` choice, applying dark tokens whenever the OS prefers dark. This is correct, intended behavior for the *authenticated app* (a fresh user with no stored preference should see their OS's theme) but wrong for the public landing page, which has its own signed-off default independent of OS preference. Fixed with a second, landing-page-scoped pre-paint script (`app/page.tsx`, `LANDING_THEME_PREPAINT_SCRIPT`) that runs after the root layout's script and forces `data-theme="light"` unless an explicit `"light"`/`"dark"` value is already stored (i.e., the visitor previously used a toggle) — same synchronous-inline-`<script>`-as-first-`<body>`-child technique as the root layout's own script, scoped to this one route only. **Live-verified via Playwright, 5 cases**: a dark-OS visitor with empty `localStorage` now gets `data-theme="light"` and a real Pearl Perfect background (`rgb(251,249,228)`); a light-OS visitor unaffected; an explicit prior `"dark"` choice is honored even on a dark OS; an explicit prior `"light"` choice is honored; and `/login` (a different route, still only covered by the root layout's script) is confirmed **unchanged** — still falls through to OS preference when nothing is stored, proving the fix is scoped to the landing page only, not a global default-to-light change that would regress the authenticated app's "System" theme option. | `frontend/src/app/page.tsx`'s `LANDING_THEME_PREPAINT_SCRIPT` vs `frontend/src/app/layout.tsx`'s script vs `tokens.css`'s dark-mode media query.

---

## 4. Workflow Rules

These govern how any Claude Code session (including this one) operates on this repo:

1. **Read this file in full at the start of every session before taking any action.**
2. **Work one issue or feature per session.** Verify each fix against the real running system (start the stack, hit the actual endpoint/flow, don't just eyeball the diff) before moving to the next one. Don't batch multiple unverified changes together.
3. **If verifying a fix surfaces a new or hidden related bug, resolve it in the same session** rather than stopping at the original scope — but still commit it separately (see rule 5).
4. **For any non-trivial feature or architectural change, propose the approach and tradeoffs first and wait for approval before writing code.** This includes anything touching tenant isolation, auth, the approval workflow, or the Celery scheduling mismatch — these are exactly the areas where a quick fix can silently reintroduce one of the gotchas above.
5. **Write or update a regression test alongside every bug fix, in the same commit.** Given current test coverage (auth + smoke only), most fixes in `agent/`, `modules/`, or `services/` will be the first test for that code path — that's expected, not a sign you're doing extra work.
6. **Commit after every individually verified fix**, with a message describing the issue and how it was verified. Don't batch multiple fixes into one commit.
7. **Update this file's Status Table and Known Gotchas section as part of the same commit** whenever something is fixed, added, or newly discovered. Move superseded information into the Archive below rather than deleting it.
8. Given there are no commits yet in this repo, the first commit is an unusually large diff — that's expected, but subsequent commits should follow the one-fix-per-commit rule above.
9. **When a fix supersedes an existing Known-broken row, mark that row `[RESOLVED: <how/when>]` in the *same* commit as the fix — never just add a new row and leave the old one riding.** This has already failed silently three times in this project (the `login()`/`register()` disambiguation rows, and `request_approval`, all fixed sessions before anyone marked them resolved) — each time because the fix's own commit only touched the Status Table, not the matching Known-broken row, and nothing ever prompted a later pass to reconcile them. A stale "known-broken" row is worse than no row at all: it actively tells a future session (or an outside reader, like the Phase 19 user manual) that something is still wrong when it isn't. Before closing out any commit that fixes a documented bug, grep the Known-broken table for that bug's description and update it in the same commit — don't defer it to a later cleanup pass.

---

## Archive

*Superseded information kept for history — do not delete, append new superseded items here as they arise.*

- **LLM fallback routing bug (fixed, pre-dates this document).** `services/llm_service.py` previously had `get_fallback_llm` always constructing a `ChatGroq` client regardless of the configured `FALLBACK_LLM_MODEL`. An inline comment at `llm_service.py:35-39` notes this was fixed to route via `_build_llm(settings.FALLBACK_LLM_MODEL, ...)`, matching whatever model name is actually configured.
- **Prior CLAUDE.md content:** none — the file was empty (0 bytes) before this rewrite. This is the first substantive version.
- **Repo previously had zero git commits.** Everything was staged (via an earlier broad `git add -A`-style setup) but never committed, and the git index carried 11,446 `node_modules/` + 84 `.next/` files because `.gitignore` had no Node/Next.js section. Resolved by adding the Node/Next.js block to `.gitignore`, unstaging those ~11.5k files plus one stray garbage index entry (`4))`, then making an initial commit of the clean scaffold baseline, followed by one commit per already-verified fix (message-history reducer bug, tool-output capping, tenant-id injection). See Status Table and the gotchas above for what's fixed.
- **`get_cicd_status` tool called back into this app's own API unauthenticated (fixed).** It took no `tenant_id` and hit `http://localhost:8000/api/v1/cicd/status/summary` via plain `httpx.get()` with no auth header — always 401'd, but silently returned fake empty defaults since `httpx` doesn't raise on 4xx. Fixed by extracting the endpoint's query logic into `services/cicd_service.py:get_status_summary(db, tenant_id)` and having both the REST endpoint and the tool (now `get_cicd_status(tenant_id: str, query: str = "")`) call it in-process. See Status Table and the new general Gotcha above (no tool should call back into this app's own API).
- **"Frontend can't build" / "Frontend directory missing from working tree" (both resolved, discovered during this session).** A prior commit (`220b07a`, "Remove stale, broken original Next.js placeholder scaffold") already deleted the broken legacy Next.js source these two Known-broken rows described (`chat/page.tsx`/`dashboard/page.tsx`/`login/page.tsx` and their missing `lib/api.ts`/`lib/store.ts`) — confirmed via `git ls-files dataops-agent/frontend`, which now lists only 5 reference/design files (`wunomo-ai MASTER DESIGN.html`, `color scheme.png`, `Wunomo ui walkthrough.pdf`, `wunomo_architecture.html`, `design-proposal.html`), all present on disk. `dataops-agent/frontend/` is a clean slate for `FRONTEND_BUILD_PLAN.md` Phase 2 — no legacy broken source to work around or delete first.
- **`RuleEngine` import bug in `business_rules.py` (fixed).** Imported `from modules.quality.rule_engine import RuleEngine` — no such class exists; the real class is `QualityRuleEngine`. Made the entire `BusinessRules` class unimportable (`ImportError` on module load) until fixed alongside the enum-case bug cluster in the same file. See Status Table and Gotchas.
- **`README.md` claims frontend is "(Coming soon)"** (`dataops-agent/README.md` Architecture section) — this is stale. Frontend source exists (partially — see Status Table "Known-broken") and was staged into git, it's just not present on disk in the current working tree and can't build due to missing `lib/api.ts`/`lib/store.ts`. Treat the README's tech-stack and API-endpoint tables as directionally correct but not exhaustive; the endpoint table in this file's Architecture Summary supersedes it for anything not listed there — see the full per-file agent reports that produced this document for the complete endpoint list (chat, uploads, sources, pipelines, quality, incidents, governance, approvals, analytics, transformations, cicd — ~40+ routes total, README only lists 9).
