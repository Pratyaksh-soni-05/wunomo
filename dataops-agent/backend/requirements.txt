# ─── Web Framework ────────────────────────────────────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12

# ─── Database ─────────────────────────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
psycopg2-binary==2.9.10

# ─── Queue / Cache ────────────────────────────────────────────────────────────
redis==5.2.1
celery[redis]==5.4.0

# ─── LangChain / AI stack ─────────────────────────────────────────────────────
# Bumped 0.3.25 → 1.x (see CLAUDE.md Known-broken/Gotchas for the full writeup):
# langchain-google-genai 2.1.4 had zero handling for Gemini 3's thought_signature
# requirement on multi-turn tool calls; support landed at 3.0.0, which requires
# langchain-core>=1.0.0. Pinned to 3.2.0, not the newer 4.x line — 4.x pulls in
# Google's new unified `google-genai` SDK and forces a pydantic bump that ripples
# into ~every FastAPI schema in this app; 3.2.0 still uses the older
# google-ai-generativelanguage client and already has thought_signature support,
# verified via a dry-run resolving cleanly against the existing pydantic==2.10.2
# pin (zero pydantic/fastapi changes). langchain-core pinned explicitly (not left
# purely transitive) since it's the version-sensitive piece here.
langchain==1.3.14
langchain-core==1.4.9
langchain-google-genai==3.2.0
langchain-groq==1.1.3
langchain-community==0.4.2
langgraph==1.2.9

# ─── Data processing ──────────────────────────────────────────────────────────
pandas==2.2.3
# numpy 2.x is now compatible with langchain 0.3.25+
numpy==2.1.3
openpyxl==3.1.5
xlrd==2.0.1
pdfplumber==0.11.4
python-docx==1.1.2

# ─── Database connectors ──────────────────────────────────────────────────────
pymysql==1.1.1

# ─── HTTP clients ─────────────────────────────────────────────────────────────
httpx==0.27.2
aiohttp==3.11.7
# Bumped 2.32.3 -> 2.32.5 alongside the LangChain upgrade: langchain-community 0.4.2
# requires requests>=2.32.5.
requests==2.32.5

# ─── Auth / Security ──────────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# ─── Config / Utils ───────────────────────────────────────────────────────────
python-dotenv==1.0.1
apscheduler==3.10.4
croniter==3.0.3
slack-sdk==3.33.3

# ─── Pydantic / Validation ────────────────────────────────────────────────────
pydantic==2.10.2
# Bumped 2.6.1 -> 2.14.2 alongside the LangChain upgrade above: langchain-community
# 0.4.2 requires pydantic-settings>=2.10.1. Confirmed pydantic itself (2.10.2) needs
# no change — this is a pydantic-settings-only bump, still 2.x, no breaking changes
# expected for config.py's Settings(BaseSettings) usage.
pydantic-settings==2.14.2
email-validator==2.2.0

# ─── Testing ──────────────────────────────────────────────────────────────────
pytest==8.3.3
pytest-asyncio==0.24.0
faker==33.0.0
httpx==0.27.2

# ─── Logging / Templating ─────────────────────────────────────────────────────
structlog==24.4.0
tenacity==9.0.0
jinja2==3.1.4
python-slugify==8.0.4